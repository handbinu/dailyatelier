# DailyAtelier 입찰 MVP 구현 계획

## 진행 원칙

- 이 문서는 입찰 MVP의 작업 범위와 승인 순서를 정의한다.
- 각 단계는 **구현 → 테스트 → 변경 내용 및 테스트 결과 보고 → 사용자 확인** 순서로 진행한다.
- 사용자가 현재 단계를 명시적으로 승인하기 전에는 다음 단계의 코드 작업을 시작하지 않는다.
- 승인 과정에서 범위나 API 계약이 바뀌면 먼저 이 문서를 갱신하고 다시 확인받는다.
- 실제 구현은 이 계획 문서에 대한 사용자 승인 이후에만 시작한다.
- 기존 작품 조회 구현 계획은 `PLAN_DONE.md`에 보존한다.

## 0. 입찰 정책 및 API 계약 확정

- [x] 기존 `Art`, `Bid`, `User` 모델과 인증 방식을 기준으로 입찰 정책을 확정한다.
- [x] 입찰 가능 조건을 서버의 단일 기준으로 확정한다.
  - 인증된 사용자만 입찰할 수 있다.
  - `Art.artStatus == 0`인 진행 중 작품만 입찰할 수 있다.
  - 서버 현재 시각이 `bidStartTime` 이상이고 `closingTime` 미만이어야 한다.
  - `현재 시각 < bidStartTime`이면 시작 전, `현재 시각 >= closingTime`이면 마감 후로 차단한다.
  - 판매자 판정은 `Art.artist.user.userId`와 인증 사용자 ID를 비교하며, 본인 작품 입찰은 금지한다.
- [x] 입찰 금액 정책을 확정한다.
  - 기존 `Integer`를 유지하고 정수 원 단위로 처리한다.
  - 허용 범위는 `1원 이상 2,100,000,000원 이하`로 제한한다.
  - 최소 입찰 증분 기능 도입 전까지 현재가보다 `1원 이상` 높으면 허용한다.
  - 현재가와 같거나 낮은 금액은 거절한다.
- [x] `POST /api/arts/{artId}/bids` 계약을 확정한다.
  - 인증: 필수
  - 성공 상태: `201 Created`
  - 요청 DTO 예시:

    ```json
    {
      "bidPrice": 150000
    }
    ```

  - 성공 응답 DTO 예시:

    ```json
    {
      "bidId": 31,
      "artId": 7,
      "bidPrice": 150000,
      "currentPrice": 150000,
      "bidTime": "2026-07-23T18:30:00"
    }
    ```

- [x] API 오류 응답 형식을 확정한다.
  - 공통 형식은 `timestamp`, `status`, `code`, `message`, `path` 필드를 사용한다.
  - `400 INVALID_BID_AMOUNT`: 금액 누락, 정수 형식 오류, 1원 미만 또는 21억 원 초과
  - `401 UNAUTHORIZED`: 인증 정보 없음 또는 유효하지 않은 토큰
  - `403 SELF_BID_NOT_ALLOWED`: 판매자 본인의 작품에 입찰
  - `404 ART_NOT_FOUND`: 존재하지 않는 작품
  - `409 AUCTION_NOT_STARTED`: 경매 시작 전
  - `409 AUCTION_CLOSED`: `artStatus`가 진행 중이 아니거나 마감 시각이 지난 작품
  - `409 BID_TOO_LOW`: 현재가와 같거나 낮은 금액
  - `409 BID_CONFLICT`: 비관적 락 대기 실패·타임아웃 등 재시도 가능한 동시성 충돌
- [x] `GET /api/users/me/bids` 계약을 확정한다.
  - 인증: 필수
  - 성공 상태: `200 OK`
  - Spring `Page` 형식을 사용하며 `page` 기본값은 0, `size` 기본값은 12, 최대 크기는 50으로 제한한다.
  - 로그인 사용자의 입찰만 대상으로 작품별 한 건으로 요약한다.
  - `myBidPrice`는 해당 작품에 대한 사용자의 최고 입찰가, `lastBidTime`은 사용자의 가장 최근 입찰 시각이다.
  - 기본 정렬은 `lastBidTime` 내림차순이며, 동률이면 작품 ID 내림차순으로 정렬한다.
  - `isLeading`은 `myBidPrice == currentPrice`로 판정한다.
  - 항목 DTO 예시:

    ```json
    {
      "artId": 7,
      "artName": "여름의 정원",
      "artistName": "하루",
      "imgPath": "https://example.com/art.jpg",
      "myBidPrice": 150000,
      "currentPrice": 160000,
      "isLeading": false,
      "auctionStatus": "IMMINENT",
      "lastBidTime": "2026-07-23T18:30:00",
      "bidStartTime": "2026-07-20T10:00:00",
      "closingTime": "2026-07-24T10:00:00"
    }
    ```

- [x] 조회용 경매 상태 기준을 확정한다.
  - `ENDED`: `artStatus != 0` 또는 서버 현재 시각이 마감 시각 이상
  - `IMMINENT`: 종료되지 않았고 마감까지 남은 시간이 24시간 이하
  - `ONGOING`: 종료되지 않았고 마감까지 남은 시간이 24시간 초과
  - 기존 프론트 작품 조회의 마감 임박 24시간 기준과 동일하게 사용한다.
- [x] MVP 범위 제외 정책을 확정한다.
  - 등록된 입찰은 취소·수정·철회할 수 없다.
  - 마감 후 낙찰자 확정, `artStatus` 자동 변경 및 결제 연계는 이번 입찰 MVP에 포함하지 않고 백로그에서 별도로 진행한다.

### 0단계 완료 조건 및 승인 게이트

- [x] API 요청·응답 예시와 상태 코드, 목록 집계 기준을 문서화한다.
- 사용자가 계약을 승인하기 전에는 1단계를 시작하지 않는다.

## 1. 입찰 등록 기본 흐름과 트랜잭션

- [x] 입찰 등록용 Repository, Service, Controller, DTO를 구현한다.
- [x] 작품 존재 여부와 경매 시작·마감 시간을 검증한다.
- [x] 인증 사용자가 작품 판매자 본인인지 백엔드 API에서 검증하고, 본인 작품에는 입찰할 수 없도록 차단한다.
- [x] 입찰 금액이 조회된 작품의 현재가보다 높은지 검증한다.
- [x] 입찰 이력 저장과 `Art.currentPrice` 갱신을 하나의 `@Transactional` 서비스 메서드에서 처리한다.
- [x] 검증 실패나 저장 실패 시 입찰 저장과 현재가 변경이 모두 롤백되는지 확인한다.
- [x] 예외를 일관된 HTTP 상태 및 메시지로 변환한다.
- [x] 이 단계에서는 단일 요청 기준의 정확성을 구현하며, 동시 요청 안전성은 2단계에서 별도로 완성한다.

### 1단계 테스트

- [x] 정상 입찰 시 `Bid` 저장과 현재가 갱신을 함께 검증한다.
- [x] 작품 판매자 본인의 입찰 요청이 거절되고 입찰 이력과 현재가가 변경되지 않는지 검증한다.
- [x] 시작 전과 마감 시각 이후 요청이 거절되는지 검증한다.
- [x] 현재가와 같거나 낮은 금액, 잘못된 금액이 거절되는지 검증한다.
- [x] 존재하지 않는 작품과 비인증 요청을 검증한다.
- [x] 트랜잭션 중 실패 시 두 변경이 모두 롤백되는지 검증한다.

### 1단계 완료 조건 및 승인 게이트

- [x] 구현 파일, API 호출 예시, 자동 테스트 결과를 보고한다.
- 사용자가 승인하기 전에는 동시성 제어 작업을 시작하지 않는다.

## 2. 동시 입찰 충돌 방지

### 검토 옵션

1. **비관적 락 — MVP 권장**
   - 입찰 트랜잭션에서 대상 작품을 `PESSIMISTIC_WRITE` 또는 `SELECT ... FOR UPDATE`로 조회한다.
   - 같은 작품의 입찰 요청을 DB가 순서대로 처리하므로, 뒤 요청은 앞 요청이 갱신한 현재가를 다시 확인한다.
   - 장점: 충돌이 실제로 발생하는 경매 도메인에서 동작이 직관적이고, 재시도 로직 없이 현재가 검증과 갱신을 강하게 보장하기 쉽다.
   - 단점: 인기 작품에 요청이 집중되면 대기 시간이 늘고, 트랜잭션이 길어질 경우 락 경합이나 타임아웃 위험이 있다.
   - 적용 원칙: 락 획득 후 검증·저장·가격 갱신만 수행하고 외부 I/O 없이 트랜잭션을 짧게 유지한다.

2. **낙관적 락**
   - `Art`에 `@Version` 필드를 추가하고 갱신 충돌 시 예외를 반환하거나 제한 횟수만 재시도한다.
   - 장점: 충돌이 드문 환경에서는 DB 락 대기 없이 처리량을 확보하기 좋다.
   - 단점: 경매처럼 같은 작품에 요청이 몰릴 수 있는 경우 충돌 예외와 재시도 정책이 복잡해지고, 사용자가 제출한 금액이 재조회된 현재가보다 낮아지는 상황을 별도로 처리해야 한다.

3. **조건부 원자 갱신**
   - `UPDATE art SET current_price = :amount WHERE art_id = :id AND current_price < :amount`의 영향 행 수로 성공 여부를 판정한다.
   - 장점: 짧은 단일 갱신으로 가격 역전을 막을 수 있다.
   - 단점: 입찰 이력 저장과의 순서·롤백 설계, 갱신된 상태 조회, JPA 영속성 컨텍스트 동기화가 더 복잡해진다.

### 확정 방식 및 구현 계획

- [x] 동시성 제어 방식은 **비관적 락**으로 확정한다.
- [x] `ArtRepository`에 입찰 전용 잠금 조회를 추가한다.
- [x] 잠금 획득 이후 서버 시간을 기준으로 경매 상태와 최신 현재가를 다시 검증한다.
- [x] 유효한 요청만 입찰 이력 저장과 현재가 갱신을 수행한다.
- [x] 락 대기 힌트는 3초로 설정하고 락 실패·타임아웃을 `409 BID_CONFLICT`로 변환한다.
- [x] 격리된 H2 DB의 실제 트랜잭션에서 동일 작품을 대상으로 한 동시 요청 테스트를 작성한다.
- [x] 실행 SQL에 `FOR UPDATE`가 적용되는지 확인한다.
- [x] 테스트 결과와 현재 운영 DB 제약에서 비관적 락 권장안을 변경할 사유가 없는지 확인한다.

### 2단계 테스트

- [x] 동일한 현재가를 읽으려는 복수 입찰을 동시에 실행한다.
- [x] 더 낮은 금액이 뒤늦게 현재가를 덮어쓰지 못하는지 검증한다.
- [x] 유효한 입찰 이력과 최종 현재가가 일치하는지 검증한다.
- [x] 서로 다른 작품의 입찰은 불필요하게 서로를 차단하지 않는지 검증한다.
- [x] 성공 및 롤백으로 트랜잭션이 종료될 때 락이 정상 해제되는지 검증한다.

### 2단계 완료 조건 및 승인 게이트

- [x] 선택한 락 방식, 동시 요청 테스트 조건, 최종 DB 결과를 보고한다.
- 사용자가 승인하기 전에는 내 입찰 조회 API 작업을 시작하지 않는다.

## 3. 내 입찰 현황 조회 API

- [x] `GET /api/users/me/bids`를 구현한다.
- [x] 로그인 사용자의 입찰만 조회하고 작품별 내 최고 입찰가로 요약한다.
- [x] 작품 현재가와 비교해 최고가 여부를 계산한다.
- [x] 진행 중, 종료 임박, 종료 상태를 일관된 기준으로 내려준다.
- [x] 최근 입찰순 정렬과 0건 응답을 구현한다.
- [x] 페이지네이션과 페이지 크기 상한 50을 적용한다.
- [x] 작품·작가 정보를 DTO 프로젝션 단일 집계 쿼리로 조회해 N+1을 방지한다.

### 3단계 테스트

- [x] 다른 사용자의 입찰이 노출되지 않는지 검증한다.
- [x] 한 작품에 여러 번 입찰한 경우 한 건으로 요약되고 내 최고가가 표시되는지 검증한다.
- [x] 최고가 여부, 상태, 정렬, 빈 목록과 페이지 경계를 검증한다.
- [x] 비인증 요청이 차단되는지 검증한다.

### 3단계 완료 조건 및 승인 게이트

- [x] 응답 예시, 쿼리 방식, 자동 테스트 결과를 보고한다.
- 사용자가 승인하기 전에는 상세 페이지 연동을 시작하지 않는다.

## 4. 작품 상세 페이지 입찰 폼 연동

- [x] `ArtDetail.jsx`에 실제 입찰 폼을 연결한다.
- [x] 현재가보다 높은 정수 금액만 제출할 수 있도록 클라이언트 기본 검증을 추가한다.
- [x] 비로그인, 본인 작품, 시작 전, 마감 후 상태에서는 입찰을 차단하고 이유를 표시한다.
- [x] 제출 중 중복 클릭을 막고 로딩 상태를 표시한다.
- [x] 성공 시 서버가 반환한 현재가로 상세 화면을 갱신하고 입력값과 안내 메시지를 정리한다.
- [x] 현재가 충돌을 포함한 서버 오류 발생 시 최신 작품 정보를 다시 조회해 사용자가 재입찰할 수 있게 한다.
- [x] 기존 상세 조회, 찜, 이미지 모달 동작을 보존한다.

### 4단계 테스트 및 확인

- [x] 백엔드 API 테스트 결과와 프론트 오류 코드별 처리 로직으로 정상 입찰, 금액·인증·시간·충돌 응답 연결을 확인한다.
- [x] 성공 응답의 `currentPrice`로 상세 상태를 즉시 갱신하는지 확인한다.
- [x] 변경 파일 ESLint와 프론트 빌드를 실행한다.
- [x] 데스크톱 및 모바일 화면에서 비로그인 차단 폼과 오류 안내 레이아웃을 확인한다.

### 4단계 완료 조건 및 승인 게이트

- [x] 주요 UI 상태 화면과 API 연동 결과를 보고한다.
- 사용자가 승인하기 전에는 마이페이지 연동을 시작하지 않는다.

## 5. 마이페이지 입찰 현황 연동

- [x] `BidStatus.jsx`의 `MOCK_BIDS` 의존성과 준비 중 안내를 제거한다.
- [x] `GET /api/users/me/bids`를 호출해 실제 입찰 내역을 표시한다.
- [x] 내 최고 입찰가, 현재 최고가, 최고가/경쟁 중 상태, 작품 상태와 마감 시각을 표시한다.
- [x] 전체, 진행 중, 종료 임박, 종료 필터와 요약 건수를 실제 데이터 기준으로 동작시킨다.
- [x] 로딩, 오류, 재시도, 빈 목록 상태를 구현한다.
- [x] 작품 이미지 fallback과 실제 상세 페이지 이동을 연결한다.
- [x] 진행 중인 작품의 `가격 올리기` 동작을 상세 페이지 입찰 폼으로 연결한다.
- [x] 상태별 요약과 필터가 전체 데이터 기준으로 정확하도록 API의 모든 페이지를 최대 50건씩 조회해 합친다.

### 5단계 테스트 및 확인

- [x] 필터별 목록과 요약 건수가 API 데이터와 일치하는지 확인한다.
- [x] 최고가/경쟁 중 표시와 종료 상태별 버튼 노출을 확인한다.
- [x] 인증 만료, 로딩, 오류, 빈 목록, 이미지 오류 상태를 처리한다.
- [x] 변경 파일 ESLint와 프론트 빌드를 실행한다.
- [x] 데스크톱 및 모바일 화면을 시각 검증한다.

### 5단계 완료 조건 및 승인 게이트

- 주요 UI 상태 화면과 검증 결과를 보고한다.
- 사용자가 승인하기 전에는 최종 통합 검증을 시작하지 않는다.

## 6. 최종 통합 검증 및 정리

- [x] 상세 페이지에서 입찰한 내역이 마이페이지에 반영되는 전체 흐름을 검증한다.
- [x] 시작 시각과 마감 시각 경계, 연속 입찰, 동시 입찰 시나리오를 다시 검증한다.
- [x] 백엔드 전체 테스트를 실행한다.
- [x] 프론트 전체 빌드와 관련 ESLint를 실행한다.
- [x] Git diff를 검토해 기존 사용자 변경과 `PLAN_DONE.md`가 보존되었는지 확인한다.
- [x] 구현 중 확정된 API 정책과 동시성 전략을 이 문서에 반영한다.
- [x] 사용자 확인 전에는 커밋하거나 push하지 않는다.

### 6단계 완료 조건 및 최종 승인 게이트

- 전체 변경 요약, 테스트 결과, 남은 제한 사항을 보고한다.
- 최종 승인 후에만 사용자가 요청한 범위에서 커밋 등 후속 작업을 진행한다.
