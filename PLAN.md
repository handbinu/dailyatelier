# DailyAtelier 개발 계획

> [!IMPORTANT]
> 완료된 계획과 검증 결과는 `PLAN_DONE.md`에서 관리한다.
> 작업 완료 시 계획과 검증 결과를 `PLAN_DONE.md`에 먼저 보존한 뒤 `PLAN.md`에서 해당 계획을 제거한다.
> 완료 문서 커밋 전에는 두 파일을 대조해 완료 기록 누락과 중복이 없는지 확인한다.
> 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
> 새 작업 계획은 사용자 요청 범위가 확정된 뒤 이 문서에 추가한다.

## 작가 마이페이지 메뉴 구조 분리와 전용 route guard

### 목표와 범위

일반 회원과 작가의 마이페이지 탐색을 의미 있는 메뉴 그룹으로 분리하고, 작가 전용
라우트에 공통 역할 guard를 적용해 직접 URL 접근도 일관되게 처리한다. 로그인 여부는
기존 `PrivateRoute`와 로그인 복귀 계약이 먼저 처리하며, 서버의 `ROLE_ARTIST` 권한
검사는 그대로 유지한다. 주문 UI 개편, 백엔드 권한 변경과 새로운 작가 기능은 제외한다.

### 현재 코드와 적용 범위

- `App.jsx`의 `/upload`, `/mypage/manage-arts`, `/mypage/artist-review`,
  `/mypage/sales-orders`는 현재 로그인 여부만 확인하는 공통 보호 영역에 있다.
- `UploadSell.jsx`와 `ArtistReview.jsx`는 개별 alert·redirect를 수행하고,
  `ManageArts.jsx`와 `SalesOrders.jsx`는 API 권한 오류에 주로 의존한다.
- `MyPage.jsx`는 일반 메뉴 7개 뒤에 작가 메뉴 3개를 이어 붙이고 배열 인덱스로만
  스타일을 구분하며, 다른 라우트로 이동하는 메뉴에 tab 의미를 사용한다.
- 구현 대상은 `App.jsx`, 공통 auth route guard, 위 작가 전용 페이지의 중복 권한 처리,
  `MyPage.jsx`·`MyPage.module.css`와 직접 관련된 컴포넌트 테스트로 제한한다.

### 1단계 — 작가 전용 route guard

- [ ] 기존 `PrivateRoute` 안에 작가 역할을 확인하는 공통 `ArtistRoute`를 추가한다.
- [ ] 비로그인은 기존 로그인 복귀 계약으로 원래 작가 URL을 보존하고, 로그인한 일반
      회원은 작가 화면을 렌더링하거나 작가 API를 호출하기 전에 안전한 화면으로 이동한다.
- [ ] 일반 회원의 권한 부족 안내는 alert와 render 중 navigate 없이 일관된 라우팅
      state 또는 접근 가능한 화면 안내로 제공하고 무한 redirect를 방지한다.
- [ ] localStorage의 `userStatus`는 프런트 표시·진입 제어에만 사용하고 보안 권한의
      근거는 기존 백엔드 `ROLE_ARTIST` 검사로 유지한다.
- [ ] 네 작가 전용 라우트를 공통 guard 아래로 묶고 페이지 내부의 중복 역할 redirect만
      제거하되 API 401·403 오류 처리는 유지한다.
- [ ] 비로그인, 일반 회원, 작가 회원과 직접 URL 접근을 route 컴포넌트 테스트로 검증한다.

### 2단계 — 마이페이지 메뉴 구조 분리

- [ ] 일반 회원 메뉴와 작가 관리 메뉴를 별도 데이터·마크업 그룹으로 나누고 각 그룹에
      이해 가능한 제목과 navigation 이름을 제공한다.
- [ ] 실제 페이지 이동에 맞게 링크와 `aria-current="page"`를 사용하고 기존 tab 역할과
      배열 인덱스 기반 작가 스타일 판정을 제거한다.
- [ ] 일반 회원에게는 작가 관리 그룹을 노출하지 않고 작가에게는 작품 등록, 작품 관리,
      작품 리뷰와 판매 주문의 진입 동선을 한 그룹에서 제공한다.
- [ ] 현재 위치, hover와 focus-visible을 시각적으로 구분하고 기존 프로필·포인트·요약
      콘텐츠와 라우트 동작을 유지한다.
- [ ] 768px 이하와 브라우저 확대에서 메뉴가 잘리거나 불필요한 페이지 가로 스크롤을
      만들지 않도록 그룹별 wrap 또는 안전한 축소 화면 탐색을 적용한다.
- [ ] 일반·작가 메뉴 노출, 그룹 이름, 현재 위치와 기존 링크 목적지를 컴포넌트 테스트로
      검증한다.

### 3단계 — 회귀 검증과 문서

- [ ] lint, 단위·컴포넌트 테스트, production build와 `git diff --check`를 통과한다.
- [ ] 브라우저에서 로그아웃·일반·작가 계정의 네 전용 URL 직접 접근과 로그인 후 복귀를
      확인하고 권한 없는 화면 또는 API가 먼저 노출되지 않는지 확인한다.
- [ ] 1440px, 768px, 390px과 200% 확대에서 두 메뉴 그룹, 현재 위치, 키보드 탐색과
      페이지 가로 overflow를 확인한다.
- [ ] 기존 작가 페이지의 정상 진입과 서버 401·403 보호가 유지되는지 확인한다.
- [ ] 검증 후 작가 마이페이지 메뉴 구조 분리 BACKLOG 항목만 완료 처리하고 주문 UI와
      배송지 dialog 항목은 미완료로 유지한다.

### 완료 기준

- 네 작가 전용 라우트가 하나의 공통 guard 아래 있고 일반 회원은 내용을 렌더링하거나
  작가 API를 호출하기 전에 차단된다.
- 비로그인 사용자는 로그인 후 원래 작가 URL로 돌아오며, 일반 회원은 무한 redirect나
  alert 반복 없이 일관된 권한 부족 안내를 받는다.
- 마이페이지에서 일반 메뉴와 작가 관리 메뉴가 구조·접근성 정보·시각 표현으로 구분되고
  현재 페이지가 `aria-current`와 포커스 표시로 식별된다.
- 390px과 200% 확대에서도 모든 허용 메뉴를 키보드로 사용할 수 있고 페이지 가로
  overflow가 발생하지 않는다.
- 백엔드 역할 권한, 기존 메뉴 목적지와 작가 기능 동작에 회귀가 없다.

### 커밋 경계

1. `fix(auth): 작가 전용 라우트 접근 계약 추가`
2. `fix(frontend): 작가 마이페이지 메뉴 구조 개선`
3. `chore: 작가 마이페이지 검증 결과 정리`

각 기능 코드와 직접 관련된 테스트는 첫 번째·두 번째 커밋에 각각 포함한다. `PLAN.md`의
계획·검증 상태와 `BACKLOG.md` 완료 표시는 전체 검증 후 세 번째 문서 커밋에서만 처리한다.
