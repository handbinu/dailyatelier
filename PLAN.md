# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 작품 수정·삭제 프론트 연동과 상태 표시

### 조사 결과

- 작업 시작 시 `git status --porcelain=v2`, unstaged diff, staged diff를 확인했으며 기존 미커밋 변경은 없다. 이번 계획 작성 전 저장소는 clean 상태였다.
- 작가 작품 관리 화면 `ManageArts`는 `GET /api/users/me/arts`의 상태 필터·페이지네이션과 상세 이동까지만 연결되어 있다. 수정·삭제 버튼, 편집 route·화면, mutation API 함수는 없다.
- 프론트 `artApi`에는 조회·등록·입찰 호출만 있고, 이미 구현된 `PATCH /api/arts/{artId}`와 `DELETE /api/arts/{artId}` 호출 함수가 없다.
- 백엔드는 소유 작가의 활성·마감 전 작품만 수정·삭제하도록 제한한다. 입찰 전에는 가격·최소 입찰 증분·기간·비가격 정보를 수정할 수 있고, 입찰 후에는 가격·기간을 수정할 수 없다. 최소 입찰 증분은 입찰 유무와 별개로 경매 시작 이후 수정할 수 없다.
- 수정 계약에는 작품명 필드가 없으므로 작품명은 현재 정책상 수정 대상이 아니다. 반면 수정 가능한 `format`·`category`는 상세 응답에 없어 편집 폼 초기화에 필요한 최소 조회 계약이 부족하다. 기존 수정 정책은 유지하고 상세 응답에 두 필드를 추가하는 호환 가능한 보완이 필요하다.
- 삭제 시 입찰이 없으면 작품을 물리 삭제하고 찜 참조를 분리하며 응답 `action`은 `DELETED`이다. 입찰이 있으면 이력과 찜을 보존한 채 상태 3(`CANCELED`)으로 전환하고 `action`은 `CANCELED`이다. 두 경우 모두 성공 응답은 200이다.
- 작가 관리 API는 취소 작품을 `ALL`·`ENDED`에 포함하고 `result: CANCELED`를 내려주지만, 현재 화면은 상태 3을 `상태 미정`으로, 결과를 `유찰`로 잘못 표시한다.
- 물리 삭제된 작품의 찜은 `artDeleted: true`, `availabilityMessage: "없어진 작품입니다."`인 tombstone으로 조회되지만, 현재 찜 화면은 null 작품 ID로 상세·입찰·찜 해제 링크를 만든다. 취소 작품도 일반 종료로만 표시하고 입찰 버튼 제한 외에 취소 사유를 보여주지 않는다.
- 입찰 현황 API는 취소 작품에 `auctionStatus: ENDED`, `bidResult: CANCELED`, `bidResultMessage`를 제공하지만, 현재 화면은 이를 패찰로 표시하고 안내 메시지를 사용하지 않는다.
- 공개 목록·검색·홈·작가 상세는 활성 또는 정상 종료 작품만 대상으로 하여 취소 작품을 노출하지 않는다. 물리 삭제 작품 상세는 404이며, 취소 작품은 ID 직접 접근 시 상세 응답이 남아 있고 프론트는 일반 종료로 표시하며 찜 조작도 노출한다.
- 상세 화면은 관리 목록에서 넘긴 복귀 경로를 허용하지 않는 정규식을 사용해 `내 작품 관리`가 아니라 전체 경매 목록으로 돌아간다. 수정·삭제 뒤 복귀·재조회 흐름은 아직 없고, 입찰 409에서만 최신 상세를 다시 조회하는 선례가 있다.
- 백엔드에는 수정·삭제 권한, 필드 제한, 입력 검증, 물리 삭제·취소 전환, 찜 분리, 예치 해제, 경합과 트랜잭션 테스트가 있다. 취소된 입찰 응답과 내 작품 조회 계약 테스트도 있다. 프론트에는 `ManageArts`, `Likes`, `BidStatus`의 직접 컴포넌트 테스트와 수정·삭제 API 테스트가 없다.

### 현재 작업 범위

1. 기존 수정 정책을 그대로 노출할 수 있도록 작품 상세 응답에 `format`·`category`를 추가하고 계약 테스트를 보완한다. 작품명 수정이나 기존 mutation 정책·오류 코드·HTTP 상태는 변경하지 않는다.
2. 프론트 API에 작품 수정·삭제 호출을 추가하고, 소유 작가가 관리 목록 또는 상세에서 편집 화면으로 진입하도록 연결한다.
3. 기존 등록 화면의 입력·이미지 업로드 규칙을 재사용하는 편집 화면을 제공한다. 작품명은 읽기 전용으로 설명하고, 서버 정책에 따라 입찰 후 가격·기간을 잠그며 경매 시작 후 최소 입찰 증분을 잠근다. 최종 허용 여부는 항상 서버 응답을 기준으로 한다.
4. 삭제 전 확인에서 입찰 유무에 따라 물리 삭제 또는 경매 취소 가능성을 명확히 알리고, 중복 제출을 막는다. 성공 시 `DELETED`·`CANCELED` 결과에 맞는 피드백과 함께 기존 필터·페이지를 보존해 관리 목록으로 복귀한다.
5. 수정 성공 후 관리 목록으로 복귀해 API를 다시 조회하도록 하고, 409 상태 충돌은 서버 메시지를 표시한 뒤 최신 상세를 재조회하여 편집 가능 필드와 상태를 갱신한다. 401·403·404 및 그 밖의 공통 오류 응답도 화면 안에서 처리한다.
6. 관리 목록의 취소 상태·결과, 찜의 삭제 tombstone·취소 상태, 입찰 현황의 취소 결과·안내 문구, 상세의 취소 상태와 입찰 불가 사유를 기존 응답 필드에 맞게 표시한다. 삭제 tombstone에는 잘못된 상세·입찰·찜 해제 동작을 노출하지 않는다.
7. 관리 목록에서 상세로 이동한 경우 기존 필터·페이지로 돌아오도록 허용 경로를 보완한다.

### 구현 단계

1. **조회 계약 최소 보완**
   - `ArtDetailResponseDto`와 매핑에 `format`·`category`를 추가한다.
   - 기존 상세·수정·삭제 정책은 건드리지 않고 API 계약 테스트로 필드와 회귀를 고정한다.
2. **작가 편집·삭제 흐름 연결**
   - `artApi` mutation 함수, 편집 route·화면, 관리 목록/상세 진입 동작을 추가한다.
   - 초기 조회, 정책별 입력 잠금, 이미지 교체, client validation, 저장·삭제 확인, 처리 중 상태, 성공 복귀, 오류와 409 최신 상태 복구를 구현한다.
   - 관리 목록 복귀 위치를 보존하고 mutation 성공 뒤 목록 API가 최신 데이터를 다시 읽게 한다.
3. **삭제·취소 상태 표시 정리**
   - 관리 목록, 찜, 입찰 현황, 상세에서 `CANCELED`와 삭제 tombstone을 명시적으로 구분한다.
   - 불가능한 상세·입찰 동작을 제거하고 서버가 제공하는 안내 문구를 표시한다.
4. **회귀 검증과 완료 문서 정리**
   - 아래 자동 테스트와 브라우저 QA를 통과한 뒤 승인된 범위의 결과를 `PLAN_DONE.md`에 기록하고 이 계획을 `PLAN.md`에서 제거한다.

### 자동 테스트 범위

- 백엔드: 작품 상세의 `format`·`category` 응답 계약, 기존 소유권·상태·입찰 전후 수정 제한과 삭제 결과 테스트 회귀.
- 프론트 API: `PATCH` payload와 `DELETE` 경로·응답 사용 검증.
- 편집 화면: 초기값, 작품명 읽기 전용, 입찰 전 전체 허용 필드, 입찰 후 가격·기간 잠금, 시작 후 최소 증분 잠금, 이미지 유지·교체, validation, 중복 제출 방지.
- mutation UX: 수정 성공 복귀, `DELETED`·`CANCELED` 성공 분기, 확인 취소, 401·403·404·409·일반 오류, 409 뒤 최신 상세 재조회.
- 상태 화면: 관리 목록의 취소 배지·결과, 삭제된 찜 tombstone과 action 제거, 취소된 찜의 입찰 action 제거, 취소 입찰의 결과·안내 문구, 취소 상세의 상태·입찰 불가 표시, 관리 목록 복귀 URL 보존.
- 전체 검증: 관련 백엔드 Gradle 테스트, 프론트 component test, utility test, lint, production build를 실행한다.

### 사용자 브라우저 QA 시나리오

1. 입찰 전 활성 작품을 관리 목록에서 열어 작품명이 읽기 전용인지, 가격·증분·기간·설명·분류·이미지를 수정할 수 있는지 확인하고 저장 후 원래 필터·페이지의 최신 카드 값으로 복귀하는지 확인한다.
2. 경매 시작 후 입찰 없는 작품과 입찰 있는 작품에서 잠긴 필드가 각각 서버 정책과 일치하고, 허용된 비가격 정보 수정은 성공하는지 확인한다.
3. 입찰 없는 작품 삭제 확인을 취소했을 때 변화가 없는지 확인한 뒤 삭제하여 `DELETED` 안내, 목록 제거, 기존 찜의 `없어진 작품입니다.` 표시와 잘못된 action 제거를 확인한다.
4. 입찰 있는 작품을 삭제하여 `CANCELED` 안내, 관리 목록의 취소 표시, 입찰자 입찰 현황의 `경매 취소`와 서버 안내, 찜·상세의 취소 상태와 입찰 불가를 확인한다.
5. 다른 탭에서 먼저 종료·취소·수정한 뒤 기존 편집 화면에서 저장·삭제하여 409 안내와 최신 상태 복구를 확인한다.
6. 비소유자 직접 편집 URL, 만료 로그인, 존재하지 않는 작품에서 403·401·404 피드백과 안전한 복귀 동작을 확인한다.
7. 데스크톱과 390×844 화면에서 편집 폼, 확인 dialog, 상태 배지, 피드백과 action 배치를 확인한다.

### 예상 커밋 경계

1. `chore: 작품 수정·삭제 연동 계획 추가`
   - 사용자 승인 후 이 계획 문서만 별도 커밋한다.
2. `feat(backend): 작품 편집 조회 계약 보완`
   - 상세 응답의 분류 필드와 백엔드 계약 테스트만 포함한다.
3. `feat(frontend): 작품 수정·삭제 흐름 연동`
   - API 호출, 편집 화면, 삭제 확인, 복귀·최신 상태 복구와 직접 관련 테스트를 포함한다.
4. `fix(frontend): 삭제·취소 작품 상태 표시`
   - 관리·찜·입찰·상세 상태 표시와 관련 회귀 테스트를 포함한다.
5. `chore: 작품 수정·삭제 연동 완료 기록 정리`
   - 구현·QA 완료 후 `PLAN_DONE.md` 기록과 `PLAN.md` 정리만 포함한다.

### 현재 범위에서 제외할 후속 BACKLOG 후보

- 취소 작품 상세를 소유 작가와 기존 입찰자에게만 허용할지, 공개 직접 접근도 유지할지에 대한 조회 권한 정책. 현재는 공개 목록에서만 제외되고 ID 직접 조회는 가능하므로 별도 도메인·접근 정책 결정이 필요하다.
- 삭제 tombstone 찜을 사용자가 직접 정리하는 기능. 현재 삭제 과정에서 작품 참조가 분리되어 기존 `DELETE /api/arts/{artId}/like`로는 해제할 수 없으므로 like ID 기반 삭제 등 별도 API 계약이 필요하다.
- 취소 작품의 찜 신규 등록·유지 허용 여부와 취소 시 기존 찜을 자동 제거할지에 대한 정책. 이번 작업에서는 저장된 이력을 보존하고 상태만 정확히 표시한다.
- 작품명 수정 허용. 현재 업데이트 DTO에 작품명이 없으므로 이번 프론트 연동에서 임의로 도메인 계약을 확대하지 않는다.
- `LikesService`의 기존 `ResponseStatusException`을 공통 `ApiErrorResponseDto` 도메인 예외 계약으로 이관하는 작업. 이번 화면 연동에 필요한 오류 표시는 현재 응답을 수용하고, 전면 오류 계약 정리는 별도 작업으로 둔다.
