# DailyAtelier 개발 계획

> [!IMPORTANT]
> 완료된 계획과 검증 결과는 `PLAN_DONE.md`에서 관리한다.
> 작업 완료 시 계획과 검증 결과를 `PLAN_DONE.md`에 먼저 보존한 뒤 `PLAN.md`에서 해당 계획을 제거한다.
> 완료 문서 커밋 전에는 두 파일을 대조해 완료 기록 누락과 중복이 없는지 확인한다.
> 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
> 새 작업 계획은 사용자 요청 범위가 확정된 뒤 이 문서에 추가한다.

---



### 목표와 범위

- `/mypage/inquiry`, `/mypage/inquiry/write`의 `MOCK_INQUIRIES`와 API 호출 없는 완료 화면을 실제 문의 등록·조회·답변 흐름으로 교체한다.
- 일반 사용자와 작가 사용자는 본인 문의만 조회하고, 관리자는 전체 문의 조회와 답변 등록을 수행한다.
- 이번 범위에서는 프로필 사진 변경, 문의/리뷰 이외의 신규 기능, 이메일 실제 발송, Cloudinary 원본 삭제, 대규모 공통 컴포넌트 리팩터링은 구현하지 않는다.

### 설계 원칙

- **문의 API 계약**: 문의 등록은 `POST /api/inquiries`의 `multipart/form-data`로 `request` JSON(문의 유형, 제목, 내용, 이메일 알림 수신 여부)과 선택 `attachment` 파일을 함께 전송한다. 서버는 허용 형식(JPG/PNG/PDF), 최대 10MB를 검증하고 Cloudinary에 업로드한 뒤 URL·원본 파일명·리소스 유형을 문의와 함께 저장한다.
- **문의 조회 계약**: `GET /api/inquiries/me`는 현재 로그인 사용자의 문의 목록을 페이징·최신순으로 반환하고, `GET /api/inquiries/{inquiryId}`는 작성자 또는 관리자만 조회하게 한다. 목록 응답에는 상태와 요약을, 상세 응답에는 본문·첨부·답변·답변 시각을 포함한다.
- **관리자 계약**: `GET /api/admin/inquiries`는 전체 문의를 상태 필터·페이징·최신순으로 반환하고, `POST /api/admin/inquiries/{inquiryId}/answer`는 답변 본문을 저장한다. 이미 답변된 문의의 재답변은 단일 답변 등록으로 제한하고 서버가 409 오류 코드로 거부한다.
- **권한 일관성**: 백엔드는 `userStatus=2`가 JWT의 `ROLE_ADMIN`이 되는 기존 인증 구조를 사용하고 `/api/admin/**`에 `hasRole("ADMIN")`를 적용한다. 프론트는 `ArtistRoute`와 같은 `useSyncExternalStore` 기반의 `AdminRoute`를 추가해 `userStatus===2`만 관리자 화면을 열 수 있게 하며, 화면 가드는 UX 보조이고 API 권한 검증을 최종 기준으로 둔다.
- **오류 응답**: 모든 신규 백엔드 오류는 `ApiErrorResponseDto`와 `DomainApiException`으로 반환한다. 인증·권한, 문의 없음, 타인 문의 접근, 첨부 형식/크기, 유효하지 않은 답변, 중복 답변에 각각 의미 있는 오류 코드를 둔다.

### 1단계 — 백엔드 데이터·API 구현

1. `Inquiry` 도메인을 문의 유형, 이메일 알림 동의, 첨부 URL·파일명·리소스 유형, 답변 상태를 표현할 수 있게 확장한다.
2. 기존 DB 운영 방식에 맞는 스키마 변경을 추가하고, 기존 문의 레코드는 첨부 없음·답변 여부를 `answer/answeredAt` 기준으로 안전하게 해석한다.
3. `InquiryRepository`, 요청·목록·상세·답변 DTO, `InquiryService`, `InquiryController`를 구현한다. 소유자 검증은 서비스 계층에서 수행하고, 목록 쿼리는 사용자별/전체 관리자별로 분리한다.
4. 첨부 파일 업로드를 Cloudinary 서비스에 추가한다. 문의 첨부는 이미지와 PDF를 지원하고, 파일 검증 후 저장 URL만 도메인에 기록한다.
5. `SecurityConfig`에 관리자 API 권한을 명시하고, 신규 컨트롤러의 검증 실패·잘못된 multipart 요청도 공통 400 오류 응답으로 변환되는지 확인한다.

**완료 기준**

- 로그인 사용자가 첨부 없이/첨부와 함께 문의를 등록하면 DB에 작성자·내용·첨부 메타데이터가 저장되고 201 응답을 받는다.
- 사용자는 자신의 목록과 상세만 조회하며, 다른 사용자의 문의 상세는 403 또는 404의 공통 오류 형식으로 차단된다.
- 관리자는 전체 문의를 조회하고 미답변 문의에 답변을 저장할 수 있으며, 상세·사용자 목록에서 답변과 답변 시각이 즉시 확인된다.
- 일반/작가 사용자가 관리자 API를 호출하면 403 `FORBIDDEN` 응답을 받고, 첨부 제한 위반은 구조화된 400 응답을 받는다.

### 2단계 — 프론트 API·사용자/관리자 문의 화면 연동

1. `inquiryApi`를 추가해 문의 등록 multipart 요청, 내 목록·상세 조회, 관리자 목록·답변 요청을 분리한다. API 오류 메시지는 공통 응답의 `message`를 우선 사용한다.
2. `InquiryWrite`에서 가짜 지연과 `done` 성공 화면을 제거하고, 선택 파일·폼 데이터를 실제 API로 전송한다. 등록 버튼은 요청 중 중복 제출을 막고, 성공 시 목록으로 이동해 서버에서 새 데이터를 다시 조회하며, 실패 시 작성 내용은 유지한 채 오류를 표시한다.
3. `InquiryList`에서 mock 경고·`MOCK_INQUIRIES` 의존을 제거하고 서버 목록의 대기/답변 완료 통계와 필터를 사용한다. 아코디언을 열 때 상세 API를 조회해 실제 문의 본문·첨부 링크·답변·답변 시각을 표시하고 로딩·오류·빈 상태를 구분한다.
4. `AdminRoute`와 관리자 문의 관리 페이지·라우트를 추가한다. 기존 `ArtistRoute`의 저장된 `userStatus` 구독 패턴과 동일하게 접근을 차단하고, 관리자는 전체 목록 필터·상세 확인·답변 작성만 수행한다.

**완료 기준**

- 사용자 문의 목록에 목업 안내나 가짜 문의 데이터가 남지 않고, 새 문의가 성공 응답 뒤 목록 첫 항목에 보인다.
- 문의 작성은 네트워크/API 실패 시 성공 안내를 표시하지 않으며, 버튼 상태·필드 오류·서버 오류를 사용자가 구분할 수 있다.
- 답변 대기와 답변 완료 필터·통계·상세 내용이 서버 데이터와 일치하고, 첨부 파일은 안전한 링크로 열 수 있다.
- 관리자 아닌 사용자는 관리자 라우트에 직접 접근해도 기존 작가 전용 화면과 같은 접근 거부 UI를 보고, 관리자만 답변 제출 UI를 사용한다.

### 3단계 — 자동·수동 검증 및 문서 정리

1. 백엔드 단위/웹 계층 테스트로 문의 등록, 본인 목록·상세, 타인 접근 거부, 관리자 목록·답변, 중복 답변, 첨부 검증을 검증한다.
2. 프론트 테스트로 실제 API 상태에 따른 문의 작성 성공/실패, 목록 필터·상세 답변 표시, `AdminRoute` 접근 제어를 검증한다.
3. 로컬 브라우저에서 일반 사용자와 관리자 계정으로 문의 등록→답변→사용자 확인의 전체 흐름과 모바일 폭 390px을 점검한다. 로그인 검증 전에는 `_local/test-accounts.md`의 로컬 계정만 사용한다.
4. 완료된 항목과 실행한 검증 결과를 `PLAN_DONE.md`로 보존한 뒤 이 계획을 `PLAN.md`에서 제거하고, `BACKLOG.md`의 해당 미완료 항목을 완료 상태로 갱신한다.

**완료 기준**

- 변경한 백엔드·프론트 테스트가 모두 통과하고, 관련 기존 테스트의 회귀가 없다.
- 수동 시나리오에서 일반 사용자·관리자 권한 경계, 첨부 제약, 답변 노출이 확인된다.
- 완료 문서와 백로그 상태가 실제 구현·검증 결과와 일치한다.

### 커밋 경계 제안

1. `feat(backend): 1:1 문의 API와 관리자 답변 구현` — 1단계의 엔티티/스키마/DTO/Repository/Service/Controller/Security/백엔드 테스트를 포함한다.
2. `feat(frontend): 1:1 문의 실제 연동` — 2단계의 API 모듈, 사용자·관리자 문의 화면, 라우트 가드와 프론트 테스트를 포함한다.
3. `chore: 문의 기능 검증 결과 문서화` — 3단계 통과 뒤 `PLAN.md`, `PLAN_DONE.md`, `BACKLOG.md`의 명시적 상태 갱신만 포함한다.

백엔드 계약과 프론트 화면을 분리하면 API 검증과 롤백 범위가 명확하며, 가짜 성공 화면 제거는 두 번째 커밋에서 실제 등록 요청과 원자적으로 적용한다.
