# DailyAtelier 개발 계획

> [!IMPORTANT]
> 완료된 계획과 검증 결과는 `PLAN_DONE.md`에서 관리한다.
> 작업 완료 시 계획과 검증 결과를 `PLAN_DONE.md`에 먼저 보존한 뒤 `PLAN.md`에서 해당 계획을 제거한다.
> 완료 문서 커밋 전에는 두 파일을 대조해 완료 기록 누락과 중복이 없는지 확인한다.
> 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
> 새 작업 계획은 사용자 요청 범위가 확정된 뒤 이 문서에 추가한다.

## 홈 종료 작품 mock 제거·실제 데이터 연결

### 목표와 완료 기준

- 홈의 종료 작품 6개를 실제 작품 검색 API 결과로 표시하고 `e1` 같은 문자열 mock ID 링크를 완전히 제거한다.
- 판매 작품은 `낙찰가`, 유찰 작품은 `최종가`와 `유찰` 상태로 구분해 실제 경매 결과를 오해 없이 표시한다.
- 전체·디지털·실물 탭, 로딩·오류·빈 상태와 재시도가 실제 조회 흐름에서 동작한다.
- 종료 시각이 최근인 작품부터 노출하고 카드와 “전체 보기” 링크는 존재하는 숫자 작품 ID 및 종료 작품 검색 화면으로 연결한다.

### 1단계. 종료 작품 검색 계약 보완

- [ ] `ArtSearchResponseDto`에 종료 결과(`SOLD`, `UNSOLD`, 진행 중에는 `null`)를 추가해 공개 검색에서도 판매와 유찰을 구분한다.
- [ ] 검색 정렬에 최근 종료순 조건을 추가하고 종료 시각 내림차순, 작품 ID 내림차순으로 결과를 안정적으로 정렬한다.
- [ ] 기존 `ENDED` 조건이 판매 상태, 유찰 상태, 마감 시각이 지난 활성 작품을 모두 포함하고 취소 작품은 제외하는 계약을 유지한다.
- [ ] 서비스·저장소·컨트롤러 테스트에서 결과 매핑, 최근 종료순, 잘못된 정렬 파라미터의 공통 400 오류 응답을 검증한다.

### 2단계. 홈 종료 작품 실제 연동

- [ ] `Home.jsx`의 `MOCK_ARTS`와 범용 mock `ArtGrid`를 제거하고 `searchArts`로 `status=ENDED`, 최근 종료순, 6개를 조회한다.
- [ ] 탭 값을 API의 `DIGITAL`·`PHYSICAL` 형식에 매핑하며 탭 전환 시 이전 요청을 취소하고 최신 응답만 반영한다.
- [ ] 실제 `artId`, 이미지 fallback, 작품명, 판매·유찰 배지, 낙찰가·최종가를 사용하는 종료 작품 전용 카드를 구성한다.
- [ ] 스켈레톤, 오류와 다시 시도, 전체 및 필터별 빈 상태를 제공하고 종료 작품 전체 보기 링크에 `status=ENDED`와 선택 형식을 보존한다.

### 3단계. 회귀 검증과 문서 정리

- [ ] 프론트 테스트에서 최초 조회 파라미터, 탭별 재조회, 문자열 mock ID 부재, 판매·유찰 표시, 로딩·오류·빈 상태와 재시도를 검증한다.
- [ ] 백엔드 검색 테스트와 프론트 홈 관련 테스트를 실행하고 UTF-8 한글 표시 및 기존 신규 작품 영역 회귀 여부를 확인한다.
- [ ] 브라우저 검증은 사용자가 직접 수행한다. 구현자는 브라우저를 열거나 클릭 테스트를 대신하지 않고, 판매·유찰·디지털·실물 상태를 확인할 수 있는 로컬 데이터와 실행 방법을 준비한다.
- [ ] 사용자용 브라우저 체크리스트에 홈 최초 진입, 전체·디지털·실물 탭, 카드 상세 이동, 종료 작품 전체 보기, 이미지 fallback과 빈 상태 확인 절차 및 기대 결과를 정리한다.
- [ ] 사용자 브라우저 검증에서 발견된 문제는 보고받은 재현 절차를 기준으로 수정하고 관련 자동화 테스트를 보강한다.
- [ ] 구현과 검증 결과를 `PLAN_DONE.md`에 보존한 뒤 이 계획을 `PLAN.md`에서 제거한다.
