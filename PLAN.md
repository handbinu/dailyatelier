# DailyAtelier 경매 마감·낙찰 MVP 구현 계획

## 목표

- 마감 시각이 지난 경매를 스케줄러가 자동 종료한다.
- 종료 시 최고 입찰자를 한 번만 확정하고 결과를 DB에 보존한다.
- 입찰자는 자신의 낙찰·패찰 여부와 낙찰 작품 목록을 확인한다.
- 작가는 자신의 진행 중 작품과 종료 작품을 구분해 확인한다.
- 결제·배송·리뷰 연계 없이 경매 결과 확인까지를 MVP 완료 범위로 한다.

## 권장 정책

- `Art.artStatus`의 기존 의미를 유지한다.
  - `0`: 진행 중
  - `1`: 종료(입찰 0건으로 유찰)
  - `2`: 낙찰
- `Art`에 낙찰 입찰을 가리키는 nullable `winningBid` 연관관계와 실제 처리 시각 `closedAt`을 추가한다.
  - 유찰이면 `winningBid = null`, `artStatus = 1`
  - 낙찰이면 `winningBid = 최고 입찰`, `artStatus = 2`
  - 낙찰가는 `winningBid.bidPrice`를 기준으로 하며 `currentPrice`와 일치해야 한다.
- 최고 입찰은 `bidPrice DESC, bidTime ASC, bidId ASC` 순으로 결정한다.
  - 현재 입찰 정책상 새 입찰가는 반드시 현재가보다 커야 하므로 최고 금액 동률은 발생하지 않는다.
  - 보정 데이터나 향후 정책 변경에 대비해 시각과 ID를 보조 정렬로 둔다.
- 스케줄러는 10초마다 `artStatus = 0 AND closingTime <= now`인 작품을 작은 배치로 조회한다.
- 마감 시각부터 저장 상태 반영까지 최대 10초의 지연을 허용한다.
- 각 작품은 별도 트랜잭션과 비관적 락으로 마감한다.
  - 이미 종료된 작품은 아무 변경 없이 건너뛰어 반복 실행에 안전하게 한다.
  - 락 획득 후 마감 시각과 상태를 다시 확인한다.
- 애플리케이션 시작 직후에도 같은 마감 작업을 한 번 실행해 서버 중단 중 누락된 경매를 보정한다.
- 조회 API는 경매 상태를 현재 시각으로 임시 계산하지 않고 저장된 `artStatus`와 낙찰 결과를 사용한다.
- 입찰 등록 API는 스케줄러 지연과 무관하게 `closingTime`을 직접 검사해 마감 후 입찰을 즉시 차단한다.

## API 계약

### 입찰자의 낙찰 작품 목록

- `GET /api/users/me/wins?page=0&size=12`
- 인증된 사용자가 낙찰자로 확정된 작품만 `closedAt DESC, artId DESC`로 조회한다.
- 응답에는 작품 ID·작품명·작가명·이미지·낙찰가·종료 시각을 포함한다.
- 페이지 크기는 최대 50으로 제한한다.

### 입찰자의 입찰 결과

- 기존 `GET /api/users/me/bids` 응답에 `bidResult`를 추가한다.
- 진행 중이면 `PENDING`, 종료 후 낙찰자면 `WON`, 그 외에는 `LOST`로 반환한다.

### 작가의 작품 결과

- `GET /api/users/me/arts?state=ALL|ACTIVE|ENDED&page=0&size=12`
- 진행 작품은 `PENDING`, 낙찰 작품은 `SOLD`, 유찰 작품은 `UNSOLD`로 반환한다.
- 종료 작품에는 낙찰가와 입찰 수를 제공한다.
- 낙찰자의 ID와 닉네임은 작가 응답에 노출하지 않는다.

## 기존 상태값 검토

- [x] 저장소 전체에서 `artStatus = 1`, `2` 사용처를 검색했다.
- [x] 백엔드에서 상태 1과 2를 종료·낙찰 이외의 업무 의미로 사용하는 코드는 없음을 확인했다.
- [x] 공개 작품 목록과 입찰 등록은 상태 0만 허용하므로 상태 1과 2는 진행 목록과 입찰 대상에서 제외됨을 확인했다.
- [x] `Likes.jsx`의 상태 2 라벨을 `승인 대기`에서 `낙찰`로 바꾸고 상태 1·2 모두 종료로 처리했다.
- [x] 조회 시점에 종료를 임시 계산하던 백엔드와 프론트 보정 로직을 저장 상태 기준으로 전환했다.

## 마감 락과 입찰 락의 상호작용

- 입찰 처리와 마감 처리는 같은 `Art` 행에 `PESSIMISTIC_WRITE` 락을 사용하므로 동시에 해당 작품을 변경할 수 없다.
- 마감 처리가 먼저 락을 잡으면 입찰 요청은 락 해제를 기다린다.
  - 3초 안에 마감 트랜잭션이 끝나면 입찰 요청은 종료 상태나 마감 시각을 재검사하고 `409 AUCTION_CLOSED`를 받는다.
  - 3초 안에 락을 얻지 못하면 `409 BID_CONFLICT`를 받는다.
- 입찰 처리가 먼저 락을 잡으면 마감 처리가 기다리며, 입찰 커밋 후 최신 입찰을 포함해 낙찰자를 확정한다.
- 현재 입찰 시각은 락 획득 후 서버 시각으로 정하므로 요청이 마감 전에 도착했더라도 락 획득 시점이 마감 이후면 거절한다.
- 마감 트랜잭션에는 외부 I/O를 넣지 않고 짧게 유지한다.
- [x] 실제 MySQL에서 JPA의 3초 락 타임아웃 힌트만으로는 3초 제한이 지켜지지 않음을 확인했다.
- 전역 `innodb_lock_wait_timeout` 변경은 경매 외 DB 작업에도 영향을 주므로 적용하지 않고, 실제 배포 전 검증 항목으로 `BACKLOG.md`에 남긴다.

## 실패 및 재처리 정책

- 락 경합이나 일시적인 DB 오류로 롤백된 작품은 `artStatus = 0`으로 남는다.
- 해당 작품은 다음 10초 스케줄러 사이클의 대상 쿼리에 다시 포함되므로 사용자 조작 없이 자동 재시도한다.
- 한 작품의 실패는 같은 배치의 다른 작품 처리를 중단시키지 않는다.
- `winningBid.bidPrice`와 `currentPrice` 불일치 같은 무결성 오류는 자동 확정하지 않는다.
- 무결성 오류는 반복 재시도로 해결되지 않으므로 작품 ID와 가격을 오류 로그에 남기고 운영자가 확인·보정한다.

## 확정 정책

- [x] 입찰 0건은 유찰(`artStatus = 1`)로 처리한다.
- [x] 작가 화면에 낙찰자 닉네임을 노출하지 않는다.
- [x] 스케줄러는 10초 주기로 실행하고 최대 10초의 상태 반영 지연을 허용한다.
- [x] 낙찰 작품의 리뷰 작성 버튼은 실제 리뷰 API가 준비될 때까지 숨긴다.
- [x] API 필드명과 페이지네이션 계약을 확정한다.

## 1. 마감 결과 모델과 단건 마감 서비스

- [x] `Art`에 `winningBid`, `closedAt`을 추가하고 상태 상수를 한곳에서 관리한다.
- [x] 마감 대상 작품을 비관적 락으로 다시 조회하는 Repository 메서드를 추가한다.
- [x] 작품의 최고 입찰 한 건을 결정하는 쿼리를 추가한다.
- [x] 단건 마감 트랜잭션에서 유찰 또는 낙찰 상태와 결과를 함께 저장한다.
- [x] 이미 처리된 작품을 재호출해도 결과가 바뀌지 않도록 멱등성을 보장한다.
- [x] 현재가와 최고 입찰가가 다르면 확정하지 않고 오류 로그와 운영 확인 대상으로 남긴다.

### 1단계 테스트 및 결과

- [x] 입찰 0건은 유찰로 종료되는지 검증한다.
- [x] 입찰 1건·여러 건에서 최고 입찰자가 정확히 확정되는지 검증한다.
- [x] 마감 시각 이전 작품은 처리하지 않는지 검증한다.
- [x] 같은 작품을 반복 또는 동시에 처리해도 결과가 하나인지 검증한다.
- [x] 마감 처리와 동시 입찰이 경합할 때 마감 전 입찰만 반영되는지 검증한다.
- [x] 가격 불일치 로그에 `artId`, `currentPrice`, `winningPrice`가 남는지 검증한다.

## 2. 스케줄러와 누락 보정

- [x] Spring Scheduling을 활성화하고 10초 주기 마감 작업을 추가한다.
- [x] 한 번에 처리할 작품 수를 제한하고 `closingTime`, `artId` 오름차순으로 가져온다.
- [x] 작품 하나의 실패가 나머지 작품 처리를 막지 않도록 트랜잭션 경계를 분리한다.
- [x] 서버 시작 시 동일한 catch-up 작업을 실행한다.
- [x] 처리 대상 수, 성공, 유찰, 낙찰, 실패를 구조화된 로그로 남긴다.
- [x] 조회 시점 종료 보정 로직을 저장 상태 기준으로 전환한다.

### 2단계 테스트 및 결과

- [x] 고정 `Clock`으로 스케줄러 대상 경계를 검증한다.
- [x] 서버 중단 중 마감된 작품이 시작 후 처리되는지 검증한다.
- [x] 일부 작품 처리 실패 후 다음 실행에서 자동 재처리되는지 검증한다.
- [x] 데이터 무결성 오류가 자동 확정되지 않고 식별 정보가 로그에 남는지 검증한다.
- [x] 상태 반영 지연 중에도 마감 후 입찰이 거절되는지 검증한다.
- [x] 실행 주기와 배치 크기를 설정으로 조정할 수 있게 했다.

## 3. 결과 조회 API

- [x] 인증 사용자의 낙찰 작품 목록 API를 구현한다.
- [x] 기존 내 입찰 조회에 `PENDING`, `WON`, `LOST` 결과를 추가한다.
- [x] 작가 작품 조회에 `ALL`, `ACTIVE`, `ENDED` 필터를 추가한다.
- [x] 작가 작품에 낙찰가·입찰 수·`SOLD`, `UNSOLD`, `PENDING` 결과를 제공한다.
- [x] 낙찰자의 ID와 닉네임을 외부 응답에 노출하지 않는다.

### 3단계 테스트 및 결과

- [x] 낙찰 작품 목록의 사용자 격리, 정렬, 빈 목록, 페이지 경계를 검증한다.
- [x] 내 입찰 결과가 진행·낙찰·패찰로 정확히 구분되는지 검증한다.
- [x] 작가의 진행·종료 필터와 유찰·낙찰 결과를 검증한다.
- [x] 일반 회원의 작가 작품 조회와 비인증 요청이 차단되는지 검증한다.
- [x] 실제 H2 프로젝션·집계 쿼리와 API 보안을 검증한다.

## 4. 마이페이지 연동

- [x] `SuccessfulBid.jsx`의 목업과 준비 중 안내를 제거하고 낙찰 목록 API를 연결한다.
- [x] 낙찰 작품 화면에 작품, 작가, 낙찰가, 종료 시각, 상세 보기 동작을 표시한다.
- [x] 결제·리뷰 기능은 MVP에서 노출하지 않는다.
- [x] `BidStatus.jsx`의 종료 카드에 `낙찰` 또는 `패찰` 결과를 표시한다.
- [x] `ManageArts.jsx`에 전체·진행·종료 필터와 낙찰·유찰 결과를 표시한다.
- [x] `Likes.jsx`의 상태 2 라벨을 `낙찰`로 수정하고 상태 1·2에서 입찰 버튼을 숨긴다.
- [x] 마이페이지 요약의 낙찰 건수·목록·작품 수를 실제 API 데이터로 전환한다.
- [x] 로딩, 오류, 재시도, 빈 목록, 인증 만료 상태를 구현한다.

### 4단계 테스트 및 결과

- [x] 프론트 전체 ESLint와 프로덕션 빌드를 통과한다.
- [x] 실제 작가·입찰자 데이터로 데스크톱·모바일 카드, 필터, 상태 배지와 빈 화면을 검증한다.
- [x] 모바일 390px 화면에서 가로 넘침과 브라우저 콘솔 오류가 없음을 확인한다.
- [x] `artStatus = 2`이지만 낙찰 정보가 없던 `artId=1`의 원인을 과거 작품 등록 기본값 오류로 확인했다.
- [x] 사용자 승인에 따라 `artId=1`을 입찰 0건 유찰 상태로 보정했다.

## 5. 통합 검증 및 MVP 마감

- [x] 작품 등록 → 복수 사용자 입찰 → 자동 마감 → 작가 종료 확인 → 낙찰자·패찰자 결과 확인 흐름을 검증한다.
- [x] 마감 경계 시각과 스케줄러 지연 구간의 입찰 차단을 검증한다.
- [x] 백엔드 전체 테스트와 프론트 전체 빌드·ESLint를 실행한다.
- [x] 운영 DB에서 `winning_bid_id`, `closed_at` 스키마를 확인하고 마감 대상 조회 인덱스를 추가한다.
- [x] API 계약과 남은 제한 사항을 문서에 반영한다.
- [x] 사용자 확인 전에는 커밋하거나 push하지 않는다.

### 5단계 완료 결과

- [x] 실제 MySQL에서 작품 등록, 두 사용자 입찰, 자동 마감, 작가 `SOLD`, 낙찰자 목록, 패찰자 `LOST` 흐름을 확인했다.
- [x] 마감 대상 복합 인덱스 `art_status, closing_time, art_id`를 추가하고 실제 DB에 생성됐는지 확인했다.
- [x] 백엔드 전체 68개 테스트를 통과했다.
- [x] 프론트 전체 ESLint와 프로덕션 빌드를 통과했다.
- [x] 검증용 작품·입찰·회원 데이터와 임시 파일을 제거했다.

## MVP 제외 및 백로그

- 결제, 배송, 환불, 낙찰 포기, 차순위자 승계
- 리뷰 작성 API와 구매 확정 연계
- 마감 임박 자동 연장
- 사용자 알림과 실시간 상태 갱신
- 관리자의 수동 마감·재처리 화면
- 다중 애플리케이션 인스턴스의 스케줄러 리더 선출 또는 분산 락
- 대량 마감 성능 측정, 재처리 큐, 운영 모니터링·알림

## 권장 인덱스

- [x] `art(art_status, closing_time, art_id)`: 마감 대상 배치 조회
- [ ] `bid(art_id, bid_price, bid_time, bid_id)`: 최고 입찰 조회
- 실제 MySQL 실행 계획을 확인한 뒤 중복 인덱스가 있으면 추가하지 않는다.
