# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

# 프로필 기본정보 저장 피드백 개선 계획

## 목표

- 프로필 기본정보 저장 성공 후 `/mypage`로 이동한 사용자가 저장 완료를 명확히 인지할 수 있게 한다.
- 이동 직후 프로필 재조회가 실패하더라도 프로필을 기본값으로 조용히 대체하지 않고 오류와 재시도 방법을 제공한다.
- 기본정보 저장 중 중복 제출과 화면 이탈·프로필 사진 동시 저장처럼 실제로 충돌하는 작업을 막는다.
- 위 경계를 컴포넌트 테스트로 고정해 같은 피드백 누락과 중복 요청이 다시 발생하지 않게 한다.

## 현재 구조와 최소 변경 방향

- `ProfileEdit`은 기본정보 저장 성공 직후 `navigate('/mypage')`만 호출하고, `MyPage`는 이미
  `useLocation()`과 성공 문구 `T.profileChanged`를 보유하지만 실제 피드백으로 표시하지 않는다.
- 성공 여부는 새 전역 저장소나 알림 시스템 없이 React Router의 location state로 전달한다.
  `MyPage`가 성공 상태를 읽어 기존 페이지 안에 `role="status"` 피드백을 표시하고, 뒤로 가기나
  재방문에서 반복 노출되지 않도록 해당 상태를 일회성으로 소비한다.
- `MyPage`의 프로필 조회는 입찰·낙찰 등 다른 요약 조회와 분리된 상태를 유지한다. 조회 실패 시
  `user === null`을 정상 기본값처럼 렌더링하지 않고, 프로필 영역에 오류 메시지와 `다시 시도`
  동작을 제공한다. 기존 `EmptyState`의 오류·재시도 표현을 우선 재사용하거나 같은 페이지 안의
  최소 피드백 UI로 한정한다.
- 저장 중 제어는 `ProfileEdit` 내부 상태와 ref 기반 요청 가드로 처리한다. 공통 submit 시스템이나
  전역 로딩 시스템은 도입하지 않는다.
- 프로필 기본정보 저장 계약의 `name` 누락을 함께 보완한다. `ProfileEdit` payload,
  `ProfileUpdateDto`, `UserService.updateUserProfile()`을 연결해 이름 변경이 DB에 저장되고
  `/api/users/me` 재조회 응답에 반영되도록 한다. `name` 외 다른 프로필 필드의 계약은 변경하지 않는다.

## 구현 단계

### 1. 기본정보 저장 요청과 이동 상태 보강

- 대상: `frontend/src/pages/MyPage/ProfileEdit.jsx`
- 렌더 반영보다 빠른 연속 submit도 한 번만 API를 호출하도록 ref 기반 요청 가드를 추가하고,
  모든 성공·실패 종료 경로에서 가드를 정리한다.
- 저장 성공 시 `/mypage` 이동 state에 프로필 저장 완료 신호를 전달한다.
- `name`을 기본정보 저장 payload에 포함한다.
- 백엔드 `ProfileUpdateDto`에 `name`을 추가하고, `UserService.updateUserProfile()`에서 이름을 저장한다.
- `saving` 중에는 취소를 비활성화해 요청이 계속 처리되는 동안 화면만 먼저 이탈하지 않게 한다.
- 기본정보 저장과 프로필 사진 저장은 같은 사용자 행을 별도 요청으로 갱신하므로 서로 동시에
  시작할 수 없게 한다. `saving` 중에는 프로필 사진 저장을, `imageSaving` 중에는 기본정보 저장을
  비활성화하고 두 핸들러에서도 같은 조건을 확인한다.
- 닉네임 중복확인은 조회 요청이고 우편번호 찾기와 프로필 사진 선택은 로컬 상태만 변경하므로,
  기본정보 저장 중이라는 이유만으로 비활성화하지 않는다. 프로필 사진 선택은 기존처럼 실제
  사진 저장 요청이 진행 중인 `imageSaving` 동안에만 비활성화한다.
- 이미 열린 주소 검색 dialog는 기본정보 저장을 시작할 수 없는 현재 화면 흐름을 유지하며,
  프로필 사진 저장 중 기본정보 저장도 시작되지 않도록 양방향 충돌을 막는다.
- 기존 필드 값, 유효성 검사, 프로필 사진의 독립적인 성공·실패 메시지와 기본정보 저장 실패 시
  현재 화면을 유지하는 동작은 변경하지 않는다.

### 2. 마이페이지 성공 및 프로필 조회 실패 피드백 추가

- 대상: `frontend/src/pages/MyPage/MyPage.jsx`, `frontend/src/pages/MyPage/MyPage.module.css`
- location state로 전달된 기본정보 저장 성공을 마이페이지 진입 직후 확인 가능한 위치에
  `role="status"`로 표시한다. 프로필 조회가 지연되거나 실패해도 저장 API 성공 사실은 유지한다.
- 성공 state는 표시 후 현재 `/mypage` history entry에서 제거해 새로고침·뒤로 가기·일반 재방문 시
  오래된 완료 메시지가 다시 나타나지 않게 한다.
- 프로필 조회 전용 오류 상태와 재시도 함수를 추가한다. 실패 시 프로필 카드와 포인트 카드를
  가짜 기본값으로 렌더링하지 않고 오류 메시지와 `다시 시도` 버튼을 표시한다.
- 프로필만 재시도하며 입찰·낙찰·작품·문의 요약 요청은 다시 실행하지 않는다. 프로필 재조회가
  성공하면 오류를 지우고 실제 프로필·포인트 카드를 복원한다.
- 로그인 만료 처리나 다른 마이페이지 요약의 로딩·오류 정책은 이번 범위에서 변경하지 않는다.

### 3. 컴포넌트 회귀 테스트 추가

- 대상: `frontend/src/test/ProfileEdit.test.jsx`, `frontend/src/test/MyPage.test.jsx`
- 지연 중인 기본정보 저장 요청에서 연속 submit을 발생시켜 `updateUserProfile`이 한 번만 호출되는지
  검증한다.
- 기본정보 저장 중 취소와 프로필 사진 저장이 비활성화되고, 프로필 사진 저장 중 기본정보 저장이
  비활성화되며 요청 종료 후 다시 활성화되는지 검증한다.
- 기본정보 저장 중에도 닉네임 중복확인, 우편번호 찾기와 프로필 사진 선택은 불필요하게
  비활성화되지 않는지 검증한다.
- 이름 변경값이 프론트 payload에 포함되고, 백엔드 저장 후 `/api/users/me` 재조회 응답에
  반영되는지 프론트·백엔드 테스트로 검증한다.
- 저장 성공 후 `/mypage` 경로와 location state가 함께 전달되는지 검증한다.
- 성공 state로 진입한 `MyPage`가 완료 메시지를 표시하고 state를 일회성으로 소비하는지 검증한다.
- 프로필 조회 실패 시 `사용자`, `이메일 정보 없음`, `0원` 같은 정상 데이터 대체 표시가 나오지
  않고 오류와 재시도가 제공되는지, 재시도 성공 후 실제 프로필 카드가 복구되는지 검증한다.
- 프로필 조회 실패와 저장 성공이 동시에 존재할 때 두 사실이 구분되어 표시되는지 검증한다.

## 검증 계획

- 변경 대상 컴포넌트 테스트를 우선 실행한다.
  - `ProfileEdit.test.jsx`
  - `MyPage.test.jsx`
- 프런트 전체 컴포넌트 테스트, ESLint, production build를 실행해 기존 프로필 사진과 마이페이지
  요약 흐름의 회귀 여부를 확인한다.
- `git diff --check`로 공백과 파일 형식 문제를 확인한다.
- 브라우저 QA는 자동 수행하거나 완료 조건으로 대신 판단하지 않고 사용자 검증 항목으로 남긴다.

## 완료 기준

- 기본정보 저장 요청은 사용자 연속 조작에도 한 번만 전송된다.
- 기본정보 저장 중 화면 이탈과 프로필 사진 동시 저장은 차단되고, 조회·로컬 상태 동작인
  닉네임 중복확인, 우편번호 찾기와 프로필 사진 선택은 불필요하게 차단되지 않는다.
- 저장 성공 후 `/mypage`에서 완료 메시지가 한 번 표시되며 일반 재방문에는 남지 않는다.
- 프로필 조회 실패 시 가짜 기본 프로필·포인트가 표시되지 않고, 사용자가 프로필 조회 실패
  사실과 재시도 방법을 확인할 수 있다.
- 프로필 이름 변경값이 DB에 실제 저장되고, 이후 `/api/users/me` 재조회에서 변경된 이름이 반환된다.
- 프로필 재시도 성공 시 다른 마이페이지 요약을 불필요하게 다시 조회하지 않고 실제 프로필이
  복원된다.
- 관련 자동 테스트, 전체 프런트 회귀 검사와 정적 검증이 통과한다.

## 제외 범위

- 작품 등록·수정, 구매·판매 주문 및 리뷰의 로딩·저장 UX
- 전역 로딩, toast, 알림 또는 공통 submit 시스템 신설
- `name` 저장 계약 외 다른 백엔드 API·DTO·오류 계약 변경
- `name` 외 다른 프로필 필드 변경
- 다른 사용자 API/DTO 리팩토링
- 프로필 사진 업로드 방식과 마이페이지의 다른 요약 조회 정책 변경
- Codex가 수행하는 브라우저 QA

---
