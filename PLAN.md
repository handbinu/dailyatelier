# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 낙찰 후 주문·배송지·결제 연결 개선

### 조사 결론

- 네 후보는 모두 낙찰자가 경매 종료 후 생성된 하나의 구매 주문을 찾아 배송지를 확정하고,
  포인트 결제와 주문 상세 확인으로 이어지는 같은 사용자 흐름이다. 하나의 PLAN으로 묶는 것이 적절하다.
- 경매 마감 트랜잭션은 낙찰 작품을 `SOLD`로 바꾸면서 작품당 하나의 주문을 즉시 생성한다.
  주문은 `orderId`가 상세·배송지·결제 API의 기준 식별자이고, `artId`는 주문과 작품을 연결하는
  도메인 식별자다.
- 구매자 주문 목록·상세 응답에는 이미 `orderId`, `artId`, 주문 상태, 배송지 확정 여부와
  가능한 작업이 포함된다. 반면 낙찰 작품 응답과 입찰 현황 응답에는 `artId`만 있고 직접 연결에
  필요한 `orderId`가 없다. 작품 상세 응답도 소유 작가 여부만 제공하며 현재 로그인 사용자의
  구매 주문 관계를 알려 주지 않는다.
- 현재 낙찰 작품의 "주문 확인"은 `/mypage/order-status?artId=...`로 이동한 뒤 구매자 주문 목록의
  **현재 페이지**에서 같은 `artId`를 찾는다. 해당 주문이 첫 페이지에 없으면 아무 주문도 열리지
  않으므로 정확한 상세 연결 계약이 아니다.
- 입찰 현황은 작품, 내 입찰가, 현재가, 선두 여부, 경매 상태, 낙찰·패찰·취소 결과까지만 받는다.
  낙찰 후 생성된 주문의 식별자는 받지 않는다.
- 공개 작품 상세 API는 선택적 인증으로 `isOwner`만 계산한다. 현재 사용자가 낙찰 구매자인지,
  관련 주문이 있는지 또는 그 주문을 열 권한이 있는지는 확인할 수 없다.
- `GET /api/users/me/points`는 `availablePoint`와 전체 `heldPoint`를 제공하며 적립금 충전 화면과
  마이페이지에서 이미 사용한다. 충전 화면에는 조회 중 표시, 실패 안내·재조회, 미조회 중 충전 차단이
  구현돼 있지만 주문 화면은 잔액을 조회하지 않고 결제 버튼을 바로 실행한다.
- 낙찰가는 입찰 시 이미 사용 가능 포인트에서 차감되어 예치 포인트가 된다. 주문 결제는 새로
  `availablePoint`를 차감하지 않고 해당 예치를 확정하므로, `availablePoint < winningPrice`를
  잔액 부족으로 해석해 결제를 막아서는 안 된다. 정상 주문의 결제 후 예상 사용 가능 포인트는
  조회 시점의 `availablePoint`와 같다. 서버 결제 실패는 잔액 부족보다 예치 누락·불일치 계약으로
  처리되고 있다.
- 조사 시작 시 `git status --short`와 `git status --porcelain=v2`는 모두 비어 있어 기존 미커밋
  변경은 없었다. 관련 백엔드 테스트는 경매 마감 주문 생성, 낙찰·입찰 조회 계약, 주문 조회·배송지·
  결제·포인트 원장을 다루고, 프론트 테스트는 주문 목록·상세·배송지, 충전 잔액 상태를 다루지만
  낙찰 작품·입찰 현황에서 특정 주문을 직접 여는 회귀 테스트와 주문 결제 잔액 UI 테스트는 없다.

### 먼저 보완할 계약

- 주문 목록 첫 페이지 탐색을 없애기 위해 낙찰 구매자용 응답이 정확한 `orderId`를 직접 제공해야 한다.
  작품당 주문 하나라는 현재 유니크 정책과 `OrderRepository.findByArtArtId`를 유지하고, 구매자 권한을
  확인한 뒤에만 주문 연결 정보를 노출한다.
- 낙찰 작품, 종료된 낙찰 입찰, 작품 상세에는 직접 연결에 필요한 nullable `orderId`만 추가하는 것을
  기본 계약으로 삼는다. 주문 상태, 배송지 확정 여부와 가능한 작업은 대상 주문 상세 API가 이미
  제공하므로 세 응답 DTO에 복제하지 않는다. 화면의 다음 행동은 `orderId`로 주문 상세를 조회한 뒤
  기존 `availableActions`와 배송지 확정 정보를 기준으로 주문 화면에서 결정한다.
- 경매 종료와 주문 생성이 같은 트랜잭션이므로 정상 데이터에서는 낙찰과 주문이 함께 존재해야 한다.
  과거·비정상 데이터의 주문 없음은 nullable `orderId`와 명확한 대체 안내로 방어한다.
- 작품 상세의 `orderId`는 비로그인, 판매 작가, 패찰자와 무관한 사용자에게 노출하지 않고 낙찰
  구매자 본인에게만 제공한다. 구현 조사 중 `orderId` 외 필드가 없으면 요청 수가 비정상적으로
  증가하는 등 구체적인 필요가 확인될 경우에는 범위를 임의 확대하지 않고 사용자에게 보고한다.
- 포인트 API 자체는 이번 작업에 필요한 조회 계약을 이미 제공한다. 주문별 낙찰 예치액은
  `winningPrice`와 서버의 주문-예치 무결성 계약으로 확정되어 있으므로 별도 잔액/원장 API나
  결제 구조 변경은 추가하지 않는다. 사용 가능 잔액과 낙찰가를 비교한 부족 상태는 만들지 않으며,
  주문 예치 무결성 실패와 결제 충돌은 최신 주문·포인트 재조회와 서버 오류 안내로 처리한다.

### 구현 범위

1. **구매자 주문 연결 응답 보완**
   - 낙찰 작품 조회와 종료된 낙찰 입찰 조회가 구매자 본인의 nullable `orderId`만 함께 반환하도록
     조회·DTO를 보완하고, 주문 없음·타 사용자 비노출·기존 페이지네이션을 계약 테스트로 고정한다.
   - 작품 상세의 선택적 인증 흐름에서 현재 사용자가 낙찰 구매자인 경우에만 nullable `orderId`를
     반환하고 공개 응답의 개인정보 범위를 유지한다.
   - 주문 상태, 배송지 확정 여부, 가능한 작업 등 기존 주문 상세·목록 API에서 조회할 수 있는 정보는
     낙찰 작품·입찰 현황·작품 상세 DTO에 추가하지 않는다.

2. **정확한 주문 상세 진입과 다음 행동 연결**
   - 주문 화면이 `orderId` URL 파라미터로 특정 구매 주문을 직접 조회·펼치도록 바꾸고, 목록 현재
     페이지나 필터와 무관하게 접근하게 한다. `artId` 기반 첫 페이지 탐색은 제거하되 기존 북마크
     호환이 필요하면 명시적인 주문 연결 조회를 거쳐 한시적으로 수용한다.
   - 낙찰 작품 카드는 응답의 `orderId`로 해당 상세를 열고, 주문 없음·접근 불가에는 주문 목록으로
     잘못 이동시키지 않고 대체 안내를 표시한다.
   - 입찰 현황의 낙찰 건은 `orderId`로 주문 화면에 진입한 뒤 주문 상세 응답의 기존 정보를 이용해
     배송지 미확정이면 입력 상태를 열고, 확정 후에는 주문 상세를 연다. 진행 중·패찰·취소 건의 기존
     작품 상세 동선은 유지한다.
   - 작품 상세는 낙찰 구매자에게만 관련 주문 버튼을 표시한다. 배송지 입력 등 구체적인 다음 행동은
     중복 상태를 전달받지 않고 주문 화면이 상세 조회 후 결정하며, 주문 접근 불가 상태를 안내한다.

3. **주문 결제 포인트 상태 표시**
   - 결제 가능한 주문 상세가 열릴 때 기존 포인트 요약 API로 `availablePoint`와 `heldPoint`를 조회해
     현재 사용 가능 포인트와 전체 예치 포인트, 낙찰 결제 금액을 안내한다.
   - 조회 중에는 결제를 차단하고, 실패 시 오류·재조회 버튼을 제공한다.
   - 정상 낙찰 예치가 결제 금액을 이미 담보한다는 안내를 함께 표시한다. `availablePoint < winningPrice`
     비교로 부족 상태를 만들거나 포인트 충전을 결제 해결책처럼 노출하지 않는다.
   - 충전 화면 링크를 유지할 필요가 있다면 일반적인 포인트 관리 동선으로만 제공하고, 부족 경고나
     결제 차단의 해제 수단으로 표현하지 않는다.
   - 결제 충돌·예치 무결성 오류 시 주문 상세와 포인트를 재조회한 뒤 서버 오류 메시지와 가능한
     다음 행동을 안내한다.

4. **회귀 검증**
   - 백엔드: 낙찰 작품·입찰 현황·작품 상세의 최소 `orderId` 연결, 구매자 권한 비노출, 주문 없음
     방어, 기존 주문 상세 권한과 공통 오류 응답을 테스트한다.
   - 프론트: 여러 페이지 주문 중 정확한 `orderId` 상세 열기, 작품 상세·낙찰 카드·입찰 현황의
     주문 연결, 주문 화면의 배송지 분기, 포인트 조회 중·실패·재조회·결제 성공/충돌을 테스트한다.
   - 기존 주문·경매 마감·포인트 원장 테스트와 프론트 전체 테스트·빌드를 실행한다.

### 이번 범위에서 제외하고 BACKLOG에 유지

- 낙찰 작품 카드 전체에 배송·환불·구매 확정까지 시각화하는 **낙찰 작품 카드의 주문 상태 요약**은
  이번에는 `orderId` 연결과 주문 화면 진입 후 다음 행동 처리까지만 구현하고 별도 항목으로 남긴다.
- 마이페이지 홈 요약, 메인 페이지 행동 유도, 주문·낙찰 목록 URL 페이지·필터 보존은 별도 동선
  고도화로 남긴다. 단, 이번 작업의 특정 `orderId` 직접 진입 URL만 지원한다.
- 복합 결제·외부 PG·포인트 원장 전체 이력, 배송 정책·실시간 배송 추적, 주문 상태 모델·결제 서비스·
  원장 구조 리팩토링은 포함하지 않는다.
- 판매자 주문 바로가기는 이번 후보의 낙찰 구매자 흐름과 권한 계약이 다르므로 작품 상세에서 새로
  만들지 않는다. 판매 작가의 기존 판매 주문 화면은 변경하지 않는다.

### 완료 기준

- 낙찰 작품, 낙찰된 입찰 현황, 낙찰 구매자가 보는 작품 상세에서 같은 `orderId`의 구매 주문으로
  바로 이동하며 주문 목록 페이지 위치에 의존하지 않는다.
- 배송지 미확정 주문은 배송지 입력으로, 확정 주문은 주문 상세로 연결되고 주문 없음·권한 없음은
  잘못된 상세를 열지 않은 채 명확히 안내한다.
- 결제 버튼은 포인트 조회 완료 전 비활성화되고 조회 실패 시 재시도할 수 있다. 현재 사용 가능·예치
  포인트를 안내하되 정상 낙찰 예치를 잔액 부족으로 오판하거나 충전을 해결책으로 노출하지 않는다.
- 기존 주문 생성·배송지·결제·포인트 원장 계약을 변경하지 않고 관련 자동화 테스트와 빌드가 통과한다.

### 승인 후 커밋 경계

- `chore: 낙찰 후 주문 동선 개선 계획 추가`
- `feat(backend): 낙찰 후 구매 주문 연결 계약 보완`
- `feat(frontend): 낙찰 후 주문·배송지·결제 동선 개선`
- `chore: 낙찰 후 주문 동선 완료 문서 정리`
