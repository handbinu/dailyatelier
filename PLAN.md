# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 동시 입찰 부하 테스트 및 병목 개선

### 목표와 판단 원칙

- 실제 MySQL과 HTTP 입찰 API를 대상으로 동시 요청의 처리량, 평균·p95·p99 응답 시간과 응답 결과 분포를 측정한다.
- 단일 작품의 `art` 행 잠금, 여러 작품에서 공유되는 `point_account` 행 잠금, Hikari 커넥션 풀 대기를 서로 구분할 수 있는 시나리오와 관찰 자료를 남긴다.
- `409 BID_TOO_LOW` 같은 정상적인 경쟁 결과와 `409 BID_CONFLICT`, 예상하지 못한 500, 클라이언트 timeout을 HTTP 상태와 도메인 오류 코드로 분리한다.
- 부하 종료 후 작품 현재가, 최고 입찰, 활성 예치, 계정 잔액과 포인트 원장의 정합성을 검증한다.
- 인덱스, 풀 크기, 락 전략, timeout 값은 실측 결과만으로 판단한다. 계약 문제나 병목이 발견되어도 이번 계획 안에서 production 코드를 즉시 수정하지 않고 원인, 영향, 변경 후보와 재현 근거를 먼저 보고한다.
- 입찰 외 도메인의 성능 개선, 운영·공유 DB 데이터 사용, Cloudinary 등 외부 이미지 업로드는 범위에서 제외한다.

### 1단계 — 격리된 MySQL 부하 환경과 fixture 구성

#### 구현

- 기존 개발·운영·공유 스키마와 다른 부하 테스트 전용 MySQL 스키마를 사용한다. 실행 전 DB 이름과 전용 표식을 검사하여 허용된 테스트 스키마가 아니면 초기화와 fixture 적재를 중단한다.
- 전용 스키마에는 현재 Flyway migration을 순서대로 적용하고, 반복 실행마다 업무 fixture만 결정적으로 초기화하거나 실행별 고유 fixture 묶음을 생성한다. 다른 스키마와 기존 사용자 데이터는 삭제하거나 변경하지 않는다.
- 다음 fixture를 자동으로 준비한다.
  - 로그인 가능한 판매자와 충분한 수의 입찰자
  - 입찰자별 `point_account`와 시나리오 수행에 충분한 가용 잔액
  - 현재 시각에 시작되어 있고 테스트 동안 마감되지 않는 ACTIVE 작품
  - 단일 작품 집중용 작품 1개, 작품·계정 분산용 작품 묶음, 동일 계정 경합용 작품 묶음
- 작품 이미지는 외부 업로드 없이 고정된 로컬 테스트 경로나 외부 호출이 발생하지 않는 문자열 값만 사용한다.
- 로그인은 부하 구간 밖의 setup 단계에서 수행하고 사용자별 JWT를 준비한다. fixture 생성과 로그인 실패는 부하 결과에 섞지 않고 즉시 실행 실패로 처리한다.
- 자동 마감 스케줄러가 시험 중 fixture를 마감하지 않도록 종료 시각을 충분히 뒤로 설정한다. production 기본 설정을 바꾸지 않고 전용 실행 환경에서만 필요한 조건을 적용한다.

#### 완료 기준

- 허용되지 않은 DB를 지정했을 때 fixture 초기화가 실행되지 않는다.
- 깨끗한 전용 스키마에서 Flyway 적용, fixture 생성, 사용자별 로그인, 단일 정상 입찰까지 재현 가능하다.
- fixture 수, 작품 상태·기간, 계정 잔액이 시나리오 요구량과 일치하며 같은 절차를 반복 실행할 수 있다.
- fixture 준비 전후로 테스트 대상 이외 스키마와 기존 데이터가 변경되지 않았음을 확인한다.

#### 자동 검증·실측

- 준비 스크립트의 사전 조건 실패 테스트와 종료 코드 확인
- Flyway 적용 상태 및 대상 DB 이름 조회
- 사용자·포인트 계정·ACTIVE 작품 수와 참조 관계 검증 SQL
- JWT를 사용한 HTTP 정상 입찰 smoke test와 응답·DB 반영 확인

### 2단계 — k6 부하 시나리오와 잠금 진단 구성

#### 구현

- 요청별 응답 시간, HTTP 상태, 도메인 오류 코드와 timeout을 분리 집계하는 공통 k6 실행 구조를 만든다. 실행 ID, 시나리오, VU/요청률, 지속 시간과 fixture ID를 결과에 함께 기록한다.
- 먼저 낮은 부하로 smoke 실행한 뒤 동일한 단계 증가 조건으로 다음 핵심 시나리오를 수행한다.
  1. **단일 작품 집중 경쟁:** 서로 다른 계정이 같은 작품에 동시에 입찰하여 작품 행 직렬화, 잠금 대기, stale 가격에 의한 `BID_TOO_LOW`와 실제 `BID_CONFLICT`를 측정한다.
  2. **작품·계정 분산 기준선:** 각 VU가 분리된 작품과 계정을 사용하여 HTTP/JWT/JPA/DB 쓰기의 비경합 기준 처리량과 지연을 측정한다.
  3. **여러 작품·동일 계정 경합:** 한 계정 또는 제한된 계정 집합이 여러 작품에 입찰하여 작품 잠금과 별개인 `point_account` 잠금 대기와 오류 계약을 확인한다.
  4. **강제 장기 잠금 진단:** 별도 MySQL 세션이 대상 `art` 또는 `point_account` 행을 의도적으로 잠근 동안 HTTP 요청을 보내 실제 대기 시간, 409 코드, 500과 클라이언트 timeout을 확인한다. 잠금 세션은 전용 fixture 행만 대상으로 하고 성공·실패 시 모두 명시적으로 rollback·종료한다.
- 경쟁 시나리오의 입찰가는 최소 증분과 최대 입찰가를 넘지 않도록 실행 전에 할당한다. 요청 도착 순서 때문에 정상 발생하는 `BID_TOO_LOW`를 실패나 잠금 충돌로 오분류하지 않는다.
- VU·요청률·지속 시간은 smoke와 단계 증가 실행으로 나누고, 로컬 장비 과부하가 서버 병목으로 오인되지 않도록 k6 실행 호스트의 CPU·메모리도 함께 확인한다.
- **조건부 확장:** 핵심 시나리오만으로 쓰기 비용 차이를 구분할 수 없을 때에만 동일 입찰자 연속 갱신과 입찰자 교체 시나리오를 추가한다. 확장 이유와 비교하려는 지표를 먼저 보고한다.

#### 완료 기준

- 네 핵심 시나리오가 동일한 fixture 준비 절차와 명시된 부하 조건으로 반복 실행된다.
- 결과에서 201, `BID_TOO_LOW`, `BID_CONFLICT`, 그 밖의 4xx, 5xx와 timeout을 각각 구분할 수 있다.
- 강제 `art` 잠금과 `point_account` 잠금의 실제 대기 시간 및 최종 HTTP 오류 계약이 각각 확인된다.
- 단일 작품, 완전 분산, 동일 계정 경합 결과를 같은 부하 단계에서 비교할 수 있다.

#### 자동 검증·실측

- k6 threshold는 스크립트 정상 동작을 보장하는 최소 조건과 예상하지 못한 5xx·네트워크 오류 탐지에만 사용한다. 성능 목표치는 첫 기준 측정 전에 임의로 정하지 않는다.
- 각 시나리오에서 처리량, 평균·p95·p99, 상태/오류 코드별 비율과 클라이언트 timeout 비율을 k6 summary로 저장한다.
- 강제 잠금 시작·해제 시각과 HTTP 요청 시작·종료 시각을 함께 기록하여 실제 timeout 경계를 계산한다.
- 각 실행 직후 요청 집계와 생성된 입찰 수를 비교하여 누락되거나 분류되지 않은 결과가 없는지 확인한다.

### 3단계 — MySQL·커넥션 풀 관찰과 종료 후 정합성 검증

#### 구현

- MySQL `performance_schema`와 사용 가능한 `sys` 뷰에서 lock waiter/blocker, 대기 시간과 관련 statement 시간을 시나리오 실행 구간에 수집한다. 전역 설정 변경이 필요한 관찰 기능은 임의로 활성화하지 않고 필요성과 영향을 먼저 보고한다.
- Hikari의 실제 maximum pool size와 connection timeout을 실행 시작 시 기록한다. 가능한 경우 테스트 전용 JMX 또는 이미 제공되는 런타임 지표로 active, idle, pending과 connection acquire 시간을 수집한다.
- 풀 내부 지표를 안전하게 얻을 수 없으면 애플리케이션 스레드/JFR과 DB 활성 세션 수를 보조 자료로 사용하고, 직접 확인한 값과 추론한 값을 결과에서 구분한다. production 관찰 설정은 이번 단계에서 변경하지 않는다.
- 실행 종료 후 시나리오 fixture 범위만 대상으로 다음 정합성 검증 SQL을 실행한다.
  - `art.current_price`와 정렬 계약상 최고 입찰 금액의 일치
  - `art.active_point_hold_id`와 최고 입찰의 사용자·금액·latest bid 일치
  - 작품별 HELD 상태의 활성 예치가 하나 이하인지 확인
  - 사용자별 가용 잔액과 예치 잔액의 합계 보존
  - 계정 잔액과 포인트 원장 delta 합계 일치
  - 실패·timeout 요청이 부분 입찰, 부분 예치 또는 부분 원장을 남기지 않았는지 확인

#### 완료 기준

- 각 k6 실행 구간을 대응하는 MySQL 잠금 자료와 가능한 범위의 커넥션 풀 자료가 존재한다.
- 단일 작품 병목, 동일 계정 병목, 풀 포화를 구분할 수 있는 비교표를 만들 수 있다.
- 모든 핵심 시나리오 종료 후 정합성 SQL이 통과하거나, 실패 항목이 재현 요청 및 DB 행까지 추적된다.
- 관찰 불가능한 지표가 있다면 누락 사실, 대체 관찰 자료와 판단 한계를 명시한다.

#### 자동 검증·실측

- 시나리오 시작 전·실행 중·종료 후 MySQL 세션 및 잠금 snapshot 수집
- Hikari JMX/런타임 지표 또는 대체 자료의 시계열 수집
- fixture 실행 ID를 제한 조건으로 한 정합성 검증 SQL과 실패 시 상세 조회
- k6 시각, 애플리케이션 오류 로그, DB lock wait 시각의 상호 대조

### 4단계 — 결과 판정과 후속 변경 제안

#### 구현

- 시나리오별 부하 조건, 처리량, 평균·p95·p99, 응답·오류 비율, DB 잠금과 풀 상태, 정합성 결과를 한 기준으로 정리한다.
- 다음 비교 근거로 병목을 판정한다.
  - 단일 작품만 지연 증가: `art` 행 잠금 후보
  - 작품 분산 후에도 동일 계정에서 지연 증가: `point_account` 행 잠금 후보
  - 경합 없는 분산 기준선에서도 pending/acquire time 증가: Hikari 풀 또는 애플리케이션 자원 후보
  - DB 실행·잠금 시간보다 HTTP 지연이 큼: 애플리케이션/JVM/커넥션 획득 후보
- 예상하지 못한 500, 장기 대기, 잘못된 도메인 오류 코드나 부분 반영이 발견되면 재현 조건, 원인, 영향, 지금 수정 필요 여부와 최소 변경 후보를 보고한다.
- 인덱스, 풀 크기, 락 전략, timeout 조정 등 production 변경은 이 결과 보고와 별도 사용자 승인 후 새 계획으로 진행한다. 이번 계획의 완료 조건에 production 변경을 포함하지 않는다.
- 작업 완료 시 `PLAN.md`에서 본 계획을 제거하고 `PLAN_DONE.md`에는 이후 판단에 필요한 핵심 실측 결과·계약·검증 한계만 1~3개 bullet로 남긴다. `BACKLOG.md`는 별도 승인 없이 수정하지 않는다.

#### 완료 기준

- 병목 위치를 실측 근거로 판정하거나, 판정할 수 없는 경우 부족한 관찰 자료와 추가 실험을 명확히 제시한다.
- 정상 경쟁 409와 잠금 409, 예상하지 못한 오류 및 timeout이 구분되어 있다.
- 후속 production 변경의 필요 여부가 `변경 불필요`, `추가 실험 필요`, `변경 제안` 중 하나로 정리되어 있다.
- 저장소에 민감정보, 실제 JWT, 사용자 비밀번호와 불필요한 대용량 원시 결과가 포함되지 않는다.

#### 자동 검증·실측

- 동일 조건 반복 실행에서 병목 판정이 재현되는지 비교
- k6 집계, MySQL/Hikari 관찰값과 정합성 SQL 결과의 누락 여부 점검
- 부하 도구·fixture 파일의 비밀정보 및 대상 DB 안전장치 검사
- 관련 테스트와 부하 smoke 실행 후 Git diff로 production 코드·설정 및 `BACKLOG.md` 비변경 확인

### 커밋 경계

사용자 승인 전에는 아래 커밋을 만들지 않는다. 승인 후에도 단계별 검증이 통과한 경우에만 다음 경계를 유지한다.

1. **계획 문서 커밋** — `PLAN.md`만 별도 커밋한다.
   - 예정 메시지: `chore: 동시 입찰 부하 테스트 계획 추가`
2. **부하 환경·검증 도구 커밋** — 전용 fixture 준비·안전장치, k6 핵심 시나리오, 잠금/풀 관찰과 정합성 검증 도구만 포함한다. production 코드·기본 설정과 결과 문서는 포함하지 않는다.
   - 예정 메시지: `chore: 동시 입찰 부하 검증 도구 추가`
3. **완료 문서 커밋** — 실측과 판정이 끝난 뒤 `PLAN.md`의 본 계획 제거와 `PLAN_DONE.md` 핵심 결과만 포함한다. 부하 원시 결과와 production 변경은 포함하지 않는다.
   - 예정 메시지: `chore: 동시 입찰 부하 검증 결과 정리`

병목 개선을 위한 production 변경이 필요하면 이번 세 커밋에 섞지 않고, 결과 보고와 사용자 승인 후 별도 계획·커밋 경계를 정한다.
