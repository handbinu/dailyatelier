# DailyAtelier 개발 계획

> [!IMPORTANT]
> 완료된 계획과 검증 결과는 `PLAN_DONE.md`에서 관리한다.
> 미완료·후속 작업은 삭제하지 않고 `BACKLOG.md`에서 관리한다.
> 새 작업 계획은 사용자 요청 범위가 확정된 뒤 이 문서에 추가한다.

## 핵심 도메인 품질 감사 계획

### 1. 목적과 기준

- 기준 브랜치는 현재 `dev`로 고정한다.
- 이번 감사의 목적은 코드 수정이 아니라 구현·테스트·설정 근거를 바탕으로 결함과 잠재 위험을 식별하는 것이다.
- 감사 중 코드, 테스트, 설정 파일은 수정하지 않는다.
- 발견사항은 구현만 보고 추측하지 않고 관련 테스트, 트랜잭션 경계, 보안 설정, DB 제약과 호출 흐름을 교차 확인한다.
- 확인된 동작 위반은 `확정된 결함`, 현재 근거만으로 재현이 확정되지 않은 취약 지점은 `잠재 위험`으로 구분한다.
- 부하 테스트, SQL 실행 계획 분석, 운영 환경의 락 타임아웃 실측은 실행하지 않으며 기존 `BACKLOG.md` 범위로 유지한다.

### 2. 감사 준비와 추적 방법

- [x] 감사 시작 시 `dev`의 커밋과 작업 트리 상태를 기록해 조사 기준점을 고정한다.
- [x] 회원, 작품, 입찰·경매, 주문, 포인트·결제, 배송·취소·환불 관련 코드와 테스트 파일 목록을 수집한다.
- [x] 컨트롤러부터 서비스, 저장소, 엔티티, DB 마이그레이션까지 핵심 호출 경로를 연결해 조사한다.
- [x] 각 의심 사항마다 정상 경로, 실패 경로, 권한 경로, 재시도·동시 실행 경로와 기존 테스트 유무를 확인한다.
- [x] 정적 검토만으로 판정할 수 없는 항목은 필요한 재현 또는 검증 방법을 기록하되 이번 계획 작성 단계에서는 실행하지 않는다.
- [x] 감사는 검사 영역 단위로 진행하며, 각 영역을 완료할 때마다 `QUALITY_AUDIT.md`를 즉시 갱신한 뒤 다음 영역으로 진행한다.
- [x] 영역별 기록에는 확인한 파일과 테스트, 발견사항 또는 무발견 근거, 추가 검증이 필요한 사항을 포함한다.
- [x] 모든 검사가 끝난 뒤 결과를 한꺼번에 작성하지 않으며, 발견사항이 없는 영역도 확인 근거를 반드시 남긴다.

### 3. 사용자 흐름 연결 감사

- [x] 회원가입 후 인증·사용자 상태가 작품 등록과 이후 거래 주체 식별에 일관되게 사용되는지 확인한다.
- [x] 작품 등록부터 입찰 가능 상태, 최고 입찰 결정, 경매 마감과 낙찰 주문 생성까지 데이터 연결을 추적한다.
- [x] 낙찰자의 배송지 확정 조건과 포인트 결제 가능 조건이 주문 상태 정책과 일치하는지 확인한다.
- [x] 결제 이후 배송 상태 변경, 구매 확정, 취소·포기·만료·환불 흐름에서 주문 상태와 포인트 원장이 함께 일관되게 변경되는지 확인한다.
- [x] 중간 단계 실패 또는 재호출 시 다음 단계가 잘못 허용되거나 영구적으로 막히는 경로가 있는지 확인한다.
- [x] 각 연결 구간을 보장하는 기존 통합·서비스·컨트롤러 테스트와 누락된 경계 조건을 대조한다.

### 4. 소유권과 권한 감사

- [x] 구매자 주문 조회·수정·결제·취소 요청이 인증 사용자와 주문 소유자를 검증하는지 확인한다.
- [x] 판매자 주문 조회와 배송 상태 변경이 작품 판매자 또는 허용된 역할로 제한되는지 확인한다.
- [x] 포인트 잔액, 거래 내역, 충전 내역이 요청 파라미터가 아닌 인증 주체를 기준으로 조회되는지 확인한다.
- [x] 포인트 충전 승인, 결제 콜백 처리와 기타 내부 승인 API의 노출 경로, 보안 설정, 호출 주체 검증을 확인한다.
- [x] IDOR, 역할 혼동, 누락된 인증, 타인 식별자 주입 가능성을 관련 보안·API 테스트와 함께 검토한다.

### 5. 트랜잭션 경계와 동시성 감사

- [x] 경매 마감과 최고 입찰 확정, 패찰 예치 해제, 낙찰 주문 생성이 하나의 실패 경계에서 원자적으로 처리되는지 확인한다.
- [x] 동시 마감, 마감과 입찰 경합, 마감 재실행에서 중복 주문·중복 원장 거래가 방지되는지 확인한다.
- [x] 포인트 차감, 예치·해제·확정, 주문 상태 변경의 트랜잭션 전파와 잠금 순서를 확인한다.
- [x] 결제, 결제 만료, 낙찰 포기, 취소·환불이 동시에 실행될 때 상태 전이와 잔액 변경이 정확히 한 번만 적용되는지 확인한다.
- [x] 예외가 잡혀서 정상 반환되거나 별도 트랜잭션으로 분리되면서 부분 커밋될 가능성을 확인한다.
- [x] H2 및 MySQL 전용 동시성·롤백 테스트가 실제 구현 경계를 충분히 검증하는지 대조한다.

### 6. 결제 콜백과 포인트 원장 감사

- [x] 결제 콜백의 공급자 이벤트 식별자, 저장 순서, 중복 수신 처리와 재처리 상태 전이를 확인한다.
- [x] 같은 콜백의 동시 처리, 처리 중 실패, 처리 완료 후 재수신에서 충전·잔액 변경이 중복 적용되지 않는지 확인한다.
- [x] 콜백 금액·주문·사용자·상태 검증과 내부 충전 주문의 연결 조건을 확인한다.
- [x] 계정 잔액과 원장 합계, 활성 예치와 예치 잔액, 주문 결제 상태와 원장 참조 사이의 불일치 가능성을 확인한다.
- [x] 정합성 검사 서비스와 운영 조회 SQL이 모든 거래 유형과 장애 상태를 포괄하는지 확인한다.
- [x] 멱등 키, 유니크 제약, 행 잠금과 애플리케이션 검증이 서로 다른 실패 시점에도 유효한지 관련 테스트와 대조한다.

### 7. Flyway 적용 위험 감사

- [x] 빈 MySQL DB에 `V1`부터 최신 버전까지 순서대로 적용할 때 엔티티 매핑과 일치하는지 정적으로 확인한다.
- [x] 기존 테이블과 데이터가 있는 DB에 적용할 때 컬럼 추가, 외래키 정규화, `NOT NULL`, 유니크·체크 제약으로 실패하거나 데이터가 손실될 위험을 확인한다.
- [x] 마이그레이션의 재실행·부분 실패·복구 경계와 Flyway 이력 불일치 가능성을 확인한다.
- [x] H2 테스트 스키마가 MySQL 전용 타입, 인덱스, 제약과 다른 동작을 숨기는지 확인한다.
- [x] 기존 MySQL 스키마·복구 테스트가 다루는 조건과 실제 기존 데이터 전제 사이의 공백을 기록한다.

### 8. 프론트 상태 감사

- [x] 회원가입, 작품 등록, 입찰, 주문, 충전·포인트 결제 화면의 로딩 시작·종료와 버튼 비활성화 조건을 확인한다.
- [x] 성공, 유효성 오류, 권한 오류, 잔액 부족, 충돌, 서버 오류가 사용자에게 구분되어 표시되는지 확인한다.
- [x] 중복 클릭과 네트워크 재시도에서 입찰·충전·결제가 중복 요청되거나 잘못 성공 표시되는지 확인한다.
- [x] `401` 또는 만료 토큰 발생 시 인증 상태 정리, 로그인 이동, 진행 중 화면 상태가 일관되는지 확인한다.
- [x] `409` 등 경합 응답 후 주문·잔액·입찰 상태를 다시 조회하는 범위가 충분한지 확인한다.
- [x] 화면 상태 처리와 API 오류 변환을 기존 프론트 테스트, ESLint와 빌드 결과만으로 보장할 수 없는 부분까지 구분한다.

### 9. 구조와 응답 일관성 감사

- [x] 핵심 서비스가 도메인 정책, 트랜잭션 조정, 외부 연동과 응답 조립을 과도하게 함께 담당하는지 확인한다.
- [x] 주문 상태 검증, 소유권 확인, 포인트 멱등 처리와 오류 변환이 여러 위치에 중복되어 서로 다르게 동작할 가능성을 확인한다.
- [x] 컨트롤러별 예외 처리기가 동일한 인증·권한·충돌·검증 오류에 일관된 HTTP 상태와 응답 형식을 제공하는지 확인한다.
- [x] 엔티티 상태 변경 메서드와 서비스 직접 변경이 혼용되어 불변식이 우회되는지 확인한다.
- [x] 구조상 개선 여지는 실제 결함과 분리하고, 현재 기능 위험에 연결되는 근거가 있을 때만 발견사항으로 기록한다.

### 10. 발견사항 기록 계획

- [ ] 신규 `QUALITY_AUDIT.md`를 만들고 검사 영역별 완료 시점마다 결과를 즉시 기록한다.
- [ ] 한 영역의 확인 파일과 테스트, 발견사항 또는 무발견 근거, 추가 검증 필요 사항을 모두 기록하기 전에는 다음 영역으로 진행하지 않는다.
- [ ] 전체 감사 종료 후에는 이미 작성한 영역별 기록을 바탕으로 심각도별 발견사항과 전체 요약만 정리하며, 결과를 사후에 일괄 작성하지 않는다.
- [ ] 각 발견사항에는 아래 항목을 빠짐없이 작성한다.
  - 심각도
  - 관련 파일과 코드 위치
  - 발생 조건
  - 영향
  - 근거
  - 재현 또는 검증 방법
  - 권장 수정안
  - 확정된 결함인지 잠재 위험인지
- [ ] 관련 코드 위치는 가능한 한 파일 경로와 메서드 또는 코드 줄을 함께 기록한다.
- [ ] 기존 테스트가 발견사항을 보장하지 못하는 이유 또는 발견을 반증하는 범위를 명시한다.
- [ ] 재현하지 못했거나 환경 의존적인 항목은 결함으로 단정하지 않고 필요한 추가 검증 조건을 기록한다.
- [ ] 발견사항이 없는 검사 영역도 확인한 파일·테스트와 판단 근거를 요약해 감사 누락과 무발견을 구분한다.

### 11. 감사 완료 조건

- [ ] 일곱 개 집중 검사 범위마다 구현과 기존 테스트 근거를 모두 확인한다.
- [ ] 모든 발견사항이 지정된 기록 항목과 결함·위험 구분을 충족한다.
- [ ] 실행하지 않은 운영 검증 항목은 `BACKLOG.md` 대상임을 명확히 표시한다.
- [ ] 코드, 테스트, 설정 파일에 변경이 없음을 확인한다.
- [ ] 감사 결과와 우선순위 요약을 사용자에게 보고한 뒤 수정 작업은 별도 승인을 받아 진행한다.

## 품질 감사 발견사항 수정 실행 계획

### 1. 실행 원칙과 범위

- 기준은 `QUALITY_AUDIT.md`의 QA-001~QA-014와 현재 P0~P2 우선순위다.
- 각 단계는 테스트로 현재 문제를 재현하거나 계약을 고정한 뒤 구현하고, 관련 회귀 테스트까지 통과해야 완료한다.
- 서로 다른 위험 경계를 한 커밋에 섞지 않는다. 특히 주문 상태 정책, 콜백 트랜잭션, 신규 DB 기준 스키마와 기존 DB 데이터 이관은 각각 독립적으로 검증하고 커밋한다.
- 작업 중 감사 범위 밖의 결함을 발견하면 임의 수정하지 않고 별도 보고 후 승인받는다.
- 실제 운영 DB, 실제 PG, 부하·실행 계획·락 타임아웃과 브라우저 장애 주입이 필요한 검증은 이번 수정 범위에서 제외하고 `BACKLOG.md` 대상으로 유지한다.
- 실제 택배사 또는 통합 배송조회 API 연동도 이번 포트폴리오 수정 범위에서 제외하고 `BACKLOG.md` 대상으로 유지한다.
- `QA-006`은 실제 PG 콜백 진입점과 응답 계약이 없는 현재 단계에서는 코드 변경을 강행하지 않는다. 아래 4단계에서 현재 구조에 대한 회귀 테스트 범위만 확인하고 실제 동시 최초 수신 검증·보완은 BACKLOG로 분리한다.

### 2. 전체 권장 실행 순서

1. 주문 배송 완료·환불 흐름 연결
2. 운영 데모 충전의 표시·발행 통제와 프론트 상태 보완
3. 결제 콜백 트랜잭션 경계 확정
4. 포인트 의미적 정합성 검사 확장
5. 빈 DB 기준 Flyway 체계 구축
6. 기존 DB 데이터 안전 이관
7. 공통 오류 응답과 페이지 검증 통일
8. 회원가입 비동기 검증 경합 보완
9. 전체 회귀 검증과 운영 BACKLOG 인계

P0 사용자 흐름과 포인트 안전성인 1~3단계를 먼저 처리한다. DB 변경인 5~6단계는 애플리케이션 변경과 별도 커밋으로 유지하며, 5단계의 기준 스키마가 확정된 뒤 6단계 기존 데이터 이관을 작성한다. 프론트가 안정적인 오류 코드에 의존할 수 있도록 7단계 오류 계약을 8단계의 최종 프론트 회귀보다 먼저 완료한다.

### 3. 주문 배송 완료·환불 흐름 연결

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-001`, `QA-002`
- 수정 목적:
  - 실제 API·화면을 통한 주문 흐름이 `SHIPPED`에서 중단되지 않고 `DELIVERED → CONFIRMED`까지 이어지게 한다.
  - 이미 구현된 결제 후 환불과 포인트 반대 거래를 승인된 정책에 따라 실제 진입점에 연결한다.
- 확정 정책:
  - 구매자가 `SHIPPED` 주문을 배송 완료 처리한 뒤 별도로 구매 확정할 수 있게 한다.
  - 구매자가 구매 확정 전에 환불을 요청하고 판매자가 승인 또는 거절한다.
  - 판매자 승인 시 포인트 환불과 `REFUNDED` 전이를 수행하며 `CONFIRMED` 이후 일반 환불은 허용하지 않는다.
  - 실제 택배사 API 기반 자동 배송 완료는 이번 범위에서 제외한다.
- 예상 수정 범위:
  - 주문 상태 API와 DTO, `OrderStateService` 호출 경로, 필요한 권한 검증
  - 구매·판매 주문 프론트 API와 `OrderStatus` 또는 `SalesOrders` 화면 동작
  - 주문 상태·환불 서비스, 컨트롤러와 프론트 컴포넌트 테스트
- 필요한 테스트와 검증:
  - 결제 → 준비 → 발송 → 배송 완료 → 구매 확정 API 통합 테스트
  - 허용되지 않은 주체와 상태에서 배송 완료·환불이 403 또는 409로 거부되는 테스트
  - 환불 성공 시 주문 상태, 계정 잔액, 원장 반대 거래가 한 트랜잭션에서 정확히 한 번 변경되는 테스트
  - 기존 결제 만료·구매자 포기·판매자 발송 회귀 테스트
- 완료 조건:
  - 실제 컨트롤러와 프론트 호출로 `SHIPPED → DELIVERED → CONFIRMED`가 가능하다.
  - 확정한 환불 정책에 맞는 API·화면 진입점과 소유권·역할 검증이 존재한다.
  - 상태 변경과 포인트 환불의 성공·실패·재호출 테스트가 모두 통과한다.
- 커밋 단위:
  1. `fix(order): 배송 완료와 구매 확정 흐름 연결`
  2. `feat(order): 결제 후 환불 요청 흐름 연결`

### 4. 운영 데모 충전 안전장치와 화면 상태 보완

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-003`, `QA-010`, `QA-012`
- 수정 목적:
  - 운영 포트폴리오에서도 데모 충전을 유지하되 실제 금전 결제가 아님을 명확히 표시하고 발행량을 통제한다.
  - 초기 조회 실패와 불확실한 재시도에서 잘못된 잔액 표시나 멱등성 키 오용을 방지한다.
- 예상 수정 범위:
  - 데모 충전 요청·응답 DTO, 충전 정책 서비스 또는 설정값, 계정 보유 한도 검증
  - 포인트 거래 유형 또는 설명의 `DEMO_CHARGE` 구분과 조회 응답
  - 충전 화면의 데모 표시, 고정 금액 선택, 로딩·오류·재시도 상태와 제출 조건
  - 요청 내용 스냅샷과 멱등성 키 수명 관리
  - 필요 시 계정별 충전 요청 제한 지점은 인터페이스만 정의하고 실제 운영 rate limit 검증은 BACKLOG로 둔다.
- 필요한 테스트와 검증:
  - 허용 금액, 최대 보유 한도, 선택적 재충전 기준의 서비스 경계 테스트
  - 같은 요청의 동일 키 재시도와 다른 금액의 키 재사용 거부 테스트
  - 모든 데모 충전이 명시적 거래 유형·사유로 원장에 기록되는 테스트
  - 초기 조회 중·실패·재시도 화면, 금액 변경 후 새 키 발급과 동일 요청 키 유지 컴포넌트 테스트
  - 기존 충전·잔액·거래 내역 API 회귀 테스트
- 완료 조건:
  - API 응답과 화면에서 데모 포인트임을 식별할 수 있다.
  - 서버가 허용 금액과 계정별 보유 한도를 강제하고 원장에 데모 충전 유형을 기록한다.
  - 조회 완료 전 또는 실패 상태에서는 잘못된 0원 잔액을 확정값처럼 표시하거나 충전을 제출하지 않는다.
  - 요청 내용이 바뀌면 새 멱등성 키를, 동일 요청 재시도에는 기존 키를 사용한다.
- 커밋 단위:
  1. `feat(point): 데모 충전 발행 정책과 원장 유형 추가`
  2. `fix(frontend): 충전 로딩과 멱등 재시도 상태 보완`

### 5. 결제 콜백 트랜잭션 경계 확정

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-004`
- 연계 BACKLOG: `QA-006`
- 수정 목적:
  - 콜백 처리기 실패 시 도메인 변경과 inbox 상태가 부분 커밋되거나 rollback-only 충돌을 일으키지 않도록 실패 경계를 명시한다.
- 예상 수정 범위:
  - `PaymentCallbackService`의 처리 트랜잭션과 실패 기록 경계
  - 필요 시 실패 상태 기록을 위한 별도 트랜잭션 서비스
  - 콜백 처리기 테스트 대역과 트랜잭션 통합 테스트
- 필요한 테스트와 검증:
  - 처리기가 DB 변경 후 예외를 던지는 경우 도메인 변경의 롤백 여부 검증
  - 실패 상태·시도 횟수 기록의 커밋 여부와 재처리 가능 상태 검증
  - 완료 이벤트 재처리 단락과 순차 중복 수신 회귀 테스트
  - 실제 PG 서명·응답과 MySQL 동시 최초 insert 경합은 실행하지 않고 BACKLOG에 유지
- 완료 조건:
  - 처리 성공, 처리 실패, 실패 후 재시도의 트랜잭션 결과가 테스트로 고정된다.
  - 예외를 잡아 정상 반환하면서 부분 도메인 변경이 커밋되는 경로가 없다.
  - `QA-006`을 실제 PG 도입 시 수행할 테스트 조건과 기대 응답이 BACKLOG에 유지된다.
- 커밋 단위:
  - `fix(payment): 콜백 실패 트랜잭션 경계 명확화`

### 6. 포인트 의미적 정합성 검사 확장

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-005`
- 수정 목적:
  - 계정 잔액과 원장 델타 합계뿐 아니라 활성 예치, 작품 참조, 주문 결제·환불과 충전 상태의 의미적 불일치를 탐지한다.
- 예상 수정 범위:
  - `PointLedgerConsistencyService`와 관련 repository 조회 또는 전용 진단 DTO
  - 계정 없는 사용자·원장, 활성 hold 합계, `art.active_point_hold_id`, 주문 `COMMIT`·`REFUND`, 충전 `CHARGE`·환불 관계 검사
  - 검사 유형별 식별자와 원인을 제공하는 결과 모델
- 필요한 테스트와 검증:
  - 각 의미적 불일치를 독립적으로 만든 서비스·repository 테스트
  - 정상 데이터와 기존 잔액 합계 불일치 검사의 회귀 테스트
  - 큰 데이터셋 실행 계획과 운영 경보 주기 측정은 BACKLOG로 분리
- 완료 조건:
  - 감사에서 제시한 계정·원장·예치·작품·주문·충전 불일치 유형을 각각 탐지한다.
  - 결과가 불일치 종류와 대상 식별자를 구분해 후속 조사에 사용할 수 있다.
  - 정상 거래 흐름에 오탐이 없다는 테스트가 통과한다.
- 커밋 단위:
  - `feat(point): 원장 의미적 정합성 검사 확장`

### 7. 빈 DB 기준 Flyway 체계 구축

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-007`
- 수정 목적:
  - 빈 MySQL DB가 외부 Hibernate DDL 없이 Flyway 전체 이력만으로 생성되고 `ddl-auto=validate`를 통과하게 한다.
- 예상 수정 범위:
  - 핵심 레거시 테이블을 포함하는 기준 스키마 마이그레이션 또는 안전한 baseline 전략
  - 기존 V1~V5와의 버전·checksum 호환 방안
  - 빈 DB 전용 MySQL/Testcontainers 스키마 테스트와 배포 문서
- 필요한 테스트와 검증:
  - 격리된 빈 MySQL에서 최초 migration 전체 적용
  - 적용 후 Hibernate `validate` 애플리케이션 컨텍스트 기동
  - 두 번째 migrate 실행 건수 0과 필수 인덱스·외래키·체크 제약 확인
  - 운영 DB 적용은 하지 않고 복제 환경 리허설을 BACKLOG에 유지
- 완료 조건:
  - 빈 DB에서 최신 스키마까지 Flyway만으로 생성된다.
  - 기존 설치 DB를 잘못 baseline하거나 이미 적용된 migration checksum을 깨뜨리지 않는 전략이 문서화된다.
  - 신규 DB 자동 테스트가 통과한다.
- 커밋 단위:
  - `fix(backend): 빈 DB Flyway 기준 스키마 구축`

### 8. 기존 DB 데이터 안전 이관

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-008`, `QA-009`
- 수정 목적:
  - 기존 null 잔액과 진행 중 경매·주문이 존재해도 마이그레이션이 중단되거나 포인트 예치·원장 관계가 깨지지 않게 한다.
- 선행 조건:
  - 읽기 전용 사전 조사로 실제 스키마 버전, null `reserve`, 활성 입찰·주문 건수를 확인한다.
  - 활성 거래를 이관할지 배포 전에 종료·정리할지 정책을 확정한다.
- 예상 수정 범위:
  - null `reserve` 정규화와 제약 강화 순서
  - 활성 최고 입찰의 계정·예치·원장·작품 참조 이관 또는 명시적 거래 정리 절차
  - 기존 주문의 결제 방식·예치·원장 상태 변환 또는 제외 정책
  - 사전·사후 검증 SQL과 레거시 fixture 기반 migration 테스트
- 필요한 테스트와 검증:
  - null·0·양수 `reserve` 이관 테스트
  - 진행 중 경매, 결제대기, 결제완료 주문 fixture별 migration 후 업무 동작 테스트
  - 부분 데이터가 이미 존재하는 경우 중복 생성 방지와 실패 메시지 검증
  - 운영 데이터 복제본의 DDL 락 시간과 실제 리허설은 BACKLOG로 유지
- 완료 조건:
  - 확정된 정책에 따라 모든 레거시 상태가 이관 또는 사전 정리 대상으로 판별된다.
  - migration 후 계정·원장·예치·작품·주문의 사후 검증이 통과한다.
  - 기존 Flyway 이력과 복구 절차가 문서화되고 테스트 fixture에서 재현된다.
- 커밋 단위:
  1. `fix(backend): 기존 포인트 잔액 이관 안전성 보완`
  2. `fix(backend): 활성 경매와 주문 원장 이관 추가`

### 9. 공통 오류 응답과 페이지 검증 통일

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-013`, `QA-014`
- 수정 목적:
  - 회원·작품·주문·입찰·포인트·보안 API가 같은 오류 응답 필드와 안정적인 오류 코드를 제공하게 한다.
  - 포인트 목록의 잘못된 페이지 요청을 예측 가능한 400 응답으로 변환한다.
- 예상 수정 범위:
  - 공통 도메인 API 예외 또는 전역 `RestControllerAdvice`
  - 기존 컨트롤러 한정 예외 처리기의 통합·우선순위 정리
  - 회원·작품 서비스의 `ResponseStatusException` 변환
  - 목록 페이지 요청 검증의 공통 정책과 포인트 조회 적용
- 필요한 테스트와 검증:
  - 대표 400·401·403·404·409 응답의 `timestamp`, `status`, `code`, `message`, `path` 계약 테스트
  - 회원·작품의 기존 HTTP 상태와 사용자 메시지 회귀 테스트
  - 포인트 `page=-1`, 크기 0·상한 초과 경계 테스트
  - 주문·입찰·보안 예외 처리 회귀 테스트
- 완료 조건:
  - 핵심 API의 대표 오류가 동일한 JSON 구조와 문서화된 코드로 반환된다.
  - 처리되지 않은 입력 오류가 의도치 않은 500으로 노출되지 않는다.
  - 프론트가 문자열이 아닌 오류 코드로 분기할 수 있는 계약 테스트가 통과한다.
- 커밋 단위:
  1. `refactor(backend): API 예외 응답 계약 통합`
  2. `fix(point): 포인트 페이지 요청 검증 추가`

### 10. 회원가입 비동기 중복확인 경합 보완

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-011`
- 수정 목적:
  - 이전 입력값의 늦은 중복확인 응답이 현재 입력값을 검증 완료로 표시하거나 제출을 허용하지 않게 한다.
- 예상 수정 범위:
  - 일반·작가 회원가입의 필드별 확인 중 상태, 요청 값 스냅샷과 최신 요청 식별 또는 취소 처리
  - 중복확인 버튼 비활성화와 입력 변경 시 검증 상태 초기화
  - 두 화면이 공유할 수 있는 훅 또는 유틸리티는 중복 제거 효과가 명확할 때만 도입
- 필요한 테스트와 검증:
  - A 요청 후 B 입력 변경, A 응답 지연·도착 시 B가 미확인 상태로 유지되는 컴포넌트 테스트
  - 중복 응답 순서 역전, 요청 실패, 동일 값 재확인과 제출 차단 테스트
  - 일반·작가 회원가입 성공과 서버 중복 거부 회귀 테스트
- 완료 조건:
  - 검증 완료 표시는 응답이 현재 입력값과 일치할 때만 적용된다.
  - 확인 중이거나 입력 변경 후 미확인 상태에서는 제출할 수 없다.
  - 두 회원가입 화면의 비동기 경계 테스트가 통과한다.
- 커밋 단위:
  - `fix(frontend): 회원가입 중복확인 응답 경합 방지`

### 11. 전체 회귀 검증과 운영 BACKLOG 인계

- 상태: [ ] 미착수
- 수정 대상 QA: `QA-001`~`QA-005`, `QA-007`~`QA-014`
- BACKLOG 유지 QA: `QA-006` 및 각 QA의 운영 환경 의존 검증
- 수정 목적:
  - 개별 수정이 회원가입 → 작품 등록 → 입찰 → 낙찰 → 주문 → 포인트 결제 → 배송·취소·환불의 전체 연결을 훼손하지 않았는지 확인한다.
- 예상 수정 범위:
  - 이 단계는 원칙적으로 기능 코드를 추가하지 않고 테스트 결과, 계획 완료 상태와 BACKLOG 인계 문서만 정리한다.
  - 회귀에서 새 결함이 발견되면 해당 원인 단계로 돌아가 별도 커밋으로 수정한다.
- 필요한 테스트와 검증:
  - 백엔드 전체 단위·서비스·컨트롤러 테스트
  - 프론트 테스트, ESLint와 production build
  - 가능한 범위의 로컬 핵심 사용자 흐름 smoke test
  - 변경된 migration의 격리 MySQL 테스트
  - `git diff --check`와 코드·테스트·설정 변경 범위 확인
- 완료 조건:
  - 이번 수정 대상 QA마다 재현 테스트 또는 계약 테스트와 해결 근거가 연결된다.
  - 전체 자동 테스트와 정적 검사가 통과하고 실패·제외 항목의 이유가 기록된다.
  - 실제 PG, 운영 MySQL, 부하·락·브라우저 장애 주입 항목이 `BACKLOG.md`에 누락 없이 남는다.
  - `QUALITY_AUDIT.md`의 QA 상태는 검증 결과에 근거해 갱신하며 잠재 위험을 임의로 해결 처리하지 않는다.
- 커밋 단위:
  - `chore: 품질 감사 수정 결과와 후속 검증 정리`
