# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## UI 선별 개선

### 작업 목표

- 현재 UI 구조와 기능 흐름을 보존하면서, 실제 미완성 또는 사용성 문제가 확인된 화면만 선별 개선한다.
- 홈은 원본 구조를 유지하고 Header, 저개수 섹션의 밀도, 종료 작품의 정렬·간격 등 확인된 부분만 개선한다.
- 모바일과 데스크톱에서 입력·거래·탐색의 가독성과 조작 문제를 실제 QA 결과에 따라 해결한다.
- 현재 API·도메인 계약과 접근성 동작을 유지하며, 기존 자동 테스트가 검증하는 요청·상태·라우팅 계약을 깨지 않는다.

### 명시적인 비목표

- 백엔드 API, 응답 DTO, 경매·입찰·낙찰·주문·리뷰 도메인 규칙은 변경하지 않는다.
- 모든 화면을 한 번에 교체하거나 기존 CSS 전체를 일괄 재작성하지 않는다.
- 전면 구조 재설계, 홈 섹션 순서 변경, 공통 디자인 토큰의 선제 교체는 하지 않는다.
- 특정 레퍼런스 사이트의 레이아웃, 브랜드 표현, 컴포넌트를 복제하지 않는다.
- 검정·금색 중심의 전통 경매 스타일, 대형 장식 문구, 과한 패럴랙스·전환 애니메이션은 도입하지 않는다.
- 주문 상세, 작품 수정, 리뷰, 문의 화면의 정보 구조는 이번 범위에서 전면 재설계하지 않는다.
- Storybook 도입, 범용 UI 라이브러리 구축, 사용처가 하나뿐인 컴포넌트의 선제 추출은 하지 않는다.
- 낙찰과 주문의 도메인·라우트를 합치지 않는다. 중복 인상을 줄이되 기존 역할과 URL은 유지한다.

### 공통 구현 원칙

- 공통 스타일은 실제로 둘 이상의 대상 화면에서 중복이 확인된 색상, 간격, 타이포그래피, 버튼, 입력, 상태 배지, 작품 카드에 한해 정리한다.
- 기존 API 요청·응답, 도메인 규칙, 라우트, 인증·권한 조건, 사용자 행동 결과와 접근성 계약을 유지한다.
- 문제 범위 안에서만 간격·정렬·텍스트 위계·반응형 배치를 조정하며, 공통 스타일은 둘 이상의 화면에서 실제 중복이 확인될 때만 수정한다.
- 상태는 텍스트와 기호를 함께 제공하고, 오류 외 상태를 색상만으로 전달하지 않는다.
- 기존 폰트 로딩과 브랜드 자산은 교체하지 않으며, 고정 높이와 화면 밖 가로 스크롤을 피한다.
- 로딩·빈 상태·오류·재시도·비활성·제출 중 상태는 기존 동작을 보존하고 새 레이아웃에서도 같은 위치 문맥 안에서 이해되게 한다.

## 단계별 구현 범위

> 전체 UI 개선은 원본 기반 선별 개선으로 진행한다. 2·3·5단계는 각 대상 화면의 조사·브라우저 QA에서 문제가 확인되고 사용자 승인을 받은 뒤에만 구현 범위를 확정한다.

### 2단계 — 마이페이지·거래 화면의 확인된 사용성 개선

- 마이페이지의 역할별 업무 진입, 사용자 유형별 메뉴, 입찰·낙찰·주문 상태와 다음 행동이 실제 이용 흐름에서 이해되는지 조사·브라우저 QA로 확인한다.
- 좁은 화면에서 핵심 업무 진입점, 거래 상태, 목록 정보가 과도한 스크롤·겹침·잘림 없이 읽히는지 확인한다.
- 확인된 탐색·상태 이해·반응형 가독성 문제만 사용자 승인 후 개선하며, 구체적인 재배치나 전면 개편은 미리 확정하지 않는다.
- 문제 확인 전에는 구현하지 않으며 기존 정보 구조와 행동을 유지한다.

### 3단계 — 작품 목록·상세의 확인된 탐색·거래 문제 개선

- 검색·필터·정렬 후 결과 문맥, 작품 목록의 상태·가격 정보, 작품 상세의 이미지·거래 정보·행동이 실제 탐색 흐름에서 이해되는지 조사·브라우저 QA로 확인한다.
- `UPCOMING` 작품은 `/search` 상태 필터에서는 접근 가능하지만 일반 경매 진입점에서 발견성이 낮으므로, 예정 작품 탐색 진입점이 충분한지 확인한다.
- 모바일과 확대 화면에서 필터·정렬·입찰·찜·로그인 복귀 등 기존 행동이 가려지거나 혼동되지 않는지 확인한다.
- 확인된 탐색·거래 정보 이해·반응형 가독성 문제만 사용자 승인 후 개선하며, 구체적인 재배치나 전면 개편은 미리 확정하지 않는다.
- 문제 확인 전에는 구현하지 않으며 기존 정보 구조와 행동을 유지한다.

### 5단계 — 보존 대상 화면의 선별 정합성 개선

- 주문 상세의 조건부 행동, 작품 수정의 기존 값·이미지·제출 흐름, 리뷰·문의의 입력·오류·빈 상태·완료 피드백이 실제 이용 흐름에서 이해되는지 조사·브라우저 QA로 확인한다.
- 키보드 탐색, 확대·모바일 화면, 상태·오류 전달에서 사용성 또는 접근성 문제가 있는지 확인한다.
- 확인된 사용성·접근성 문제만 사용자 승인 후 개선하며, 구체적인 재배치나 전면 개편은 미리 확정하지 않는다.
- 문제 확인 전에는 구현하지 않으며 기존 정보 구조와 행동을 유지한다.

## 접근성 유지 기준

- 모든 기능을 키보드만으로 사용할 수 있어야 하며, 기존 논리적 탭 순서와 포커스 복귀를 깨지 않는다.
- 기존 `AccessibleDialog`의 포커스 트랩, Escape 닫기, 초기·복귀 포커스를 유지한다.
- 버튼과 링크의 의미 요소를 시각 편의를 위해 `div` 클릭으로 대체하지 않는다.
- 입력은 연결된 `label`, 필수 여부, 도움말과 오류의 프로그램적 관계를 유지한다. 오류는 색상만으로 표시하지 않는다.
- 상태 배지, 가격 변동, 낙찰·패찰·주문 상태는 텍스트 레이블을 제공하고 색상만으로 구분하지 않는다.
- 이미지에는 기존 대체 텍스트 계약을 유지하고, 장식 이미지만 빈 대체 텍스트를 사용한다.
- 포커스 표시를 제거하지 않으며 웜 화이트·세이지 배경에서도 식별 가능한 대비를 확보한다.
- `prefers-reduced-motion`을 존중하고 새 전환 효과는 정보 이해에 필요한 짧은 수준으로 제한한다.
- 200% 확대와 320px 폭에서 핵심 콘텐츠·행동이 잘리거나 겹치지 않아야 한다.

## 자동 테스트 범위

- 각 단계는 조사·브라우저 QA에서 문제를 확인하고 사용자 승인을 받은 뒤, 변경 화면의 기존 Vitest/Testing Library 테스트를 우선 유지·갱신한다. 시각적 클래스명이나 DOM 깊이보다 사용자에게 보이는 역할·이름·상태와 행동을 검증한다.
- 2단계가 승인되면: 사용자 유형별 메뉴, 입찰 상태, `orderId` 유무에 따른 다음 행동, 최신 요청 우선 처리와 재시도를 검증한다.
- 3단계가 승인되면: 검색 파라미터, 필터·정렬, 진행/종료/취소 상태, 입찰 단위, 찜·입찰·로그인 복귀, 접근 가능한 다이얼로그를 검증한다.
- 5단계가 승인되면: 작품 수정, 주문 상세, 리뷰·문의의 기존 API 호출과 상태별 행동이 유지되는지 검증한다.
- 각 승인된 단계의 구현 전 관련 테스트를 실행하고, 단계 종료 시 변경 대상 테스트·전체 프론트 테스트·프로덕션 빌드를 실행한다.
- CSS 픽셀 값이나 스냅샷만으로 완료를 판정하지 않는다. 반응형 배치와 실제 가독성은 사용자 브라우저 QA로 확인한다.

## 사용자 브라우저 QA 시나리오 및 완료 조건

- 브라우저 QA의 기본 수행자는 사용자이며, 에이전트가 직접 브라우저 QA를 수행하지 않는다.
- 각 단계는 `조사/브라우저 QA → 문제 확인 → 사용자 승인 → 구현 → 자동 테스트·브라우저 QA` 순서로 진행한다.
- 사용자는 실제 로컬 데이터와 로그인 계정으로 조사·QA를 수행하고 결과를 전달한다. 에이전트는 확인된 문제와 승인 범위 안에서만 구현한다.
- 사용자가 기능 회귀와 확인된 문제의 해소에 동의해야 해당 단계의 브라우저 QA가 완료된 것으로 본다.
- 실제 문제가 확인되지 않은 화면은 구현하지 않으며, 각 개선은 해당 화면의 자동 테스트와 사용자 브라우저 QA가 통과한 뒤에만 다음 확인된 문제로 진행한다.

### 공통 환경

- 데스크톱: 1440px 및 1024px 전후 폭에서 Chrome 기준으로 확인한다.
- 모바일: 390px 전후와 최소 320px 폭에서 확인하고, 세로 스크롤·키보드 입력·다이얼로그 포커스를 함께 확인한다.
- 브라우저 200% 확대 또는 동등한 좁은 뷰에서 가로 스크롤, 콘텐츠 겹침, 행동 버튼 유실 여부를 확인한다.
- 마우스 없이 주요 탐색, 폼 입력, 다이얼로그 열기/닫기, 제출까지 수행한다.

### 단계별 조사·QA 시나리오

1. 2단계 후보: 일반 사용자·작가 계정으로 마이페이지, 입찰·낙찰·주문 관련 화면을 이용하며 허용된 업무 진입, 거래 상태, 다음 행동의 혼동 여부를 기록한다.
2. 3단계 후보: 검색·필터·정렬·상세 이동과 이미지 확인, 찜·입찰·로그인 복귀를 수행하며 탐색 문맥과 거래 정보 이해의 문제를 기록한다.
3. 5단계 후보: 주문 상세, 작품 수정, 리뷰·문의에서 조건부 행동, 현재 값·이미지 유지, 입력·오류·빈 상태·완료 피드백, 키보드 탐색의 문제를 기록한다.

## 커밋 계획

1. 2단계는 조사·브라우저 QA로 문제를 확인하고 사용자 승인 후 커밋 경계를 정한다.
2. 3단계는 조사·브라우저 QA로 문제를 확인하고 사용자 승인 후 커밋 경계를 정한다.
3. 5단계는 조사·브라우저 QA로 문제를 확인하고 사용자 승인 후 커밋 경계를 정한다.

각 커밋은 해당 범위의 구현, 관련 자동 테스트, 필요한 화면 전용 CSS 정리까지만 포함한다. 계획 문서 커밋은 사용자 승인 후 구현 커밋과 분리한다.
