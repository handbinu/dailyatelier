# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 포트폴리오 UI 리디자인

### 작업 목표

- DailyAtelier의 웜 화이트·차콜·세이지 색상 방향과 현재 기능 구조를 유지하면서, 작품과 경매 상태가 먼저 읽히는 서비스 화면으로 정리한다.
- Artsy의 작품 중심 탐색과 담백한 정보 구조, Sotheby's의 절제된 타이포그래피, Christie's 작품 상세의 이미지·거래 정보 위계를 DailyAtelier 문맥에 맞게 선택적으로 해석한다.
- 홈과 마이페이지 허브를 우선 재구성하고, 입찰·낙찰 연결 경험과 작품 탐색·상세를 다음 우선순위로 개선한다.
- 모바일에서는 핵심 업무로 이동하는 데 필요한 스크롤과 판단 비용을 줄이고, 데스크톱에서는 과도한 카드 폭과 느슨한 정보 밀도를 바로잡는다.
- 현재 API·도메인 계약과 접근성 동작을 유지하며, 기존 자동 테스트가 검증하는 요청·상태·라우팅 계약을 깨지 않는다.

### 명시적인 비목표

- 백엔드 API, 응답 DTO, 경매·입찰·낙찰·주문·리뷰 도메인 규칙은 변경하지 않는다.
- 모든 화면을 한 번에 교체하거나 기존 CSS 전체를 일괄 재작성하지 않는다.
- 특정 레퍼런스 사이트의 레이아웃, 브랜드 표현, 컴포넌트를 복제하지 않는다.
- 검정·금색 중심의 전통 경매 스타일, 대형 장식 문구, 과한 패럴랙스·전환 애니메이션은 도입하지 않는다.
- 주문 상세, 작품 수정, 리뷰, 문의 화면의 정보 구조는 이번 범위에서 전면 재설계하지 않는다.
- Storybook 도입, 범용 UI 라이브러리 구축, 사용처가 하나뿐인 컴포넌트의 선제 추출은 하지 않는다.
- 낙찰과 주문의 도메인·라우트를 합치지 않는다. 중복 인상을 줄이되 기존 역할과 URL은 유지한다.

### 공통 구현 원칙

- 공통 스타일은 실제로 둘 이상의 대상 화면에서 중복이 확인된 색상, 간격, 타이포그래피, 버튼, 입력, 상태 배지, 작품 카드에 한해 정리한다.
- 기존 웜 화이트를 기본 배경, 차콜을 주요 텍스트와 핵심 행동, 세이지를 선택·상태·보조 강조에 사용한다. 강조색만으로 상태를 전달하지 않는다.
- 작품 이미지 비율, 제목, 작가, 현재가·낙찰가, 경매 상태와 마감 정보를 카드마다 동일한 순서로 표현한다.
- 페이지 제목과 본문, 거래 정보, 보조 메타데이터의 글자 크기·굵기·행간을 제한된 단계로 통일하되, 기존 폰트 로딩이나 브랜드 자산은 불필요하게 교체하지 않는다.
- 모바일 우선으로 문서 흐름을 구성하고, 데스크톱에서만 보조 패널·다단 그리드를 확장한다. 고정 높이와 화면 밖 가로 스크롤을 피한다.
- 로딩·빈 상태·오류·재시도·비활성·제출 중 상태는 기존 동작을 보존하고 새 레이아웃에서도 같은 위치 문맥 안에서 이해되게 한다.

## 단계별 구현 범위

### 1단계 — 최소 디자인 기반과 홈 전면 재구성 파일럿

#### 구현 범위

- `Global.css`와 홈 스타일에서 반복되는 웜 화이트·차콜·세이지, 경계색, 본문·보조 텍스트, 간격, 컨테이너 폭, 모서리, 포커스 링을 최소 토큰으로 정리한다.
- 기존 Header/Footer의 기능과 메뉴는 유지하면서 홈과 이어지는 컨테이너 폭, 여백, 활성·포커스 표현만 정돈한다.
- 홈을 장식 중심 랜딩이 아닌 작품 탐색 시작점으로 재구성한다.
  - 진행 중 경매/주요 작품을 첫 정보로 배치한다.
  - 종료 작품, 작가 탐색, 서비스 안내는 명확한 후순위 섹션으로 둔다.
  - 작품 카드에 이미지, 작가·작품명, 현재가 또는 종료 상태, 마감 정보를 일관된 순서로 제공한다.
- 홈에서 실제 중복이 확인된 작품 카드, 섹션 제목, 상태 배지만 공통화한다. 검색·마이페이지에 필요하지 않은 홈 전용 표현은 홈에 남긴다.
- 1단계는 이후 화면에 확장할 공통 토큰, 타이포그래피, 작품 카드, 색상·간격 방향을 확정하는 파일럿 단계로 수행한다.
- 진행 순서는 `1단계 구현 → 자동 테스트 → 사용자 브라우저 QA → 사용자 확인 → 2단계 확장`으로 고정한다.
- 사용자 QA에서 디자인 기준 수정이 필요하다고 판단되면 1단계 범위 안에서 먼저 수정하고 다시 확인한다. 검증되지 않은 기준을 다른 화면으로 먼저 확장하지 않는다.

#### 대표 화면/공통 요소

- `Home`, `Header`, `Footer`
- 페이지 컨테이너, 제목 체계, 작품 카드, 상태 배지, 기본/보조 버튼, 포커스 링

#### 완료 기준

- 데스크톱: 첫 화면에서 진행 중 작품과 핵심 거래 정보가 장식 콘텐츠보다 먼저 보이고, 카드 그리드의 이미지·텍스트 기준선과 최대 폭이 일정하다.
- 모바일: 320~430px 폭에서 가로 스크롤 없이 첫 작품 탐색 진입점이 보이며, 카드 핵심 정보가 잘리거나 장식 요소에 밀리지 않는다.
- 홈의 종료 작품 표시, 작품 상세 이동, 헤더 메뉴와 인증 상태별 동작이 기존과 동일하게 유지된다.

#### 커밋 경계

- `refactor(frontend): 홈 중심 디자인 기반 정리`
- 홈에 실제 적용되는 토큰과 공통 요소까지만 포함하고 다른 페이지의 대규모 스타일 변경은 포함하지 않는다.

### 2단계 — 마이페이지 허브 전면 재구성과 입찰·낙찰 연결 개선

#### 구현 범위

- 마이페이지 첫 화면을 프로필 소개 페이지가 아니라 사용자 업무 허브로 재구성한다.
  - 입찰, 낙찰/주문, 판매 관리, 찜, 리뷰·문의 등 역할별 핵심 진입점을 상단에 배치한다.
  - 프로필과 포인트는 중요한 요약 정보로 유지하되, 모바일에서 업무 메뉴보다 긴 선행 영역이 되지 않게 축약·재배치한다.
  - 사용자 유형에 따른 기존 메뉴 노출 조건과 링크는 그대로 유지한다.
- 입찰 현황의 통계와 작품 목록 위계를 다시 잡는다.
  - 모바일에서는 통계 카드가 목록을 과도하게 밀지 않도록 한 줄 요약 또는 압축 그리드로 바꾼다.
  - 데스크톱에서는 카드/목록의 최대 폭을 제한하고 작품 정보와 입찰 상태를 같은 시선 흐름으로 배치한다.
  - 최고가 유지, 패찰, 낙찰, 종료 등 상태를 텍스트와 배지로 함께 구분한다.
- 낙찰 화면은 경매 결과와 다음 행동을 안내하고, 주문 화면은 결제·배송 처리 상태를 관리하는 화면이라는 역할 차이를 명확히 한다.
  - 기존 `orderId` 연결과 주문 상세 이동 계약은 유지한다.
  - 두 화면을 합치거나 주문 상태 계산을 프론트에서 새로 정의하지 않는다.

#### 대표 화면/공통 요소

- `MyPage`, `BidStatus`, `SuccessfulBid`, `OrderStatus`
- 업무 바로가기, 요약 수치, 거래 상태 배지, 목록 행/카드, 빈 상태와 다음 행동 링크

#### 완료 기준

- 데스크톱: 마이페이지 주요 업무 영역이 한 화면의 안정적인 최대 폭 안에서 읽히고, 입찰 카드가 불필요하게 넓어지지 않는다.
- 모바일: 프로필·포인트 때문에 주요 업무 진입점이 첫 화면 아래로 과도하게 밀리지 않으며, 입찰 목록 첫 항목까지의 스크롤이 기존보다 줄어든다.
- 낙찰 항목에서는 주문 생성 여부와 이동 가능한 다음 행동이 즉시 구분되고, 주문 목록과 주문 상세의 기존 상태·버튼 조건은 변하지 않는다.

#### 커밋 경계

- `refactor(frontend): 마이페이지 업무 허브 재구성`
- `refactor(frontend): 입찰 및 낙찰 연결 UI 개선`
- 허브 재구성과 거래 목록 개선을 분리해 회귀 범위와 브라우저 확인 지점을 명확히 한다.

### 3단계 — 작품 목록·상세의 탐색 및 거래 정보 위계 개선

#### 구현 범위

- 작품 목록과 검색 결과에서 필터·정렬·결과 수·작품 그리드의 우선순위를 정리한다.
- 1단계 작품 카드를 재사용하되 검색에 필요한 상태와 메타데이터만 확장한다.
- 모바일 필터가 결과를 지나치게 밀거나 화면 폭을 침범하지 않도록 기존 접근 가능한 컨트롤을 유지한 채 접힘/배치 방식을 정돈한다.
- 작품 상세는 이미지 감상 영역과 거래 판단 영역을 분명히 한다.
  - 작품 이미지와 기본 설명을 주 콘텐츠로 둔다.
  - 현재가, 최소 입찰 단위, 마감, 상태, 입찰 행동은 하나의 거래 정보 영역으로 묶는다.
  - 작가·작품 세부 정보와 문의·리뷰 등 보조 정보는 후순위로 배치한다.
- 찜, 입찰, 이미지 모달, 로그인 복귀, 종료·취소 상태 등 기존 기능 계약과 이벤트 흐름은 유지한다.

#### 대표 화면/공통 요소

- `ArtListingPage`, `ArtSearch`, `ArtResults`, `AuctionTotal`, `ArtDetail`
- 작품 카드 확장형, 필터/정렬 바, 가격·마감 정보 묶음, 거래 행동 패널

#### 완료 기준

- 데스크톱: 목록에서 이미지와 거래 상태를 빠르게 비교할 수 있고, 상세에서는 이미지와 거래 패널이 서로 경쟁하지 않으며 긴 설명이 핵심 행동을 밀어내지 않는다.
- 모바일: 필터·정렬 후 결과로 자연스럽게 이어지고, 상세의 가격·마감·입찰 행동이 확대/축소 없이 읽히며 버튼이 화면 밖으로 넘치지 않는다.
- 진행 중·예정·종료·취소 작품과 로그인 여부에 따른 기존 버튼·안내 조건이 모두 유지된다.

#### 커밋 경계

- `refactor(frontend): 작품 탐색 화면 위계 개선`
- `refactor(frontend): 작품 상세 거래 정보 재구성`

### 4단계 — 작품 등록의 긴 흐름 개선

#### 구현 전 판단

- 이번 계획에서는 step wizard를 도입하지 않는다.
- 등록은 단일 제출 계약이고 단계별 서버 저장이 없으므로, wizard는 별도 상태 동기화·단계 이동 검증·새 라우팅 복잡도에 비해 이점이 작다.
- 우선안은 기존 단일 폼과 제출 guard를 유지하면서 다음 구조를 적용하는 것이다.
  - 의미 단위 섹션: 이미지, 작품 기본 정보, 분류·설명, 경매 조건, 최종 확인
  - 데스크톱: 폼 본문과 현재 입력 요약/섹션 내비게이션의 2단 구성
  - 모바일: 동일 DOM 흐름의 단일 열과 짧은 섹션 내비게이션, 명확한 오류 요약 및 제출 영역
- 구현 중 2단 구성이 입력 공간을 지나치게 줄이거나 보조 패널이 중복으로 판명되면, 별도 상태를 추가하지 않는 섹션 내비게이션만 남긴다.

#### 구현 범위

- 필드 순서를 사용자의 입력 판단 순서에 맞게 묶고, 섹션 제목·도움말·필수 표시·오류 위치를 통일한다.
- 이미지 미리보기와 업로드 상태, 경매 시작가·최소 입찰 단위·기간 입력의 관계를 가까이 배치한다.
- 제출 중 중복 방지, 업로드/생성 순서, 검증 메시지, 성공 후 이동 등 기존 계약은 변경하지 않는다.

#### 대표 화면/공통 요소

- `UploadSell`
- 폼 섹션, 입력/선택/텍스트 영역, 도움말·오류, 이미지 업로드 패널, 제출 행동 영역

#### 완료 기준

- 데스크톱: 긴 폼의 현재 위치와 남은 입력 범위를 알 수 있고, 라벨·도움말·오류가 일관된 폭과 간격으로 정렬된다.
- 모바일: 모든 필드가 자연스러운 한 열 순서를 유지하고, 키보드 사용 중 가로 넘침이나 고정 패널 가림이 없으며 오류 필드로 쉽게 돌아갈 수 있다.
- 기존 단일 제출과 중복 제출 차단, 이미지 업로드 실패 복구, 분류·입찰 단위 검증이 유지된다.

#### 커밋 경계

- `refactor(frontend): 작품 등록 폼 흐름 정리`

### 5단계 — 보존 대상 화면에 공통 스타일 적용 및 최종 정합성 정리

#### 구현 범위

- 완성도가 높은 주문 상세는 정보 구조와 행동 순서를 유지하고, 앞 단계에서 확정한 타이포그래피·간격·버튼·상태 배지만 적용한다.
- 작품 수정은 등록 화면과 공유 가능한 필드 스타일만 적용하고, 수정 전용 데이터 로딩·이미지 유지·제출 흐름은 그대로 둔다.
- 리뷰와 문의 화면은 카드·입력·빈 상태·피드백 표현의 시각적 불일치만 정리한다.
- 중복이 실제 확인된 경우에만 폼 필드 래퍼, 상태 배지, 페이지 제목/행동 영역을 공통 컴포넌트로 추출한다.
- 단계별 적용 후 사용하지 않게 된 화면 전용 CSS만 해당 단계 안에서 제거하며, 전역 일괄 정리는 하지 않는다.

#### 대표 화면/공통 요소

- 주문 상세 관련 화면, `EditArt`, `WriteReview`, `MyReview`, `ArtistReview`, `InquiryWrite`, `InquiryList`, `AdminInquiry`
- 공통 버튼·입력·카드·배지·빈 상태 중 실제 공유가 확인된 요소

#### 완료 기준

- 데스크톱·모바일 모두 기존 정보와 행동이 누락되지 않고, 핵심 화면과 보존 화면 사이의 타이포그래피·간격·상태 표현이 같은 서비스로 인식될 수준으로 정돈된다.
- 주문 상세의 현재 높은 정보 밀도와 처리 흐름이 유지되고, 작품 수정은 등록 리디자인 때문에 불필요하게 복잡해지지 않는다.

#### 커밋 경계

- `refactor(frontend): 보존 화면 공통 스타일 정리`
- 공통 컴포넌트 추출이 별도 검토가 필요할 정도로 커지면 `refactor(frontend): 공통 UI 중복 정리`로 분리한다.

## 접근성 유지 기준

- 모든 기능을 키보드만으로 사용할 수 있어야 하며, 기존 논리적 탭 순서와 포커스 복귀를 깨지 않는다.
- 기존 `AccessibleDialog`의 포커스 트랩, Escape 닫기, 초기·복귀 포커스를 유지한다.
- 버튼과 링크의 의미 요소를 시각 편의를 위해 `div` 클릭으로 대체하지 않는다.
- 입력은 연결된 `label`, 필수 여부, 도움말과 오류의 프로그램적 관계를 유지한다. 오류는 색상만으로 표시하지 않는다.
- 상태 배지, 가격 변동, 낙찰·패찰·주문 상태는 텍스트 레이블을 제공하고 색상만으로 구분하지 않는다.
- 이미지에는 기존 대체 텍스트 계약을 유지하고, 장식 이미지만 빈 대체 텍스트를 사용한다.
- 포커스 표시를 제거하지 않으며 웜 화이트·세이지 배경에서도 식별 가능한 대비를 확보한다.
- `prefers-reduced-motion`을 존중하고 새 전환 효과는 정보 이해에 필요한 짧은 수준으로 제한한다.
- 200% 확대와 320px 폭에서 핵심 콘텐츠·행동이 잘리거나 겹치지 않아야 한다.

## 자동 테스트 범위

- 각 단계에서 변경 화면의 기존 Vitest/Testing Library 테스트를 우선 유지·갱신한다. 시각적 클래스명이나 DOM 깊이보다 사용자에게 보이는 역할·이름·상태와 행동을 검증한다.
- 홈: 진행/종료 작품 렌더링, 작품 상세 링크, 로딩·빈 상태·오류 상태를 검증한다.
- 마이페이지/입찰/낙찰/주문: 사용자 유형별 메뉴, 입찰 상태, `orderId` 유무에 따른 다음 행동, 최신 요청 우선 처리와 재시도를 검증한다.
- 작품 목록/상세: 검색 파라미터, 필터·정렬, 진행/종료/취소 상태, 입찰 단위, 찜·입찰·로그인 복귀, 접근 가능한 다이얼로그를 검증한다.
- 작품 등록/수정: 필드 검증, 분류, 입찰 단위, 이미지 업로드, 중복 제출 guard, 성공·실패 흐름을 검증한다.
- 주문 상세·리뷰·문의: 기존 API 호출과 상태별 행동 테스트가 스타일 적용 뒤에도 통과하는지 확인한다.
- 각 커밋 전 변경 대상 테스트를 실행하고, 각 단계 종료 시 전체 프론트 테스트와 프로덕션 빌드를 실행한다.
- CSS 픽셀 값이나 스냅샷만으로 완료를 판정하지 않는다. 반응형 배치와 실제 가독성은 사용자 브라우저 QA로 확인한다.

## 사용자 브라우저 QA 시나리오 및 완료 조건

- 브라우저 QA의 기본 수행자는 사용자이며, 에이전트가 직접 브라우저 QA를 수행하지 않는다.
- 각 단계 구현 후 에이전트는 관련 자동 테스트를 완료하고, 사용자에게 필요한 브라우저 QA 시나리오와 확인 항목을 정리해 전달한다.
- 사용자는 실제 로컬 데이터와 로그인 계정으로 QA를 수행하고 결과를 전달한다. 에이전트는 해당 결과를 기준으로 수정 필요 여부와 다음 단계 진행 여부를 판단한다.
- 사용자가 기능 회귀와 레이아웃 문제 없음에 동의해야 각 단계의 브라우저 QA가 완료된 것으로 본다.
- 특히 1단계는 공통 토큰, 타이포그래피, 작품 카드, 색상·간격 방향을 확정하는 게이트다. 수정이 필요하면 1단계를 보완하고 사용자 재확인을 받은 뒤 2단계로 진행한다.

### 공통 환경

- 데스크톱: 1440px 및 1024px 전후 폭에서 Chrome 기준으로 확인한다.
- 모바일: 390px 전후와 최소 320px 폭에서 확인하고, 세로 스크롤·키보드 입력·다이얼로그 포커스를 함께 확인한다.
- 브라우저 200% 확대 또는 동등한 좁은 뷰에서 가로 스크롤, 콘텐츠 겹침, 행동 버튼 유실 여부를 확인한다.
- 마우스 없이 주요 탐색, 폼 입력, 다이얼로그 열기/닫기, 제출까지 수행한다.

### 단계별 시나리오

1. 비로그인 상태에서 홈의 진행 작품과 종료 작품을 구분하고 작품 목록·상세로 이동한다.
2. 작품 목록에서 검색·필터·정렬을 변경한 뒤 상세로 이동하고, 뒤로 돌아왔을 때 탐색 문맥을 확인한다.
3. 진행 중 작품 상세에서 이미지 확인, 찜, 로그인 복귀, 유효/무효 입찰과 종료·취소 상태 표시를 확인한다.
4. 일반 사용자와 작가 계정으로 마이페이지에 로그인해 첫 화면에서 입찰, 낙찰/주문, 판매 관리 등 허용된 업무로 이동한다.
5. 모바일 마이페이지에서 프로필·포인트 이후 과도한 스크롤 없이 핵심 업무 메뉴를 찾고, 입찰 현황 첫 작품과 상태를 확인한다.
6. 입찰 현황에서 최고가 유지·패찰·낙찰 상태를 구분하고, 낙찰 화면에서 주문 연결 유무에 맞는 다음 행동을 수행한다.
7. 주문 목록에서 주문 상세로 이동해 결제·배송 상태와 기존 조건부 버튼을 확인한다. 주문 상세의 기존 정보 구조가 훼손되지 않았는지 비교한다.
8. 작가 계정으로 작품 등록 폼의 섹션을 순서대로 입력하고, 필수값 오류·이미지 업로드 실패/재시도·중복 클릭 방지·성공 이동을 확인한다.
9. 기존 작품 수정에서 현재 값과 이미지가 유지되는지 확인하고, 수정 성공·실패 흐름을 수행한다.
10. 리뷰 작성/조회와 문의 작성/목록에서 키보드 포커스, 오류·빈 상태·완료 피드백을 확인한다.

## 전체 커밋 순서

1. `refactor(frontend): 홈 중심 디자인 기반 정리`
2. `refactor(frontend): 마이페이지 업무 허브 재구성`
3. `refactor(frontend): 입찰 및 낙찰 연결 UI 개선`
4. `refactor(frontend): 작품 탐색 화면 위계 개선`
5. `refactor(frontend): 작품 상세 거래 정보 재구성`
6. `refactor(frontend): 작품 등록 폼 흐름 정리`
7. `refactor(frontend): 보존 화면 공통 스타일 정리`

각 커밋은 해당 범위의 구현, 관련 자동 테스트, 필요한 화면 전용 CSS 정리까지만 포함한다. 계획 문서 커밋은 사용자 승인 후 구현 커밋과 분리한다.

## 리디자인 이후 BACKLOG로 남길 항목

아래 항목은 이번 리디자인의 완료 조건에 포함하지 않으며, 후속 구현이 확정된 작업이 아니다.

### 이번 리디자인에서 직접 미룬 후속 후보

- 실제 사용자 관찰이나 사용성 테스트를 통한 홈 섹션 순서와 작품 카드 정보량 검증
- 낙찰 화면과 주문 목록의 도메인 경계를 포함한 더 깊은 정보 구조 통합 검토
- 필요성이 확인되고 서버 임시 저장 계약이 필요한 경우에만 작품 등록 step wizard 재검토
- 주문 상세의 별도 전면 개편 및 결제·배송 타임라인 고도화
- 작품 등록·수정 공통화 범위와 작품 수정 전용 정보 구조 재검토
- 리뷰·문의·관리자 문의의 업무 흐름 자체에 대한 전면 UX 개편
- 실사용 이미지의 비율·용량·저화질 대체 처리와 이미지 전달 성능 최적화

### 장기 UI/제품 고도화 후보

- 다크 모드: 리디자인 후 색상 토큰과 공통 UI가 안정된 뒤 도입 여부 검토
- 인터랙션 모션: 탐색·거래 행동에 실제 도움이 되는 범위에서만 고도화를 검토하고 `prefers-reduced-motion` 유지
- 디자인 토큰 패키지화, Storybook 또는 시각 회귀 테스트: 공통 UI 컴포넌트가 충분히 축적된 뒤 관리 효용 재평가
- 개인화 작품 추천: UI 리디자인 후속이 아닌 별도 기능 고도화로 분리하고, 찜·입찰·탐색 이력 등 추천 신호와 범위를 먼저 설계한 뒤 검토
