# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 문제 묶음 3 — 인증 폼의 중복 제출 방지

### 조사 결과

- 대상은 `Login.jsx`, `RegisterUser.jsx`, `RegisterArtist.jsx`의 세 인증 폼이다. 세 화면 모두 API 요청 직전에 `loading`을 `true`로 바꾸고 제출 버튼의 `disabled`와 처리 중 문구를 렌더링하지만, React 상태가 반영되기 전 같은 렌더에서 `submit` 이벤트가 연속으로 들어오면 각 핸들러가 모두 API를 호출할 수 있다.
- 로그인은 아이디·비밀번호 입력과 제출 버튼만 있고, 회원가입 두 화면은 공통 `useDuplicateCheck`를 통해 아이디·닉네임 중복확인을 선행한다. 일반/작가 회원가입의 제출 흐름은 동일하며 작가 화면에만 작가명·홈페이지·SNS 선택 입력과 다른 라벨이 추가된다.
- 현재 처리 중에는 제출 버튼만 비활성화된다. 로그인 입력 두 개, 회원가입의 모든 입력, 회원가입 중복확인 버튼은 계속 조작할 수 있다. 회원가입에서 아이디나 닉네임을 바꾸면 확인 상태까지 무효화되어 화면 상태가 진행 중인 가입 요청과 달라질 수 있다.
- `useDuplicateCheck`는 요청 번호와 최신 입력값을 `ref`로 추적해 늦은 응답을 무시하지만, 가입 제출 자체를 막는 기능은 아니다.
- 기존 `WriteReview.jsx`, `AdminInquiry.jsx`, `Charge.jsx`는 `useRef(false)`를 상태와 함께 사용해 같은 렌더의 연속 실행을 동기적으로 차단한다. 특히 리뷰 작성과 충전 화면은 처리 중 요청 문맥을 바꾸는 컨트롤도 비활성화하며, 관련 테스트가 연속 이벤트에도 API 1회 호출과 컨트롤 잠금을 검증한다. 이 패턴을 인증 화면에 직접 적용할 수 있다.
- 로그인·회원가입 API에는 idempotency key 계약이 없다. 로그인은 읽기/토큰 발급 성격이고, 회원가입은 동일 아이디의 저장 충돌 가능성이 있으나 이번 요구는 한 화면에서의 연속 제출 차단이다. 프론트의 동기 guard로 해결 가능하므로 API 요청 형식, 응답 형식, 백엔드 로직·테스트는 변경하지 않는다.
- 기존 인증 관련 자동 테스트는 `LoginReturn.test.jsx`의 로그인 성공·복귀 경로와 `RegisterDuplicateCheck.test.jsx`의 두 회원가입 중복확인·제출 조건을 다룬다. 중복 제출이나 제출 중 입력 잠금 검증은 없다.
- 조사 시작 시 `git status --porcelain=v1 --untracked-files=all`은 비어 있어 기존 미커밋 변경이 없었다. 관련 기준 테스트 4개(`LoginReturn`, `RegisterDuplicateCheck`, 재사용 패턴의 `WriteReview`, `Charge`)는 26개 모두 통과했다.

### 범위와 결정

- 세 화면은 같은 원인, 같은 `ref` guard, 같은 처리 중 제어 원칙을 적용하고 두 회원가입은 이미 거의 동일한 구조와 매개변수화 테스트를 공유하므로 한 작업으로 묶는다.
- 새 인증 프레임워크, 공통 form 컴포넌트, 전역 mutation 훅은 만들지 않는다. 각 화면에 작은 `useRef(false)` 제출 guard를 두고, 검증을 통과한 뒤 API 호출 직전에 잠근 다음 `finally`에서 해제한다.
- `loading`은 사용자 피드백과 렌더링용으로 유지한다. 실제 단일 실행 보장은 렌더를 기다리지 않는 ref가 담당한다.
- 로그인은 처리 중 아이디·비밀번호와 제출 버튼을 비활성화한다.
- 일반·작가 회원가입은 처리 중 모든 텍스트/비밀번호/전화번호/이메일/URL 입력, 아이디·닉네임 중복확인 버튼, 제출 버튼을 비활성화한다. 비밀번호 확인도 요청 문맥과 화면 일관성에 포함한다. 기존 중복확인 요청 중 상태와 제출 가능 조건은 그대로 결합한다.
- 로그인·회원가입 화면 바깥의 회원가입/로그인/홈/경매 이동 링크는 요청 payload를 바꾸는 폼 action이 아니며 이번 백로그가 이탈 방지까지 요구하지 않으므로 잠그지 않는다.
- 성공 이동, 오류 문구, 중복확인 무효화·최신 응답 판별, API payload와 백엔드 계약은 유지한다.

### 구현 단계

1. 로그인 제출 핸들러에 동기 단일 실행 guard를 추가하고 요청 중 아이디·비밀번호·제출 버튼이 잠기도록 한다. 성공·실패 후 기존 이동과 오류 처리를 유지하면서 guard와 `loading`을 함께 해제한다.
2. 일반·작가 회원가입에 같은 guard를 각각 적용한다. 기존 중복확인 및 비밀번호 일치 검증 뒤에만 guard를 획득하고, 요청 중 모든 입력과 중복확인·제출 action을 잠근다.
3. 기존 인증 테스트 파일을 확장해 같은 렌더의 연속 제출, 처리 중 입력/action 잠금, 실패 후 재시도 가능 여부를 검증한다. 두 회원가입은 현재 매개변수화 구조를 재사용해 같은 계약을 동시에 보장한다.
4. 관련 자동 테스트와 전체 프론트 검증을 실행한 뒤, 브라우저에서 세 화면의 키보드/마우스 제출과 처리 중 UI를 확인한다.

### 자동 테스트 범위

- 로그인: 지연된 로그인 Promise 상태에서 form을 연속 제출해 `login`이 1회만 호출되는지, 아이디·비밀번호·제출 버튼이 비활성화되는지, 실패 후 컨트롤이 다시 활성화되고 한 번 재시도할 수 있는지 확인한다.
- 일반·작가 회원가입: 중복확인을 완료한 유효한 폼을 지연 상태로 두고 연속 제출해 각 register API가 1회만 호출되는지 확인한다.
- 일반·작가 회원가입: 제출 중 필수·선택 입력(작가 전용 입력 포함), 비밀번호 확인, 두 중복확인 버튼, 제출 버튼이 비활성화되는지 확인한다.
- 일반·작가 회원가입: 요청 실패 후 입력과 action이 다시 활성화되고 기존 오류 안내 및 재제출이 동작하는지 확인한다.
- 회귀 검증: `npm run test:component`와 `npm run lint`, `npm run build`를 실행한다. 백엔드 변경이 없으므로 백엔드 테스트 추가·실행은 이 작업의 필수 범위에서 제외한다.

### 브라우저 QA

- 로그인에서 버튼 더블 클릭과 Enter 연타를 각각 시도해 요청이 한 번만 전송되고, 응답 대기 중 두 입력과 제출 버튼이 잠기며 문구가 `로그인 중…`으로 바뀌는지 확인한다.
- 일반·작가 회원가입에서 중복확인을 완료한 뒤 버튼 더블 클릭과 Enter 연타를 시도해 가입 요청이 한 번만 전송되는지 확인한다.
- 두 회원가입에서 처리 중 모든 폼 입력과 중복확인·제출 버튼이 잠기고 `처리 중...` 문구가 표시되는지, 실패 응답 뒤 다시 편집·중복확인·제출할 수 있는지 확인한다.
- 성공 시 기존처럼 `/login`으로 이동하고, 로그인 성공 시 기존 복귀 경로가 유지되는지 확인한다. 폼 밖의 이동 링크는 기존대로 동작하는지 확인한다.
- 데스크톱과 모바일 폭에서 disabled 상태가 기존 레이아웃을 깨지 않는지와 키보드 포커스 이동을 확인한다.

### 커밋 경계

1. 사용자 승인 후 구현 전에 이 계획만 별도 커밋한다: `chore: 인증 폼 제출 차단 계획 추가`
2. 세 인증 화면의 guard·비활성화와 관련 프론트 테스트를 하나의 기능 커밋으로 묶는다: `fix(frontend): 인증 폼 중복 제출 차단`
3. 구현·검증 완료 후 결과를 `PLAN_DONE.md`로 옮기고 이 항목을 `PLAN.md`에서 제거하는 문서 커밋을 별도로 남긴다: `chore: 인증 폼 제출 차단 계획 완료 처리`
