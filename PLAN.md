# DailyAtelier 개발 계획

> [!IMPORTANT]
> 완료된 계획과 검증 결과는 `PLAN_DONE.md`에서 관리한다.
> 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
> 새 작업 계획은 사용자 요청 범위가 확정된 뒤 이 문서에 추가한다.

## 진행 중: 작가 검색 및 최소 작가 상세 페이지

### 확정 범위

- 작가 권한(`users.user_status = 1`) 사용자만 검색 결과에 포함한다.
- 작가명 부분 일치 검색을 제공하며 대소문자를 무시하고 검색어 앞뒤 공백을 제거한다.
- 검색어가 없으면 전체 작가 목록을 반환한다.
- 작가 목록에는 프로필 이미지, 작가명, 소개 요약, 현재 입찰 가능한 진행 작품 수를 표시한다.
- 기존 프로젝트의 Spring Data `Page` 응답과 프론트 페이지네이션 방식을 재사용한다.
- `/artists`에서 `keyword`와 `page`를 URL 쿼리 파라미터로 유지한다.
- `/artists/{artistId}`에 프로필, 소개, 진행 작품 수, 공개 작품 목록을 제공하는 최소 상세 페이지를 추가한다.
- 상단 검색창에 `작품/작가` 검색 유형 선택을 추가한다.
- 작품 검색은 기존 `/search?q=...` 이동을 유지하고, 작가 검색은 `/artists?keyword=...`로 이동한다.
- 목록과 상세에 로딩, 빈 결과, 오류 및 재시도 상태를 제공하고 존재하지 않는 작가는 별도로 처리한다.

### 조사 결과와 재사용 구조

- 사용자 권한은 `User.userStatus`로 관리하며 값 `1`이 작가 권한이다.
- 작가는 `Artist` 엔티티로 분리되어 있고 UUID 문자열인 `artistCode`를 공개 식별자로 사용할 수 있다.
- 기존 `Artist`에는 `artistName`, `artistIntro`가 있고 `Art.artist`로 작품과 연결된다.
- 작품 진행 상태는 `Art.STATUS_ACTIVE = 0`이지만, 실제 입찰 가능 여부를 정확히 세려면 시작·마감 시각 조건도 함께 적용해야 한다.
- 기존 공개 작품 API는 0부터 시작하는 Spring Data `Page` 응답, 기본 12개와 최대 50개 제한을 사용한다.
- 프론트 `/auction/total`의 URL 페이지 동기화, 요청 취소, 로딩 스켈레톤, 빈 결과, 오류·재시도, 페이지 버튼 구조를 재사용할 수 있다.
- 작품 이미지 fallback과 작품 카드 표시 유틸을 상세의 공개 작품 목록에 재사용할 수 있다.
- 공통 API 오류는 `ApiErrorResponseDto`의 `timestamp`, `status`, `code`, `message`, `path` 계약을 따른다.
- 현재 `/api/artists/**`는 작가 역할 전용이므로 공개 조회 GET만 허용하도록 보안 규칙을 구체화해야 한다.
- 현재 헤더 검색은 `/search?q=...`로 이동하고 `/search`는 준비 중 화면이므로 이 이동 계약을 기존 작품 검색 동작으로 유지한다.
- 작가 프로필 이미지 저장 필드는 현재 없으므로 저장 컬럼을 추가하지 않고 기존 기본 이미지(`/img/artist.png`)를 표시한다.

### API 계약 초안

- `GET /api/artists?keyword={keyword}&page=0&size=12`
  - 검색어 정규화 후 작가명 대소문자 무시 부분 일치 검색, 빈 검색어는 전체 조회
  - 작가 ID, 프로필 이미지 경로, 작가명, 소개, 현재 입찰 가능한 작품 수를 `Page`로 반환
- `GET /api/artists/{artistId}`
  - 작가 ID, 프로필 이미지 경로, 작가명, 소개, 현재 입찰 가능한 작품 수 반환
  - 없는 작가는 `404 / ARTIST_NOT_FOUND` 공통 오류 응답
- `GET /api/artists/{artistId}/arts?page=0&size=12`
  - 해당 작가의 공개 작품을 기존 페이지 형식으로 반환
  - 없는 작가는 빈 페이지 대신 `404 / ARTIST_NOT_FOUND` 반환
- 잘못된 요청값은 기존 `400 / INVALID_REQUEST` 공통 오류 계약을 사용한다.

### 확정 세부 정책

- 프로필 이미지 저장 컬럼을 추가하지 않고 기존 기본 이미지(`/img/artist.png`)만 표시한다.
- 진행 작품 수는 `art_status = 0 AND bid_start_time <= 현재 시각 AND closing_time > 현재 시각` 조건으로 집계한다.
- 작가 상세에는 현재 입찰 가능한 작품만 제한하지 않고 기존 공개 정책상 일반 사용자가 조회할 수 있는 해당 작가의 공개 작품 전체를 표시한다.
- 상세 공개 작품에서 삭제·비공개·취소된 작품은 제외하며, 진행 작품 수는 공개 작품 범위와 별개로 실제 입찰 가능 조건에 따라 집계한다.
- 작가 목록은 작가명 오름차순으로 정렬하고 동명이인은 `artistCode` 오름차순으로 정렬한다.
- 상세 작품은 마감 임박순으로 정렬하고 마감 시각이 같으면 `artId` 내림차순으로 정렬한다.
- 소개 요약은 API가 전체 소개를 반환하고 프론트에서 두 줄 말줄임 처리한다.
- 상세 작품 목록은 별도 API로 제공하며 기본 페이지당 12개, 최대 50개로 제한하고 화면 URL에 페이지를 유지한다.

### 실행 계획

- [x] 1. 계약과 데이터 기반 준비
  - 확정된 세부 정책에 따라 프로필 이미지 저장 컬럼 없이 기존 기본 이미지를 사용한다.
  - 작가 검색·상세·진행 작품 수 쿼리와 DTO를 구현하며 작가별 추가 조회가 발생하지 않도록 집계한다.
  - 공백 제거, 빈 검색어, 대소문자 무시, 작가 권한 필터 조건을 검증한다.
- [x] 2. 공개 API와 보안
  - 작가 목록·상세·공개 작품 컨트롤러를 구현하고 조회 서비스와 연결한다.
  - 작가 조회 GET API만 익명 접근을 허용하고 다른 작가 API의 기존 권한은 유지한다.
  - 공통 형식의 404·400 오류를 적용하고 저장소·서비스·컨트롤러·보안 테스트를 추가한다.
- [x] 3. 목록·상세 화면
  - `/artists?keyword=...&page=...` 목록과 `/artists/{artistId}` 상세를 구현한다.
  - 검색 시 페이지를 1로 초기화하고 페이지 이동에도 검색어를 유지한다.
  - 기본 프로필 이미지와 공개 정책상 조회 가능한 작품 전체를 표시하고, 삭제·비공개·취소 작품은 제외한다.
  - 상세 작품 페이지를 URL에 유지하며 로딩, 빈 결과, 오류·재시도, 없는 작가 상태를 처리한다.
- [x] 4. 헤더 통합
  - 헤더 검색창에 `작품/작가` 유형 선택을 추가한다.
  - 작품은 기존 `/search?q=...`, 작가는 `/artists?keyword=...`로 버튼과 Enter 입력을 동일하게 연결한다.
  - 데스크톱·모바일 레이아웃과 검색 상호작용을 검증한다.
- [ ] 5. 종합 검증
  - 백엔드 전체 테스트와 프론트 컴포넌트 테스트, lint, build를 실행한다.
  - URL 직접 진입, 뒤로 가기, 페이지 이동, 재시도와 빈·공백·대소문자 검색 및 없는 작가 상태를 확인한다.

### 예상 변경 파일

- 백엔드: `ArtistRepository`·`ArtRepository`, 신규 작가 컨트롤러·서비스·응답 DTO, `SecurityConfig`, 관련 테스트
- 프론트엔드: 신규 `artistApi`, 작가 목록·상세 페이지와 스타일·테스트, `App.jsx`, 헤더 컴포넌트와 스타일·테스트
- 문서: 구현 중에는 이 `PLAN.md`의 단계별 완료 상태만 갱신한다.

### 커밋 분리안

1. `feat(backend): 작가 검색 및 상세 조회 API 추가`
2. `feat(frontend): 작가 목록과 상세 페이지 추가`
3. `feat(frontend): 헤더에 작품·작가 검색 선택 추가`
4단계까지는 각 기능 커밋에 해당 단계의 `PLAN.md` 완료 체크를 함께 포함한다. 전체 작업 종료 후 별도 문서 정리가 필요할 때만 문서 커밋을 추가한다.

기능 구현은 별도 시작 요청 전까지 대기한다.
