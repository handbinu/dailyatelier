# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

## 화면 전환 및 조회 로딩 UX 공통 개선 — 1차 계획

### 조사 기준과 현재 상태

- 시각적 skeleton이나 레이아웃 개편보다 잘못된 성공·실패 표시, 오래된 응답 반영,
  오류를 정상 빈 상태로 숨기는 동작, 처리 중 중복 action을 우선 조사했다.
- 주문 목록은 `createLatestRequest`, 주문 mutation은 `createOrderRequestGuard`를 이미 사용하고
  있으며 관련 최신 요청·중복 방지 테스트가 있다. 검색, 작가 목록·상세, 작품 관리와 리뷰
  목록도 `AbortController` 또는 요청 키로 조건 전환 응답을 보호한다.
- 반면 아래 화면들은 같은 보호가 없거나 mutation과 후속 조회의 성공 경계를 구분하지 않는다.
- 조사 시작과 종료 시 `git status --short` 기준 미커밋 변경은 없었다. 계획 작성으로 인한
  변경은 이 `PLAN.md`뿐이다.
- 현재 기준 테스트는 프런트 유틸 20개와 컴포넌트 173개가 모두 통과했다.

### 문제 묶음 1 — mutation 성공과 후속 조회 실패의 혼합

대상: 적립금 충전, 관리자 문의 답변

- **판단:** 실제 버그다. 충전 POST 또는 답변 POST가 성공한 뒤 잔액·이력이나 문의 목록
  재조회가 실패하면 동일한 `catch`에서 각각 "충전에 실패", "답변 등록에 실패"로 표시한다.
  사용자가 같은 작업을 다시 실행하도록 유도할 수 있다.
- **추가 위험:** 충전 중 금액 선택을 바꿀 수 있어 완료 화면이 실제 요청 금액이 아니라 변경된
  현재 입력값을 표시할 수 있다. 관리자 답변 중에도 필터·다른 문의 선택·답변 편집이 가능해
  처리 대상과 화면 문맥이 어긋날 수 있다.
- **지금 고칠 가치:** 높다. 금액 충전과 관리자 상태 변경은 재실행 비용과 데이터 혼동이 크고,
  두 화면 모두 작은 범위에서 재현·회귀 검증할 수 있다.
- **공통화 판단:** 화면별 최소 수정이 낫다. 충전은 세 조회의 부분 성공과 완료 스냅샷을 다루고,
  문의는 mutation 응답 자체로 상세를 확정한 뒤 목록만 보조 갱신하므로 공통 추상화의 이득이 작다.
- **예상 변경 범위:** `Charge.jsx`, `AdminInquiry.jsx`, `Charge.test.jsx`, `Inquiry.test.jsx`.
  API·백엔드·전역 상태·공통 로딩 컴포넌트 변경은 필요하지 않다.

### 문제 묶음 2 — 선택·페이지 전환의 늦은 응답 반영

대상: 관리자 문의 상세, 내 문의 상세, 낙찰 작품 페이지

- **판단:** 실제 경쟁 상태다. 문의 A 뒤 B를 선택하거나 낙찰 페이지를 연속 이동했을 때 이전
  요청을 취소하거나 최신 요청인지 확인하지 않아 늦게 끝난 A/이전 페이지가 현재 상세 또는
  목록을 덮을 수 있다. 관리자 문의는 선택된 상세와 답변 입력값까지 함께 바뀐다.
- **지금 고칠 가치:** 높음~중간이다. 빠른 전환이나 느린 네트워크에서만 드러나지만 잘못된
  대상에 답변할 위험이 있는 관리자 화면은 특히 우선도가 높다.
- **공통화 판단:** 새 체계는 만들지 않고 기존 `createLatestRequest`와 API의 `signal` 전달 방식을
  재사용하는 것이 적절하다. 화면별 캐시 구조가 다른 상세 조회는 최소한의 요청 세대 확인을
  병행한다.
- **예상 변경 범위:** `AdminInquiry.jsx`, `InquiryList.jsx`, `SuccessfulBid.jsx`,
  `inquiryApi.js`, 관련 문의 테스트와 낙찰 목록 테스트. 스타일 변경은 없다.

### 문제 묶음 3 — 인증 폼의 state 렌더링 기반 제출 차단

대상: 로그인, 일반 회원가입, 작가 회원가입

- **판단:** 단순 polish가 아니라 중복 mutation 가능성이 있는 실제 결함이다. 버튼의
  `disabled`는 다음 렌더 이후 적용되므로 같은 렌더 안의 연속 submit에는 요청 중 여부를
  동기적으로 막지 못한다. 처리 중에도 계정 입력을 바꿀 수 있어 진행 중 요청과 화면 값이 달라진다.
- **지금 고칠 가치:** 중간이다. 계정 생성 중복 가능성은 막아야 하지만 현재 추천 묶음보다
  성공/실패 오인의 직접성은 낮다.
- **공통화 판단:** 작은 `ref` 단일 실행 guard 패턴을 각 폼에 적용하는 편이 낫다. 범용 mutation
  훅이나 전역 상태로 확대하지 않는다. 구조가 같은 두 회원가입 화면은 동일 규칙을 맞춘다.
- **예상 변경 범위:** 로그인과 회원가입 2종 화면, 로그인 반환·회원가입 컴포넌트 테스트.
  입력 비활성화는 실제 요청 payload에 영향을 주는 컨트롤에 한정한다.

### 문제 묶음 4 — 생성 요청 중 편집·이탈과 중복 제출

대상: 문의 작성, 작품 등록

- **판단:** 실제 결함이다. 두 화면 모두 state 기반 버튼 비활성화만 사용해 동기식 연속 submit을
  완전히 막지 못하며, 요청 중에도 입력·파일 변경과 문의 화면 이탈이 가능하다. 특히 작품 등록은
  Cloudinary 업로드 뒤 생성 API를 호출하므로 중복 실행 비용과 고아 이미지 위험이 더 크다.
- **지금 고칠 가치:** 중간~높음이다. 생성 데이터의 중복과 외부 업로드 비용을 막을 수 있지만,
  현재 추천 묶음과 함께 다루면 테스트·변경 범위가 커진다.
- **공통화 판단:** 화면별 `ref` guard와 관련 필드·이탈 action 비활성화가 적절하다. 업로드와 문의
  payload가 달라 공통 훅을 새로 만들 필요는 없다.
- **예상 변경 범위:** `InquiryWrite.jsx`, `UploadSell.jsx`와 각각의 기존 컴포넌트 테스트.
  Cloudinary 또는 백엔드 API 계약은 변경하지 않는다.

### 문제 묶음 5 — 조회 오류의 정상값 위장과 목록별 재시도 차이

대상: 마이페이지 문의 대기 수, 입찰 현황, 찜 목록, 낙찰 작품

- **판단:** 혼합되어 있다. 마이페이지 문의 수 조회 실패를 `0`으로 저장해 정상적으로 문의가
  없는 것처럼 보이는 것은 실제 표시 버그다. 입찰·찜·낙찰 화면은 오류 자체는 구분하지만 취소
  지원이 없고, 찜의 "더 보기" 실패는 이미 표시한 목록까지 전역 오류 화면으로 대체하는 등
  재시도 단위가 달라지는 부분은 주로 UX 일관성 문제다.
- **지금 고칠 가치:** 중간이다. 잘못된 0 표시는 바로잡을 가치가 있으나, 나머지는 기능 손상보다
  느린 네트워크에서의 복구 품질 문제라 별도 묶음이 적합하다.
- **공통화 판단:** 조회 상태 모델을 전역화하지 않는다. 문의 수는 `unknown/error`를 구분하고,
  각 목록은 기존 데이터 유지 여부와 최초 조회/추가 조회 재시도를 화면별로 최소 수정한다.
- **예상 변경 범위:** `MyPage.jsx`, `BidStatus.jsx`, `Likes.jsx`, `SuccessfulBid.jsx`,
  `userApi.js`의 선택적 `signal` 전달과 관련 컴포넌트 테스트.

### 이번 추천 범위

**문제 묶음 1(적립금 충전 + 관리자 문의 답변)**을 이번에 함께 해결한다.

선정 이유는 mutation의 실제 성공 여부를 잘못 전달해 사용자가 충전이나 답변을 재실행할 수 있는
문제가 가장 치명적이고, 두 화면만 수정해 성공 경계·제출 스냅샷·처리 중 action 차단을 명확히
검증할 수 있기 때문이다. 관리자 문의의 늦은 상세 응답 문제는 같은 파일에 존재하지만 승인 범위를
임의로 넓히지 않고 문제 묶음 2에 남긴다.

### 구현 단계

1. **mutation 결과를 요청 스냅샷으로 확정**
   - 충전 시작 시 금액·결제수단·멱등성 키를 하나의 요청 스냅샷으로 고정하고 동기식 중복 제출을
     차단한다.
   - 답변 시작 시 문의 ID와 정리된 답변을 고정하고 동일 문의의 연속 제출을 차단한다.
   - 처리 중에는 결과 문맥을 바꿀 수 있는 금액 선택, 동의 변경, 문의 필터·선택, 답변 편집을
     비활성화한다.

2. **mutation과 후속 조회의 오류 경계 분리**
   - 충전 POST 성공은 즉시 성공으로 확정하고, 반환된 충전 금액을 완료 화면에 사용한다. 잔액과
     두 이력 재조회는 각각 결과를 반영해 일부 실패가 성공을 뒤집지 않게 하며, 최신 잔액을 확인하지
     못한 경우 이를 완료 화면에서 별도로 안내하고 재조회할 수 있게 한다.
   - 답변 POST 성공 응답으로 선택 상세를 답변 완료 상태로 확정하고 성공 안내를 표시한다. 문의
     목록 재조회 실패는 "답변 등록 실패"가 아니라 "목록 갱신 실패"로 분리하며 답변을 다시
     제출할 수 있는 상태로 되돌리지 않는다.

3. **경계 회귀 테스트 추가**
   - 충전 성공 후 잔액 또는 이력 재조회 실패, 처리 중 금액 변경 시도, 연속 submit에서 POST 1회,
     실제 요청 금액 기준 완료 표시를 검증한다.
   - 답변 성공 후 목록 재조회 실패, 처리 중 다른 문의·필터·답변 변경 차단, 연속 submit에서 POST
     1회, mutation 응답 기반 답변 완료 표시를 검증한다.

4. **범위 내 검증**
   - `npm test`, 관련 Vitest 파일 우선 실행 후 전체 `npm run test:component`를 실행한다.
   - 구현 diff가 위 4개 파일과 불가피한 동일 화면 스타일 파일을 벗어나지 않았는지 확인한다.

### 완료 기준

- mutation 성공 뒤 재조회가 실패해도 충전·답변을 실패했다고 표시하지 않는다.
- 완료 결과는 요청 시작 시 고정한 대상과 값에 대응한다.
- 처리 중 문맥 변경과 같은 렌더 내 중복 제출이 차단된다.
- 재조회 실패는 별도 안내와 재시도 경로를 제공하고 이미 확인된 mutation 성공 상태를 유지한다.
- 기존 테스트와 추가 회귀 테스트가 모두 통과한다.

### 제외 범위

- skeleton·레이아웃 리디자인, 전역 로딩/상태 관리, 범용 mutation 프레임워크
- 문제 묶음 2~5의 다른 화면 구현
- 백엔드 API 계약 변경, `BACKLOG.md` 항목 삭제 또는 완료 처리

---
