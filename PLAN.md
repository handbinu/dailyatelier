# DailyAtelier 주문 기능 구현 계획

## 목표와 범위

- 낙찰 작품마다 주문을 정확히 하나 생성하고 데이터베이스 유니크 제약으로 중복을 차단한다.
- 주문 상태와 허용 전이를 서버에서 일관되게 관리한다.
- 구매자·판매자·낙찰가·배송 정보를 이후 회원·작품 정보 변경과 무관한 스냅샷으로 보존한다.
- 경매 낙찰 확정 트랜잭션에서 `PAYMENT_PENDING` 주문을 멱등하게 자동 생성한다.
- 구매자 주문 조회·관리 API와 판매 작가 주문 관리 API를 제공한다.
- 현재 목업인 주문 조회 화면과 낙찰 작품의 주문 진입 흐름을 실제 API에 연결한다.
- 실제 PG 결제, 실시간 배송 위치 추적, 반품 회수, 차순위 낙찰자 승계와 낙찰 포기 제재는 이번 범위에서 제외하고 `BACKLOG.md`에서 관리한다.

## 선행 조사: 현재 배송지 구현 상태

### 확인된 구현

- `Address` 엔티티와 `AddressRepository`가 존재하며 사용자와 공유 기본키 방식의 일대일 관계다.
- 저장 필드는 `zipCode`, `userAddress1`, `userAddress2`다.
- `GET /api/users/me`가 저장된 주소를 프로필 응답에 포함한다.
- `PUT /api/users/me`가 주소가 없으면 생성하고, 있으면 갱신한다.
- `ProfileEdit.jsx`에 우편번호·기본 주소·상세 주소 입력란이 있고 프로필 수정 API로 값을 전송한다.

### 부족한 부분과 주문 구현 영향

- 배송지는 선택 입력이므로 낙찰 시점에 저장된 주소가 없거나 일부만 입력됐을 수 있다.
- 우편번호 찾기 버튼에는 검색 동작이 연결되지 않았다.
- 주소 필드의 필수 조합, 길이, 공백, 우편번호 형식에 대한 서버 검증이 없다.
- 우편번호가 `Integer`라서 `02535` 같은 값의 앞자리 `0`이 소실된다.
- 주소 조회·저장 전용 자동화 테스트가 없고 프로필 전체 수정 API에 결합돼 있다.
- 현재 주문 조회 화면의 배송 주소는 목업 문자열이며 실제 `Address`와 연결되지 않는다.

## 배송지 처리 선택지

### 옵션 A — 2단계에 실제 배송지 확인·입력 흐름 포함 (권장)

- 낙찰 종료 시 기존 기본 배송지가 완전하면 주문에 즉시 복사하고 `addressConfirmedAt`을 기록한다.
- 기본 배송지가 없거나 불완전하면 주문 자체는 멱등하게 생성하되 배송 스냅샷을 미확정 상태로 둔다.
- 구매자는 `PAYMENT_PENDING` 동안 주문별 배송지를 입력하고 필요하면 다시 확정할 수 있다.
- 배송지가 확정되기 전에는 결제를 진행할 수 없고, `PAID` 이후에는 주문의 배송 스냅샷을 수정하지 않는다.
- 우편번호는 문자열로 정규화하고 수령인·연락처·우편번호·기본 주소·상세 주소를 주문에 저장한다.
- 기존 프로필 주소를 주문에 복사할 뿐 주문과 `Address` 사이에 외래키를 두지 않는다.
- 장점: 더미 데이터 없이 주문·결제·배송의 데이터 정합성을 확보한다.
- 비용: 주문 단계에 배송지 DTO·API·검증·화면 작업이 추가된다.

### 옵션 B — 임시 더미 배송지 사용 후 별도 단계로 분리

- 자동 생성 주문에 사전에 정한 더미 배송지를 저장하고 실제 배송지 기능은 후속 작업으로 분리한다.
- 결제·배송 전에는 반드시 실제 주소로 교체하는 별도 마이그레이션 또는 보정 절차가 필요하다.
- 장점: 주문 도메인과 조회 API를 먼저 빠르게 구현할 수 있다.
- 위험: 더미 값이 스냅샷으로 굳어 잘못 배송되거나 운영 데이터와 섞일 수 있다.

### 계획 기준

- 이번 계획은 **옵션 A**를 기준으로 작성한다.
- 옵션 B를 선택할 경우 2단계의 배송지 입력·확정 API와 UI를 제외하되, 더미 값임을 식별하는 플래그와 결제·배송 차단 조건은 반드시 둔다.

## 확정 정책

### 주문 생성과 유일성

- 주문은 낙찰된 `Art` 하나당 하나만 존재한다.
- 주문 테이블의 `art_id`에 데이터베이스 유니크 제약을 둔다.
- 낙찰 확정과 주문 생성을 같은 작품 단위 트랜잭션에서 처리한다.
- 스케줄러 재시도나 중복 호출 시 기존 `art_id` 주문을 반환하고 새 주문을 만들지 않는다.
- 동시에 생성이 시도되면 유니크 제약을 최종 방어선으로 사용하고 기존 주문을 다시 조회한다.
- 주문 생성 실패 시 낙찰 확정도 함께 롤백해 “낙찰됐지만 주문 없음” 상태를 방지한다.

### 주문 스냅샷

- 관계 확인과 권한 검증을 위한 원본 식별자와 표시·정산을 위한 스냅샷을 분리한다.
- 구매자: 사용자 ID, 이름, 닉네임, 연락처를 주문 시점 값으로 보존한다.
- 판매자: 사용자 ID, 작가명, 이름 또는 닉네임, 연락처를 주문 시점 값으로 보존한다.
- 작품: 작품 ID, 작품명, 대표 이미지 경로를 주문 시점 값으로 보존한다.
- 금액: 낙찰 입찰 ID와 낙찰가를 보존하며 이후 `Art.currentPrice` 변경에 의존하지 않는다.
- 배송: 수령인, 연락처, 우편번호, 기본 주소, 상세 주소와 배송지 확정 시각을 보존한다.
- 스냅샷 필드는 일반 회원·작가 프로필 변경으로 갱신하지 않는다.
- 배송 스냅샷만 구매자의 명시적 요청으로 `PAYMENT_PENDING` 동안 재확정할 수 있고 `PAID` 이후에는 변경하지 않는다.

### 상태와 전이

- 정상 흐름은 `PAYMENT_PENDING → PAID → PREPARING → SHIPPED → DELIVERED → CONFIRMED`다.
- `PAYMENT_PENDING → CANCELED`는 구매자의 낙찰 포기 또는 결제 기한 만료일 때만 허용한다.
- `PAID` 또는 `PREPARING → REFUNDED`는 결제 취소가 실제로 성공한 경우에만 허용한다.
- `SHIPPED` 이후의 취소·환불은 단순 상태 변경으로 처리하지 않고 후속 반품 흐름으로 분리한다.
- `CANCELED`, `REFUNDED`, `CONFIRMED`는 이번 범위의 최종 상태다.
- 모든 전이는 현재 상태, 요청자 역할과 허용 전이를 원자적으로 검사한다.
- 상태 변경 시 `paidAt`, `preparingAt`, `shippedAt`, `deliveredAt`, `confirmedAt`, `canceledAt`, `refundedAt` 중 해당 시각과 사유를 기록한다.
- 판매 작가는 `PAID → PREPARING → SHIPPED`만 직접 변경한다.
- 실제 택배 연동 전까지 `SHIPPED → DELIVERED`는 내부 운영용 전이로 제공하고 구매자 API에는 노출하지 않는다.
- 구매자는 `DELIVERED → CONFIRMED`와 `PAYMENT_PENDING → CANCELED`만 요청할 수 있다.
- `PAID`와 `REFUNDED` 전이는 결제 연동 계층 또는 관리자용 내부 서비스에서만 수행한다.

### 결제 기한과 낙찰 포기

- 결제 기한은 주문 생성 시각부터 **24시간**으로 하고 `paymentDueAt`에 고정 저장한다.
- 결제 기한 만료 판정은 서버 시각이 `paymentDueAt` 이상이고 상태가 `PAYMENT_PENDING`일 때다.
- 만료 스케줄러는 조건부 상태 갱신 또는 잠금 후 재검사로 주문을 `CANCELED` 처리하며 반복 실행해도 결과가 같아야 한다.
- 구매자가 기한 전에 낙찰 포기를 요청해도 동일하게 `CANCELED` 처리하고 취소 사유를 구분해 기록한다.
- 결제 완료와 만료 처리가 경합하면 주문 행 잠금 후 먼저 확정된 전이만 성공하고 후행 요청은 최신 상태를 반환한다.
- 차순위 입찰자 낙찰 승계와 낙찰 포기 제재는 `BACKLOG.md`의 후속 고도화 범위로 관리한다.
- 취소된 주문이 있어도 작품당 주문 하나라는 유니크 정책을 유지한다.

## API 계약

### 구매자

- `GET /api/users/me/orders`
  - 인증 구매자의 주문을 최신 주문순으로 페이지 조회한다.
  - 상태 필터를 선택적으로 받고 전체 건수와 상태별 집계를 제공한다.
- `GET /api/users/me/orders/{orderId}`
  - 구매자 본인의 주문 상세, 상태 이력에 필요한 시각, 결제 기한과 배송 스냅샷을 반환한다.
- `PUT /api/users/me/orders/{orderId}/shipping-address`
  - `PAYMENT_PENDING`인 본인 주문에 한해 배송지를 검증하고 신규 또는 변경 값으로 확정한다.
- `POST /api/users/me/orders/{orderId}/cancel`
  - 결제 전 낙찰 포기를 요청하고 `CANCELED`로 전환한다.
- `POST /api/users/me/orders/{orderId}/confirm`
  - 배송 완료된 본인 주문을 `CONFIRMED`로 전환한다.

### 판매 작가

- `GET /api/artists/me/orders`
  - 인증 작가가 판매자인 주문을 상태·기간 조건으로 페이지 조회한다.
- `GET /api/artists/me/orders/{orderId}`
  - 본인 판매 주문의 구매자·배송·작품·금액 스냅샷과 현재 상태를 반환한다.
- `PATCH /api/artists/me/orders/{orderId}/status`
  - 본인 판매 주문에 대해 `PAID → PREPARING`, `PREPARING → SHIPPED`만 허용한다.
  - `SHIPPED` 처리에는 택배사명과 송장번호를 검증해 배송 스냅샷에 기록한다.

### 내부 처리

- 결제 승인·취소 어댑터가 `PAID`, `REFUNDED` 전이를 호출할 수 있는 서비스 경계를 둔다.
- 결제 기한 만료 작업은 외부 공개 API가 아니라 스케줄러와 내부 서비스로 처리한다.
- 공개 API는 토큰의 사용자 ID와 역할만 신뢰하고 요청 본문에서 구매자·판매자 ID를 받지 않는다.
- 존재하지 않는 주문은 `404`, 타인 주문은 `403`, 허용되지 않는 상태 전이는 `409`, 입력 오류는 `400`으로 구분한다.

## 1. 주문 도메인·스키마와 정책 기반 구축

- [x] `Order` 엔티티와 주문 상태 enum을 추가한다.
- [x] `art_id` 유니크 제약과 구매자·판매자·상태·기한 조회 인덱스를 설계한다.
- [x] 구매자·판매자·작품·낙찰가·배송지 스냅샷 필드를 정의한다.
- [x] 생성·결제·배송·확정·취소·환불 시각과 취소·환불 사유 필드를 정의한다.
- [x] 우편번호를 문자열로 저장하고 주소 필드 길이와 필수 조합을 확정한다.
- [x] 허용 상태 전이를 한 곳에서 검사하는 도메인 정책을 구현한다.
- [x] 주문 조회 잠금과 `art_id` 조회를 위한 저장소 메서드를 정의한다.

### 1단계 검증

- [x] 주문 상태 enum과 허용·금지 전이를 단위 테스트한다.
- [x] 작품당 주문 유니크 제약이 실제 데이터베이스와 H2 테스트에서 동일하게 동작하는지 검증한다.
- [x] 회원·작가·작품·주소 원본 변경 후에도 주문 스냅샷이 바뀌지 않는지 검증한다.
- [x] 우편번호 앞자리 `0`, 주소 길이와 필수 필드 조합을 검증한다.

## 2. 배송지 확인·입력과 멱등 주문 자동 생성

- [x] 기존 `Address`가 완전한지 판정하는 공통 검증 규칙을 추가한다.
- [x] 완전한 기본 배송지는 주문 생성 시 스냅샷으로 복사하고 즉시 확정한다.
- [x] 주소가 없거나 불완전해도 주문은 생성하되 결제 불가 상태로 표시한다.
- [x] 구매자가 `PAYMENT_PENDING` 동안 주문별 배송지를 입력·재확정하는 API를 구현한다.
- [x] 주문 배송지 확정 뒤 프로필 기본 배송지를 함께 갱신할지는 별도 선택 값으로 받아 명시적으로 처리한다.
- [x] 경매 마감 서비스가 낙찰 확정 직후 `PAYMENT_PENDING` 주문을 같은 트랜잭션에서 생성하도록 연결한다.
- [x] `find-or-create`와 `art_id` 유니크 제약으로 재시작 catch-up, 스케줄러 재시도와 중복 호출을 멱등하게 처리한다.
- [x] 주문 생성 시 `paymentDueAt = createdAt + 24시간`을 저장한다.

### 2단계 검증

- [x] 완전한 기본 배송지, 불완전한 배송지, 배송지 없음 각각의 주문 생성 결과를 검증한다.
- [x] 배송지 미확정 주문의 결제 차단, 결제 대기 중 재확정 성공과 결제 후 변경 거절을 검증한다.
- [x] 낙찰 마감 재실행과 동시 실행에서도 주문이 하나만 생성되는지 검증한다.
- [x] 주문 저장 실패 시 낙찰 확정까지 롤백되고 다음 스케줄에서 재처리되는지 검증한다.
- [x] 유찰 작품과 이미 취소된 작품에는 주문이 생성되지 않는지 검증한다.

## 3. 주문 상태 처리와 결제 기한 만료

- [ ] 역할별 상태 변경 서비스와 상태별 처리 시각 기록을 구현한다.
- [ ] 결제 승인·취소를 받을 내부 서비스 인터페이스를 정의한다.
- [ ] 결제 전 낙찰 포기와 결제 후 환불을 서로 다른 사유·상태로 처리한다.
- [ ] 결제 기한이 지난 `PAYMENT_PENDING` 주문 조회와 만료 스케줄러를 구현한다.
- [ ] 서버 시작 시 누락 만료 주문을 catch-up하고 개별 주문 실패가 다음 주문 처리를 막지 않게 한다.
- [ ] 만료·결제·낙찰 포기 경합 시 주문 행 잠금 뒤 최신 상태를 재검사한다.
- [ ] 송장번호와 택배사 정보를 검증하고 발송 상태에 기록한다.

### 3단계 검증

- [ ] 정상 상태 흐름 전체와 각 금지 전이를 검증한다.
- [ ] 결제 기한 직전·정각·직후 경계를 주입 가능한 `Clock`으로 검증한다.
- [ ] 만료 스케줄러 반복 실행과 서버 시작 catch-up의 멱등성을 검증한다.
- [ ] 결제 승인과 만료, 낙찰 포기와 결제가 경합할 때 하나의 최종 결과만 남는지 검증한다.
- [ ] `CANCELED`, `REFUNDED`, `CONFIRMED`에서 추가 전이가 차단되는지 검증한다.

## 4. 구매자·판매 작가 API

- [ ] 구매자 주문 목록·상세·배송지 확정·낙찰 포기·구매 확정 API를 구현한다.
- [ ] 작가 판매 주문 목록·상세·준비·발송 API를 구현한다.
- [ ] 구매자·판매자 소유권과 작가 역할을 토큰 기준으로 검증한다.
- [ ] 목록의 상태 필터, 정렬, 페이지 크기 상한과 범위 초과 빈 페이지 정책을 적용한다.
- [ ] 주문 번호, 결제 기한, 상태별 시각, 스냅샷과 가능한 다음 행동을 응답 DTO로 제공한다.
- [ ] 공통 오류 응답에 주문 없음, 권한 없음, 상태 충돌, 결제 기한 만료와 배송지 미확정 코드를 추가한다.

### 4단계 검증

- [ ] 비인증, 일반 구매자, 타 구매자, 판매 작가와 타 작가의 접근 권한을 구분해 검증한다.
- [ ] 구매자·판매자별 목록 필터와 페이지네이션, 상세 정보 노출 범위를 검증한다.
- [ ] 요청 본문의 사용자 ID 조작 없이 인증 사용자만 기준으로 처리되는지 검증한다.
- [ ] 잘못된 상태 변경, 주소, 송장번호 요청의 HTTP 상태와 오류 코드를 검증한다.

## 5. 프론트 주문 흐름 연동

- [ ] 낙찰 작품 화면에서 생성된 주문 상세 또는 배송지 확인 화면으로 이동할 수 있게 한다.
- [ ] 배송지 미확정 주문에 기존 기본 배송지를 채워 보여주고 수정·확정 흐름을 제공한다.
- [ ] 우편번호 검색을 실제 주소 검색 기능에 연결하거나 직접 입력 시 검증 가능한 대체 흐름을 제공한다.
- [ ] `OrderStatus.jsx`의 `MOCK_ORDERS`와 하드코딩 배송 주소를 제거하고 구매자 주문 API에 연결한다.
- [ ] 전체 주문 상태를 필터·배지·진행 단계와 가능한 행동 버튼에 매핑한다.
- [ ] 결제 기한, 배송지, 송장번호, 취소·환불 사유와 상태별 시각을 주문 상세에 표시한다.
- [ ] 작가 마이페이지에 판매 주문 목록과 준비·발송 처리 화면을 연결한다.
- [ ] 로딩, 빈 목록, 오류·재시도, 인증 만료와 상태 변경 경합 피드백을 구현한다.

### 5단계 검증

- [ ] 구매자 주문 목록·상세·주소 확정·포기·구매 확정 화면을 검증한다.
- [ ] 작가 주문 필터·상세·준비·발송 화면과 권한 오류를 검증한다.
- [ ] 데스크톱·모바일에서 긴 주소, 긴 작품명과 모든 상태 표시를 시각 검증한다.
- [ ] 변경 파일 ESLint, 프론트 빌드와 백엔드 전체 테스트를 실행한다.
- [ ] 실제 MySQL에서 낙찰 마감부터 주문 생성, 주소 확정, 상태 전이와 기한 만료까지 통합 검증한다.
- [ ] 사용자 승인 전에는 커밋하거나 push하지 않는다.

## 구현 예정 파일

- 주문 `entity`, `repository`, `service`, `controller`, 상태 enum과 요청·응답 DTO
- `AuctionCloseService`, `AuctionCloseScheduler` 및 관련 마감·동시성 테스트
- `Address`, `ProfileUpdateDto`, `UserProfileDto`, `UserService`와 주소 검증 테스트
- `frontend/src/api/userApi.js` 또는 주문 전용 API 모듈
- `frontend/src/pages/MyPage/SuccessfulBid.jsx`
- `frontend/src/pages/MyPage/OrderStatus.jsx`와 관련 스타일
- 작가 판매 주문 관리 페이지·라우팅 및 관련 스타일

## 기존 미완료 항목 보존

- [ ] `bid(art_id, bid_price, bid_time, bid_id)`: 최고 입찰 조회 권장 인덱스
- 실제 MySQL 실행 계획을 확인한 뒤 중복 인덱스가 있으면 추가하지 않는다.
