# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 문제 묶음 5 — 조회 상태와 재시도 UX 정리

### 목표와 범위

- 마이페이지 문의 대기 수 조회가 실패했을 때 `0`건으로 표시하지 않고 조회 불가 상태로 구분한다.
- 마이페이지 홈의 입찰 메뉴 배지와 입찰 통계는 조회 중이거나 실패했을 때 `0`건 대신 미확정 상태를 표시한다.
- 입찰 현황 화면의 상태별 통계는 조회 중이거나 실패했을 때 모두 `0`건인 것처럼 표시하지 않는다.
- 찜 목록의 추가 페이지 조회가 실패해도 이미 조회한 목록을 유지하고, 실패한 추가 조회만 다시 시도할 수 있게 한다.
- 문의 현황 목록 조회가 실패하면 화면 안에서 다시 시도할 수 있게 한다.
- 요청 취소 또는 latest-request 보호는 위 동작을 정확히 구현하는 데 꼭 필요한 경우에만 해당 화면에 한정해 적용한다. 공용 요청 구조 개선이나 다른 화면 확장은 범위에 포함하지 않는다.

### 구현 단계

1. 마이페이지 요약 상태 표시
   - 문의 대기 수에 loading/error 상태를 추가하고, 조회 중·실패 중에는 배지를 `-` 등 미확정 값으로 표시한다.
   - 입찰 메뉴 배지와 진행 중·종료 임박 통계도 입찰 조회의 loading/error 상태를 반영해 `0`과 구분한다.
   - 성공한 빈 응답에서만 실제 `0`건을 표시하며, 기존 입찰·낙찰 본문 오류와 재시도 동작은 유지한다.

2. 입찰 현황과 찜 목록 상태 분리
   - 입찰 현황의 상태별 통계는 목록 조회 성공 후에만 수치를 표시하고 loading/error 중에는 미확정 상태를 표시한다.
   - 찜 목록은 최초 조회 오류와 “더 보기” 오류를 분리한다. 추가 조회 실패 시 기존 카드와 현재 페이지 정보를 유지하고 목록 하단에 오류 안내와 같은 페이지 재시도를 제공한다.
   - 추가 조회 재시도 중에는 중복 요청을 막고, 성공하면 기존 목록 뒤에 결과를 한 번만 추가한다.

3. 문의 현황 재시도와 검증
   - 문의 현황 목록 오류 영역에 재시도 동작을 추가하고 재시도 중 loading, 성공, 재실패 상태를 기존 화면 구조 안에서 처리한다.
   - 다섯 문제의 자동 테스트와 브라우저 QA를 수행하고, 범위 밖 화면이나 API 계약은 변경하지 않는다.

### 예상 변경 파일

- `frontend/src/pages/MyPage/MyPage.jsx`
- `frontend/src/pages/MyPage/BidStatus.jsx`
- `frontend/src/pages/MyPage/Likes.jsx`
- `frontend/src/pages/MyPage/InquiryList.jsx`
- `frontend/src/test/MyPage.test.jsx`
- 입찰 현황·찜 목록·문의 현황을 검증하는 기존 테스트 파일 또는 범위별 신규 컴포넌트 테스트 파일
- 상태 안내 배치에 기존 스타일만으로 부족한 경우에 한해 해당 화면의 CSS module

### 동작 완료 기준

- 문의 및 입찰 조회가 loading/error인 동안 관련 수치가 정상적인 `0`건처럼 보이지 않는다.
- 성공 응답의 실제 빈 결과는 기존대로 `0`건 또는 빈 상태로 표시된다.
- 입찰 현황 오류 안내와 재시도는 유지되며 상단 통계가 오류 안내와 모순되지 않는다.
- 찜 “더 보기” 실패 후 기존 목록이 사라지지 않고, 재시도는 실패한 다음 페이지를 다시 요청한다.
- 문의 현황 목록 실패 후 새로고침 없이 재시도하여 정상 목록 또는 정상 빈 상태로 복구할 수 있다.
- 취소된 요청이나 늦은 응답 때문에 위 상태가 뒤집히는 경로가 구현 과정에서 실제로 생기는 경우에만 최소한의 요청 보호를 추가한다.

### 필요한 자동 테스트

- 마이페이지 문의 배지: 초기 loading, 조회 실패, 성공한 `0`, 성공한 양수 건수를 구분한다.
- 마이페이지 입찰 배지·통계: loading/error에는 미확정 값, 성공한 빈 목록에는 `0`, 성공한 목록에는 계산된 건수를 표시한다.
- 입찰 현황 통계: loading/error와 성공한 빈 결과를 구분하고 재시도 성공 시 정상 통계로 복구한다.
- 찜 목록: 첫 페이지 오류는 기존 전체 오류 UI를 유지하고, 추가 페이지 오류는 기존 목록을 보존하며 동일 페이지 재시도 성공 시 중복 없이 합친다.
- 문의 현황: 조회 실패 후 재시도 버튼으로 loading을 거쳐 목록 및 빈 상태로 복구하고, 재실패 메시지도 유지한다.
- 관련 프론트 컴포넌트 테스트 전체, ESLint, production build를 실행한다.

### 사용자 브라우저 QA

- 마이페이지에서 문의·입찰 요청을 각각 지연·실패시켜 배지와 통계가 `0`으로 오인되지 않는지 확인한다.
- 입찰 현황에서 loading/error/재시도 성공/정상 빈 목록의 상단 통계와 본문이 서로 일치하는지 확인한다.
- 찜 목록 1페이지를 표시한 상태에서 다음 페이지를 실패시켜 기존 카드 유지, 하단 오류, 같은 페이지 재시도와 중복 없는 추가를 확인한다.
- 문의 현황을 실패시킨 뒤 화면 내 재시도로 목록과 정상 빈 상태가 복구되는지 확인한다.
- 데스크톱과 390px 모바일 폭에서 새 상태 문구와 재시도 버튼이 겹치거나 잘리지 않는지 확인한다.

### 커밋 경계

1. `chore: 문제 묶음 5 계획 추가`
   - 승인된 이 계획 문서만 커밋한다.
2. `fix(frontend): 마이페이지 조회 상태 표시 개선`
   - 다섯 문제의 화면 변경과 자동 테스트를 함께 커밋한다.
3. `chore: 문제 묶음 5 완료 기록`
   - 구현·검증 완료 후 `PLAN_DONE.md`에 결과를 기록하고 이 계획을 `PLAN.md`에서 제거한다.
