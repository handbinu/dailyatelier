# DailyAtelier 개발 계획

> [!IMPORTANT]
> `PLAN.md`는 현재 승인된 작업 계획을 관리한다.
>
> 작업 완료 후 핵심 구현·검증 결과를 `PLAN_DONE.md`에 기록한 다음 해당 계획을 `PLAN.md`에서 제거한다.
>
> 현재 범위에 포함되지 않은 미완료·후속 작업은 `BACKLOG.md`에서 관리한다.
>
> 새 계획이나 범위 변경은 사용자 승인 후 반영한다.

---

## 문제 묶음 4 — 생성 요청 중 편집·이탈과 중복 제출

### 조사 결과

- 대상은 `InquiryWrite.jsx`와 `UploadSell.jsx`다. 두 화면 모두 유효성 검증 뒤 `submitting` 상태만 `true`로 바꾸며, React가 disabled 상태를 다시 렌더하기 전 같은 렌더에서 submit 이벤트가 연속으로 들어오면 각 핸들러가 모두 실행될 수 있다.
- 문의 작성은 submit 시점에 문의 유형·제목·내용·이메일 알림·첨부 파일로 요청 객체와 `FormData`를 만든다. 처리 중에도 유형, 제목, 내용, 첨부 선택·제거, 이메일 알림을 바꿀 수 있고 취소 버튼으로 이전 화면에 갈 수 있다. 기존 closure와 이미 만들어진 `FormData` 때문에 전송 중 요청 자체가 뒤늦게 바뀌지는 않지만, 화면과 요청 문맥이 달라지고 실패 후 어떤 값으로 재시도하는지 혼동될 수 있다.
- 작품 등록은 서명 API 호출, 브라우저의 Cloudinary 직접 업로드, 업로드 URL을 포함한 작품 생성 API 호출 순서다. 처리 중에도 이미지 파일, 작품 정보, 형태·카테고리, 가격·증분, 경매 시간을 모두 바꿀 수 있다. 각 submit closure의 값은 유지되지만 연속 submit은 서명과 외부 업로드를 각각 실행하고, 두 업로드가 성공하면 작품 생성 POST도 각각 실행한다.
- 문의 첨부는 백엔드 `InquiryService`가 Cloudinary에 업로드한 뒤 문의를 저장한다. 문의 생성이 중복 호출되면 첨부 업로드와 문의 레코드도 중복될 수 있다.
- 문의·작품 테이블과 생성 API에는 동일 요청을 식별하는 idempotency key나 중복 생성을 막는 unique 계약이 없다. 작품 직접 업로드 뒤 생성 API가 실패할 때의 일반적인 고아 이미지 정리는 별도 백로그이며, 이번에는 같은 화면의 동기식 중복 실행 경로만 막는다.
- 요청 중에도 공통 헤더·마이페이지 이동, 브라우저 뒤로가기·새로고침·탭 닫기 같은 페이지 이탈은 가능하다. 문의 화면에는 별도 취소 버튼도 있고 작품 등록 화면에는 폼 내부 이탈 action이 없다. 현재 앱은 `BrowserRouter`를 사용하고 공통 navigation lock이나 blocker가 없으므로 모든 SPA·브라우저 이탈을 페이지 두 곳에서 완전히 차단하려면 라우터·레이아웃 수준 정책이 필요하다.
- 기존 `WriteReview`, 인증 폼, `AdminInquiry`, `Charge`는 `useRef(false)` 기반 동기 guard와 렌더용 상태를 함께 사용한다. 리뷰 작성은 처리 중 입력과 취소를 잠그고, 충전은 변경 handler에도 ref 확인을 두는 등 이번 두 화면에 재사용 가능한 선례가 있다.
- 관련 자동 테스트는 `Inquiry.test.jsx`가 문의 성공·실패와 입력 유지까지, `UploadSellClassification.test.jsx`와 `UploadSellBidIncrement.test.jsx`가 작품 분류·가격 검증 및 정상 Cloudinary→생성 흐름까지 확인한다. 두 생성 화면의 연속 submit, 처리 중 제어 잠금, 실패 후 재시도 테스트는 없다.
- 조사 시작 시 `git status --short`는 비어 있어 기존 미커밋 변경이 없었다.
- 관련 기준 테스트 3개 파일·23건은 모두 통과했다.

### 실제 버그와 UX 범위 결정

- 같은 렌더의 연속 submit으로 문의 레코드, Cloudinary 업로드와 작품 레코드가 중복될 수 있는 것은 실제 데이터 생성 버그다. 두 화면에 즉시 잠기는 ref guard를 적용하는 것을 핵심 완료 조건으로 삼는다.
- 요청 중 입력·파일·선택·동의 변경과 문의 취소는 이미 시작한 요청과 화면 문맥을 다르게 만들고 실패 후 재시도를 혼동시키므로, 생성 흐름에 직접 연결된 일관성 문제로 보아 함께 잠근다.
- 제출 전의 일반적인 미저장 변경 이탈 경고, 공통 헤더·사이드바 링크, 브라우저 history·새로고침·탭 닫기까지 막는 전역 navigation 정책은 단순 페이지 로컬 수정 범위를 넘는 UX polish다. 이번에는 문의의 명시적 취소 action만 처리 중 비활성화하고, 앱 전체 이탈 차단·확인 dialog·요청 취소는 구현하지 않는다.
- 두 화면은 같은 원인과 해결 패턴을 공유하고 변경 규모가 작으므로 한 작업으로 묶되, 공통 form 시스템이나 전역 mutation 훅은 만들지 않고 각 컴포넌트에 로컬 guard를 둔다.
- API 요청·응답과 백엔드 계약은 바꾸지 않는다. 서버 멱등성, Cloudinary public ID 저장·삭제와 고아 이미지 정리는 별도 정책이 필요한 후속 범위로 유지한다.

### 최소 구현 범위

1. `InquiryWrite.jsx`에 `useRef(false)` 제출 guard를 추가한다. 검증 성공 뒤 API 호출 직전에 guard를 획득하고 `finally`에서 guard와 `submitting`을 해제한다.
2. 문의 처리 중 유형 버튼, 제목·내용, 파일 선택·제거, 이메일 알림, 제출과 취소를 비활성화한다. 변경·파일 handler도 활성 요청 중에는 값을 바꾸지 않도록 ref를 확인해 상태 반영 전 틈까지 닫는다.
3. `UploadSell.jsx`에 같은 guard를 추가해 서명 요청 전에 단일 실행을 확보하고, Cloudinary 업로드와 작품 생성이 한 guard 구간 안에서 한 번만 이어지도록 한다.
4. 작품 처리 중 파일, 모든 입력·textarea·select와 제출을 비활성화한다. 파일·형태·일반 입력 handler도 ref를 확인해 처리 중 값을 바꾸지 않으며, 성공 화면의 상세 보기·추가 등록 action은 요청 완료 뒤에만 나타나는 기존 흐름을 유지한다.
5. 두 화면 모두 실패 시 기존 입력·파일·오류 메시지를 유지하고 guard와 컨트롤을 정상 복구해 한 번 다시 제출할 수 있게 한다. 기존 유효성 검증, 문의 성공 이동과 작품 완료 화면은 유지한다.

### 자동 테스트 범위

- `Inquiry.test.jsx`에 지연된 생성 Promise를 사용해 같은 form의 연속 submit이 `createInquiry` 1회만 호출하는지 검증한다.
- 문의 요청 중 유형·제목·내용·파일 선택 또는 제거·이메일 알림·제출·취소가 잠기고, 실패 후 값이 유지된 채 다시 활성화되어 재제출할 수 있는지 검증한다.
- 작품 등록 제출 전용 컴포넌트 테스트를 추가하거나 기존 `UploadSellBidIncrement.test.jsx`를 확장해 서명 API, Cloudinary `fetch`, `createArt` 각각의 호출 횟수를 단계별 지연 Promise로 확인한다. 같은 렌더의 연속 submit에서도 서명과 업로드가 한 번만 시작되고, 업로드 완료 뒤 작품 생성도 한 번만 실행되어야 한다.
- 작품 요청 중 이미지·텍스트·형태·카테고리·가격·증분·시간·제출이 잠기고, 서명·업로드·생성 단계 실패 후 컨트롤과 guard가 복구되어 재시도할 수 있는지 확인한다.
- 기존 문의 성공 이동·실패 입력 유지, 작품 분류·입찰 증분·정상 완료 화면 테스트를 회귀 검증한다.
- 관련 테스트 통과 후 전체 `npm run test:component`, `npm run lint`, `npm run build`와 `git diff --check`를 실행한다. 백엔드 계약을 변경하지 않으므로 백엔드 테스트 추가는 필수 범위에서 제외한다.

### 브라우저 QA

- 문의 작성과 작품 등록을 모바일 폭과 1280px 데스크톱 폭에서 열어 초기 활성 상태, 유효성 오류와 파일 미리보기 레이아웃을 확인한다.
- 통제된 지연 요청을 사용하는 자동 테스트를 기준으로 연속 submit, 처리 중 입력·파일·선택·동의·action 잠금과 실패 후 재시도를 검증한다. 로컬 브라우저에서 실제 문의·작품 또는 Cloudinary 자산을 중복 생성해 경계 조건을 재현하지 않는다.
- 정상 로컬 QA가 가능한 경우 문의 성공 후 목록 이동과 작품 성공 완료 화면을 각 1회 확인하되, 테스트 계정과 생성 데이터 사용 여부를 기록한다. 불필요한 제품 데이터나 Cloudinary 자산은 만들지 않는다.
- 브라우저 QA 결과에는 직접 확인한 초기·레이아웃·정상 흐름과 자동 테스트로 대체한 동시성·실패 경계를 구분해 기록한다.

### 커밋 경계

1. 사용자 승인 후 구현 전에 이 계획만 별도 커밋한다: `chore: 생성 폼 제출 차단 계획 추가`
2. 문의 작성의 guard·처리 중 잠금과 관련 테스트를 커밋한다: `fix(frontend): 문의 생성 중 중복 제출과 편집 차단`
3. 작품 등록의 Cloudinary→생성 guard·처리 중 잠금과 관련 테스트를 커밋한다: `fix(frontend): 작품 등록 중 중복 업로드와 편집 차단`
4. 구현·검증 완료 후 결과를 `PLAN_DONE.md`로 옮기고 `PLAN.md`와 `BACKLOG.md`를 정리하는 문서 커밋을 별도로 남긴다: `chore: 생성 폼 제출 차단 계획 완료 처리`
