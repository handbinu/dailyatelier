# 커밋 컨벤션

앞으로 커밋 메시지를 작성할 때 아래 규칙을 따른다.

## 기본 구조

- `<type>(<scope>): <subject>`
- `<type>: <subject>`

## Type

- `feat`: 신규 기능 구현 및 화면-API 연동
- `refactor`: 버그 수정이 없는 순수 코드 구조 개선 및 구조 분리
- `chore`: 정적 리소스 추가, 빌드 도구 설정, `.gitignore` 편집 등 코드 동작에 영향을 주지 않는 관리 작업
- `fix`: 버그 수정

## Scope

- 필요한 경우에만 소괄호로 작업 대상 레이어나 위치를 명시한다.
- 예: `frontend`, `backend`, `auth`, `auction`

## Subject

- 첫 줄은 50자 이내로 작성한다.
- 한글로 명확하고 간결하게 완료한 작업 내용을 적는다.

## Body

- 필요하면 본문에 변경 이유를 2~3줄로 추가한다.
- 첫 줄만으로 의미가 충분하면 본문은 생략한다.

## 예시

```text
feat: 작품 상세 페이지 레이아웃 및 찜 기능 추가

mock 데이터 기반 상세 페이지 구현
원본 이미지 모달, 찜 토글 기능 포함
```

## 테스트 계정 (로컬 개발용)

- 로그인 테스트 전 Git에서 제외된 `_local/test-accounts.md`를 읽어 계정 정보를 확인한다.
- 테스트 계정 정보는 로컬 전용이며 `AGENTS.md`에 직접 기록하지 않는다.

## 파일 인코딩 작업 원칙

- 한글이 포함된 `.jsx`, `.js`, `.css`, `.java` 등 소스 파일은 항상 UTF-8로 저장한다.
- PowerShell 저장 시 `Set-Content -Encoding utf8`, 새 터미널에서는 `chcp 65001`을 사용한다.
- 문자열이 깨져 보이면 먼저 UTF-8로 다시 읽어 실제 손상 여부를 확인하고, 필요할 때만 파일을 재작성한다.

## 진행 추적 문서 작성 원칙

- 구현 단계가 3단계 이상인 큰 작업에서만 `PLAN.md`를 사용하고, 승인 후 구현 전에 계획 문서만 별도 커밋한다.
- 구현과 검증이 끝나면 해당 계획을 `PLAN.md`에서 제거하고, `PLAN_DONE.md`에는 이후에도 참고할 핵심 결과·계약·검증 한계만 1~3개 bullet로 요약한다.
- 일반적인 테스트·lint·build·브라우저 QA 통과, 구현·조사·QA 절차, 테스트별 세부 내용, 커밋 해시·목록, 기존 `PLAN.md`의 반복 내용은 기록하지 않는다.
- 기능 변경 커밋과 계획·완료 문서 커밋은 분리한다.

## 작업 보고 원칙

- 작업 보고는 `결론 → 실제 변경 → 검증 결과 → 다음 행동` 순서로 간결하게 정리한다.
- 사용자가 이미 알고 있는 계획이나 실행 과정은 반복하지 않는다.
- 세부 구현·테스트 과정은 판단에 필요한 경우에만 설명한다.
- 문제가 발견되면 `문제 / 영향 / 지금 수정 필요 여부 / 다음 행동`을 명확히 보고한다.

## 작업 범위 및 승인 준수 원칙

- `PLAN.md`나 사용자 요청에 없는 변경은 임의로 수행하지 않는다.
- 계획 밖의 문제를 발견하면 수정하지 말고 `문제 / 영향 / 권장 대응`을 먼저 보고하고 승인을 기다린다.
- 승인된 작업 단계, 범위, 완료 기준, 커밋 경계·메시지는 임의로 변경하지 않는다.
- 승인 전에는 파일 수정, staging, commit, reset, restore, rebase, checkout, push를 수행하지 않는다.
- 파일 읽기, diff 확인, 테스트 실행 등 저장소 상태를 바꾸지 않는 검증은 승인 없이 수행할 수 있다.
- `BACKLOG.md`, `PLAN.md` 등 진행 문서는 사용자 승인 없이 항목을 추가·삭제·초기화하지 않는다.

## API 오류 응답 규칙

- 모든 API 오류 응답은 공통 `ApiErrorResponseDto` 형식을 사용한다.
- 응답 필드는 `timestamp`, `status`, `code`, `message`, `path`로 고정한다.
- 도메인 오류는 의미 있는 고유 오류 코드를 사용한다.
- DTO 검증 실패, 잘못된 JSON, 요청 파라미터 오류는 구조화된 400 응답으로 반환한다.
- 예상하지 못한 서버 오류는 내부 구현 정보나 예외 메시지를 노출하지 않고 공통 500 응답으로 처리한다.
- 기존 API의 HTTP 상태, 오류 코드, 사용자 메시지 의미는 명확한 이유 없이 변경하지 않는다.
- 새 컨트롤러와 서비스에서 `ResponseStatusException`을 직접 사용하지 않고 공통 도메인 예외 계약을 따른다.
