# 커밋 컨벤션

앞으로 커밋 메시지를 작성할 때 아래 규칙을 따른다.

## 기본 구조

- `<type>(<scope>): <subject>`
- `<type>: <subject>`

## Type

- `feat`: 신규 기능 구현 및 화면-API 연동
- `refactor`: 버그 수정이 없는 순수 코드 구조 개선 및 구조 분리
- `chore`: 정적 리소스 추가, 빌드 도구 설정, `.gitignore` 편집 등 코드 동작에 영향을 주지 않는 관리 작업
- `fix`: 버그 수정

## Scope

- 필요한 경우에만 소괄호로 작업 대상 레이어나 위치를 명시한다.
- 예: `frontend`, `backend`, `auth`, `auction`

## Subject

- 첫 줄은 50자 이내로 작성한다.
- 한글로 명확하고 간결하게 완료한 작업 내용을 적는다.

## Body

- 필요하면 본문에 변경 이유를 2~3줄로 추가한다.
- 첫 줄만으로 의미가 충분하면 본문은 생략한다.

## 예시

```text
feat: 작품 상세 페이지 레이아웃 및 찜 기능 추가

mock 데이터 기반 상세 페이지 구현
원본 이미지 모달, 찜 토글 기능 포함
```

## 테스트 계정 (로컬 개발용)

- 로그인 테스트 전 Git에서 제외된 `_local/test-accounts.md`를 읽어 계정 정보를 확인한다.
- 테스트 계정 정보는 로컬 전용이며 `AGENTS.md`에 직접 기록하지 않는다.

## 파일 인코딩 작업 원칙

- 한글이 포함된 `.jsx`, `.js`, `.css`, `.java` 파일은 항상 UTF-8로 저장한다.
- 깨진 문자열이 보이면 부분 패치보다 전체 파일 재작성으로 정리한다.
- PowerShell로 저장할 때는 `Set-Content -Encoding utf8`을 명시한다.
- 새 터미널 세션을 열어 작업을 시작할 때는 먼저 `chcp 65001`을 실행한다.
- 파일이 깨진 것처럼 보이면 바로 재작성하지 말고, 반드시 `UTF-8`을 명시해서 다시 읽어본 뒤 손상 여부를 판단한다.

## 진행 추적 문서 작성 원칙

- 구현 단계가 3단계 이상인 큰 작업에서만 `PLAN.md` 같은 진행 추적 문서를 만든다.
- 진행 추적 문서를 만든 경우 세부 작업 완료 상태를 갱신하고, 작업이 끝나면 관련 커밋에 포함한다.
- 단순 버그 수정이나 소규모 UI 변경처럼 3단계 미만인 작업에는 별도 진행 추적 문서를 만들지 않는다.

## 작업 범위 준수 원칙

- 계획서(`PLAN.md`)나 사용자 요청에 명시되지 않은 파일은 절대 수정하지 않는다.
- 작업 중 계획 범위 밖의 문제(설정 오류, 성능 이슈, 예상과 다른 동작 등)를 발견하면, 그 자리에서 수정하지 않고 반드시 작업을 멈춘 뒤 "이런 문제를 발견했습니다, 수정할까요?"라고 먼저 보고하고 승인을 기다린다.
- `BACKLOG.md`나 다른 문서에 "나중에 처리"로 명시된 항목은 절대 임의로 구현하거나 삭제하지 않는다.
- `PLAN.md`, `BACKLOG.md` 등 진행 상황 문서는 사용자가 명시적으로 요청한 내용만 수정한다. 임의로 초기화하거나 항목을 삭제하지 않는다.

## 승인된 실행 계획 준수 원칙

- 사용자가 승인한 작업 단계, 순서, 완료 기준, 커밋 개수·경계·메시지는 임의로 변경하지 않는다.
- 작업 중 기존 계획보다 더 안전하거나 합리적인 방법을 발견해도 이를 단순한 세부 조정으로 판단하여 바로 실행하지 않는다.
- 승인된 작업을 합치거나 나누거나 생략해야 하는 경우, 저장소 상태를 변경하기 전에 다음 내용을 사용자에게 보고하고 승인을 기다린다.

  * 기존 승인 내용
  * 새로 발견한 문제
  * 기존 계획대로 진행할 때의 위험
  * 권장 변경안과 대안
- 사용자 승인 전에는 파일 수정, staging, commit, reset, restore, rebase, checkout, push를 실행하지 않는다.
- diff 조회, 파일 읽기, 테스트 실행처럼 저장소 상태를 바꾸지 않는 검증만 승인 없이 수행할 수 있다.
