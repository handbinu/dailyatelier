# DailyAtelier 완료 기록

> [!IMPORTANT]
> 완료 기록은 핵심 완료 범위, 주요 설계 결정, 검증 결과, 관련 커밋과 현재도 의미 있는 검증 한계를 요약한다.
> 상세 실행 절차와 현재 BACKLOG의 작업 목록은 반복하지 않는다.

## 작품 조회 및 공개 접근

### 완료 범위

- 진행 작품 목록, 상태와 무관한 작품 상세, 작가 본인의 등록 작품 조회 API를 구현하고 목록·상세·내 작품·홈 신규 작품의 mock을 실제 데이터로 교체했다.
- URL 페이지네이션, 로딩·오류·재시도·빈 상태, 실제 작품 상세 이동과 공용 이미지 fallback을 적용했다.
- 작품 목록과 상세는 공개하고 등록·내 작품·찜 변경은 인증을 유지했다. 만료 토큰의 401과 권한 오류 403을 구분했다.

### 검증 결과

- 목록 상태·정렬·페이지 범위, 상세 404와 본인 여부, 공개·인증 API 정책 및 만료 토큰 처리를 자동 검증했다.
- 백엔드 전체 테스트와 프론트 ESLint·production build를 통과하고 주요 화면을 확인했다.

## 작품 탐색·검색

### 완료 범위

- 작품 형태·카테고리·등록 시각과 검색 상태·정렬 모델을 추가하고 공개 `GET /api/arts/search`를 구현했다.
- `/search`와 세 경매 목록의 검색·필터·정렬·페이지네이션·URL 동기화, 작품 등록 분류 UX와 상세 왕복 URL 복원을 구현했다.

### 주요 설계 결정

- 형태는 `DIGITAL`·`PHYSICAL`, 카테고리는 `ArtCategory` enum을 사용하며 `material`은 재료·매체·기법 설명으로 유지한다.
- `UPCOMING`은 `now < bidStartTime`, `ONGOING`은 `bidStartTime <= now < closingTime`, `ENDED`는 판매·유찰 처리 작품과 시간상 종료한 활성 작품이며 취소 작품은 제외한다.
- 정렬 동률 시 작품 ID 내림차순을 사용하고 API 페이지는 0, 화면 URL 페이지는 1부터 시작한다.
- 전체·디지털·실물 경매 화면의 상태·형태 preset은 URL로 덮어쓸 수 없다.

### 검증 결과

- 백엔드 271건 중 264건 통과, 실패·오류 0건, 환경 의존 7건 제외와 프론트 컴포넌트 42건·유틸리티 15건 통과를 확인했다.
- 실제 빈 MySQL에 Flyway V1→V2→V3를 적용하고 Hibernate schema validation, migration 성공 이력과 재실행 0건을 확인했다.
- 검색 URL·preset·상세 복귀와 390×844 반응형 화면을 브라우저에서 확인했다. QA 데이터 생성 중 발생한 한글 깨짐은 PowerShell 요청 인코딩 문제로 판정했다.

## 작품 수정·삭제

### 완료 범위

- 소유 작가만 진행 중·마감 전 작품을 수정·삭제하도록 하고, 입찰 전에는 가격·기간·설명·이미지, 입찰 후에는 비가격 정보만 수정하도록 제한했다.
- 입찰 없는 작품은 물리 삭제하고 입찰 있는 작품은 이력을 보존한 `CANCELED`로 전환했다. 취소 작품은 공개·입찰·마감 대상에서 제외하되 작가·입찰자 이력에는 남겼다.
- `PATCH`·`DELETE /api/arts/{artId}`를 구현하고 삭제 결과를 `DELETED`·`CANCELED`로 구분했다.

### 주요 설계 결정

- 작품 행을 비관적 쓰기 잠금한 뒤 소유권·상태·마감 시각·입찰 이력을 재검사한다.
- 삭제 작품의 찜 참조를 분리하고 리뷰가 있는 작품의 물리 삭제는 데이터 무결성을 위해 거절한다.
- 서버 시각은 `Clock`으로 주입해 경계 조건을 재현한다.

### 검증 결과

- 권한·입력·수정 범위·삭제·취소 전환과 입찰·마감 경합, 서로 다른 작품의 독립성을 검증했다.

## 작품 수정·삭제 프론트 연동과 상태 표시

### 완료 범위

- 작품 상세 응답에 편집 초기화에 필요한 형태·카테고리를 추가하고 기존 `PATCH`·`DELETE /api/arts/{artId}`를 작가 작품 관리 화면에 연결했다.
- 작품명은 기존 계약대로 읽기 전용으로 유지하고, 입찰 전 전체 허용 필드, 경매 시작 후 최소 입찰 증분, 입찰 후 가격·기간의 잠금 상태를 서버 정책에 맞게 표시했다.
- 이미지 유지·교체, 입력 검증, 저장·삭제 확인, 중복 제출 방지, 성공 후 기존 필터·페이지 복귀와 목록 재조회, 401·403·404·409 오류 및 충돌 후 최신 상세 복구를 구현했다.
- 관리 목록·찜·입찰 현황·상세에서 삭제 tombstone과 `CANCELED`를 일반 종료·유찰·패찰과 구분하고 불가능한 상세·입찰·찜 해제 action을 제거했다.

### 주요 설계 결정

- 작품명 수정, 취소 작품 공개 범위, 삭제 tombstone 직접 정리 API와 취소 시 찜 유지 정책은 기존 도메인 계약을 확대하지 않고 후속 설계 대상으로 남겼다.
- 물리 삭제와 경매 취소는 삭제 응답의 `DELETED`·`CANCELED`를 그대로 사용하고, 화면 상태도 기존 `artStatus`, `artDeleted`, `availabilityMessage`, `bidResult`, `bidResultMessage`만 소비한다.

### 검증 결과

- 관련 백엔드 서비스·트랜잭션·API 계약 테스트 68건, 프론트 컴포넌트 160건·유틸리티 20건과 ESLint·production build를 통과했다.
- 입찰 전·경매 시작 후 무입찰·입찰 있음, 물리 삭제 tombstone, 취소 찜·입찰·상세, 409 최신 상태 복구, 목록 복귀와 390×844 반응형을 포함한 브라우저 QA 7개 시나리오를 모두 통과했다.
- 로컬 `dailyatelier` DB를 빈 스키마로 재생성해 Flyway V1~V7 7건이 모두 성공하고 V7 `users.profile_image_url` 생성, Hibernate schema validation, 백엔드 기동과 작품 목록 API 200 응답을 재확인했다.

### 관련 커밋

- `2421c33 chore: 작품 수정·삭제 연동 계획 추가`
- `a167fec feat(backend): 작품 편집 조회 계약 보완`
- `e5a60e3 feat(frontend): 작품 수정·삭제 흐름 연동`
- `0e790ad fix(frontend): 삭제·취소 작품 상태 표시`

## 홈 신규·종료·Best Art 연동

### 완료 범위

- 홈 신규·종료·Best Art 영역의 mock과 고정 ID를 실제 공개 검색 데이터로 교체했다.
- 종료 작품에 `SOLD`·`UNSOLD` 결과와 `RECENTLY_ENDED` 정렬을 제공하고 취소 작품은 제외했다.
- 종료 작품 탭, 낙찰가·최종가, 이미지 fallback, 로딩·오류·빈 상태와 실제 상세 이동을 구현했다.

### 검증 결과

- 종료 작품 백엔드 테스트 12건, 프론트 단위 19건·컴포넌트 104건과 Best Art 회귀 5건을 통과했다.
- 실제 종료 데이터, 탭, 가격 문구, 숫자 작품 ID 이동과 전체 보기 URL을 브라우저에서 확인했다.

### 관련 커밋

- `e4e8b71 fix(frontend): 홈 Best Art 실제 작품 연결`

## 판매자 최소 입찰 증분

### 완료 범위

- 작품별 최소 입찰 증분을 생성·수정·상세·입찰에 연결하고 기본값 1,000원, 100원~10,000,000원, 100원 배수 정책을 적용했다.
- 경매 시작 전에는 수정할 수 있고 시작 시각부터는 `409 MINIMUM_BID_INCREMENT_LOCKED`로 차단했다.
- 등록 화면의 검증·예시와 상세의 다음 입찰 가능 금액 안내를 구현했다.

### 주요 설계 결정

- 작품 잠금 뒤 최신 현재가와 증분으로 첫 입찰·후속 입찰·동일 사용자 재입찰을 다시 검증한다.
- 로컬 V4 부분 적용 상태는 기존 4개 작품과 ID를 보존한 채 1,000원으로 백필하고 Flyway 이력을 정상화했다.

### 검증 결과

- V1~V4 성공, Hibernate `ddl-auto=validate`, 재실행 migration 0건을 확인했다. 빈 MySQL과 V1→V3 데이터 fixture에서도 행·ID 보존과 V4 적용을 검증했다.
- 백엔드 동시성·포인트 예치·마감·주문 회귀와 프론트 테스트·ESLint·build를 통과했다.
- 판매 등록, 수정 차단, 최소가·비배수·재입찰·stale response와 낙찰 주문 생성을 브라우저에서 확인했다.

### 관련 커밋

- `620e0f4 feat(backend): 최소 입찰 증분 정책 추가`
- `5194f5e feat(frontend): 입찰 증분 설정과 다음 입찰가 안내`

### 전체 검증 한계

- 2,100,000,000원 표시는 확인했지만 INTERNAL 충전 상한으로 마지막 유효 입찰 후 nullable 다음 최소가와 버튼 disabled 상태는 실행하지 않았고 DB도 직접 변경하지 않았다.

## 입찰 MVP

### 완료 범위

- 인증 사용자의 진행 중 경매 입찰, 판매자 본인 입찰 금지, 1원 이상 21억 원 이하 정수 검증과 입찰 API를 구현했다.
- 사용자별 입찰을 작품 단위 최고가·최근 시각·최고가 여부·상태로 집계하고 작품 상세와 마이페이지에 실제 입찰 흐름을 연결했다.

### 주요 설계 결정

- 입찰 이력과 `Art.currentPrice` 변경은 하나의 트랜잭션으로 처리한다.
- 작품 행을 `PESSIMISTIC_WRITE`로 잠근 뒤 상태와 최신 가격을 재검증한다.
- 같은 작품은 직렬화하되 서로 다른 작품은 차단하지 않으며, 인식 가능한 락 실패는 `409 BID_CONFLICT`로 변환한다.

### 검증 결과

- 낮은 동시 입찰이 높은 금액을 덮어쓰지 않는지, 서로 다른 작품의 독립성과 트랜잭션 종료 후 락 해제를 검증했다.
- 백엔드 전체 테스트와 프론트 ESLint·production build 및 주요 화면을 확인했다.

## 경매 마감·낙찰

### 완료 범위

- 입찰 0건은 유찰, 입찰이 있으면 금액·시각·ID 우선순위의 최고 입찰을 낙찰로 확정하고 낙찰 입찰·실제 종료 시각을 저장했다.
- 10초 주기 스케줄러와 서버 시작 catch-up으로 누락 마감을 재처리했다.
- 낙찰 작품, 입찰 결과, 작가 작품 결과와 마이페이지 요약을 실제 API에 연결했다.

### 주요 설계 결정

- 현재가와 최고 입찰가가 다르면 확정을 중단하고 작품 ID와 가격을 로그에 남긴다.
- 작품별 트랜잭션과 비관적 락으로 중복 마감과 입찰·마감 경합을 제어한다.
- 작가 응답에는 낙찰자 ID와 닉네임을 노출하지 않는다.

### 검증 결과

- 실제 MySQL에서 작품 등록, 두 사용자 입찰, 자동 마감, `SOLD`, 낙찰·패찰 결과를 검증했다.
- 1초 락은 대기 후 성공하고 5초 락은 약 3.1초 후 `409 BID_CONFLICT`가 반환됨을 실측했다.
- `winning_bid_id`, `closed_at`, `art_status, closing_time, art_id` 인덱스와 백엔드 68개 테스트, 프론트 ESLint·build 및 데스크톱·모바일 화면을 확인했다.

## 주문 기능

### 완료 범위

- 작품당 주문 하나의 unique 제약과 구매자·판매자·작품·낙찰가·배송지 스냅샷을 도입했다.
- 낙찰 확정과 `PAYMENT_PENDING` 주문 생성을 같은 작품 단위 트랜잭션에서 처리하고 재실행에도 기존 주문을 반환하도록 멱등화했다.
- 구매자·판매자 API와 목록·상세·배송지·포기·구매 확정·준비·발송 화면을 실제 데이터로 연결했다.

### 주요 설계 결정

- 완전한 기본 배송지만 주문에 복사하며 구매자는 `PAYMENT_PENDING` 동안 배송지를 확정·재확정할 수 있다. 배송지 확정 전 결제와 `PAID` 이후 변경은 차단한다.
- 정상 상태 전이는 `PAYMENT_PENDING → PAID → PREPARING → SHIPPED → DELIVERED → CONFIRMED`이며 취소·환불 사유를 별도 기록한다.
- 결제 기한은 주문 생성 후 24시간이며 서버 시작 catch-up과 주기 처리로 만료한다.
- 결제·만료·포기 경합은 주문 행 잠금 후 최신 상태를 재검사한다. 주문 생성 실패 시 낙찰 확정도 롤백하고 다음 마감 주기에서 재처리한다.

### 검증 결과

- 주문 단위·통합 테스트, 낙찰·주문 생성과 결제·만료·포기 경합, 저장 실패 롤백을 검증했다.
- 실제 MySQL에서 낙찰부터 주문 생성, 주소 확정, 상태 전이와 기한 만료를 확인했다.

## 포인트 원장

### 완료 범위

- `PointAccount`, 불변 `PointTransaction`, `PointHold`, `PointCharge`와 콜백 inbox를 도입해 충전·입찰 예치·낙찰 결제·취소·환불을 API와 화면까지 연결했다.
- 신규 잔액 기준은 `PointAccount`이며 `User.reserve`는 이관과 응답 호환을 위한 읽기 전용 필드로 유지했다.
- 외부 PG는 인터페이스·메타데이터 경계만 마련하고 권한이 제한된 `INTERNAL` 데모 충전만 연결했다.

### 주요 설계 결정

- 잔액과 원장은 같은 트랜잭션에서 변경하고 취소·환불은 원거래를 보존하는 반대 거래로 기록한다.
- 사용 가능·예치 잔액을 분리하고 입찰 전액 또는 증액 차액을 예치하며 패찰·취소·기한 만료 시 정확히 한 번 해제한다.
- 포인트 계정 행을 먼저 잠근 뒤 hold·주문·충전을 처리한다. 주문 `PAID`, `COMMIT`, hold 확정은 하나의 트랜잭션이다.
- 멱등성 키, PG 주문번호와 공급자 이벤트 ID unique 제약으로 중복 반영을 방지한다. 콜백은 최대 5회 재처리한다.

### 검증 결과

- H2 전체 206건 중 201건 통과, MySQL 전용 5건 제외를 확인했다. 단계별로 백엔드 196건, 201건 중 198건 통과·3건 제외 기록도 보존한다.
- MySQL 8.0 Flyway V1·V2 적용·재실행 0건과 인덱스·unique·CHECK·외래키를 확인했다.
- `REPEATABLE-READ`, 동일 계정 행 잠금 직렬화, 교착 감지와 부분 변경 롤백을 검증했다.
- 다수 작품·사용자 동시 입찰에서도 초과 예치가 없고 활성 예치와 모든 계정의 원장 합계가 일치했다.
- 주문 `PAID`와 `COMMIT`의 동시 존재, 실패 시 주문·hold·잔액·원장 전체 롤백, 멱등 재호출·중복 콜백의 추가 변경 방지를 확인했다.
- 프론트 단위 테스트 9건, ESLint와 Vite production build를 통과했다.

## 환불 상태 전이와 화면 연동

### 완료 범위

- 환불 승인 시 주문 `REFUNDED`와 환불 요청 `REQUESTED → APPROVED`를 함께 전이하고, 동일 작업은 멱등 성공·반대 작업은 충돌로 처리했다.
- 구매자·판매자 목록·상세에 환불 사유·요청 시각·처리 결과·처리 시각과 포인트 반환 안내를 표시했다.
- 마이페이지 낙찰 요약에 해당 주문을 여는 `주문 확인` 링크를 추가했다.

### 주요 설계 결정

- `V2__normalize_approved_refund_requests.sql`은 기존 `REFUNDED + REQUESTED` 데이터만 `APPROVED`로 보정한다.

### 검증 결과

- 백엔드 236건에서 실패·오류 0건, 환경 의존 7건 제외를 확인했다. 상태 전이·멱등성·반대 작업 충돌·포인트 환불 원장 1회 처리를 포함한다.
- 프론트 유틸리티 12건·컴포넌트 19건, ESLint·build와 구매자·판매자 브라우저 회귀를 통과했다.

### 관련 커밋

- `76e7db9 fix(backend): 환불 요청 완료 상태 전이 추가`
- `2829c0d feat(frontend): 환불 상태 표시 체계 정리`
- `7a1a17a feat(frontend): 주문 상세 환불 처리 결과 표시`

## 주문 UX 및 최신 응답 정책

### 완료 범위

- 구매자·판매자 주문 카드를 모바일 우선 구조로 정리하고 판매 상세의 주문·결제·구매자·배송·환불 정보 위계를 재구성했다.
- 배송지 변경 시 기존·변경 값을 비교하는 접근성 dialog를 추가했다.
- 목록 조회는 이전 요청을 중단하고 세대 값으로 늦은 성공·실패·finally와 unmount 이후 상태 변경을 폐기하도록 통합했다.

### 검증 결과

- 주문 UX 작업에서 유틸리티 15건·컴포넌트 97건, 최신 응답 작업에서 유틸리티 19건·컴포넌트 101건과 ESLint·build를 통과했다.
- 1440px·768px·390px 레이아웃, 배송지 재확정, 판매 상태 전이와 빠른 필터 변경을 브라우저에서 확인했다. 자동화 환경에서 200% 확대는 재현하지 못해 축소 폭과 긴 데이터 테스트로 보완했다.

### 관련 커밋

- `781f48a fix(frontend): 주문 목록 모바일 조작성 개선`
- `28aefa8 fix(frontend): 판매 주문 상세 정보 구조 개선`
- `fc81779 feat(frontend): 배송지 재확정 dialog 추가`
- `69fe2d6 fix(frontend): 주문 목록 최신 응답 정책 적용`

## 로그인 후 원래 위치 복귀

### 완료 범위

- 내부 복귀 경로를 로그인 성공 후 한 번만 소비하고 보호 라우트, 작품 상세, 구매자·판매자 주문의 401 이동에 공통 적용했다.
- 절대 URL, protocol-relative·백슬래시 URL, 인증 화면 순환과 손상된 state는 홈으로 대체했다.

### 검증 결과

- 단위 15건·컴포넌트 78건, ESLint·build를 통과하고 query·hash 복귀와 직접 로그인, 다른 탭 인증 해제 흐름을 브라우저에서 확인했다.

### 관련 커밋

- `30c21d6 fix(auth): 로그인 후 안전한 원래 위치 복귀`
- `df0314e chore: 로그인 복귀 검증 결과 정리`

## 프로필 사진 변경

### 완료 범위

- Flyway V7로 `users.profile_image_url`을 추가하고 인증 principal 기준 JPEG/PNG 5MB 이하 multipart 업로드를 Cloudinary에 연결했다.
- 프론트 파일 검증·미리보기·중복 제출 방지·영속 반영과 이미지 없음·실패 fallback을 구현했다.

### 주요 설계 결정

- 클라이언트의 사용자 ID·이미지 URL·public ID를 권한 근거로 사용하지 않고 `secure_url`만 저장한다.
- 잘못된 파일 400, 미인증 401, 사용자 없음 404, Cloudinary 실패 502의 공통 오류 응답을 사용한다.

### 검증 결과

- 백엔드 331건, 프론트 단위 19건·컴포넌트 136건, ESLint·build를 통과했다.
- 빈 MySQL에서 Flyway V1~V7, 재실행 0건과 Hibernate `ddl-auto=validate` 기동을 확인하고 업로드·교체·재로그인 영속성을 브라우저에서 검증했다.

### 관련 커밋

- `dd29c80 feat(backend): 프로필 이미지 업로드와 저장 API 추가`
- `487c547 feat(frontend): 프로필 사진 선택과 저장 흐름 연결`
- `bebf7b9 fix: 프로필 이미지 회귀와 표시 문제 보완`

## 우편번호 검색과 프로필 수정 동선

### 완료 범위

- Kakao 우편번호 `embed()`를 접근성 dialog에 연결하고 5자리 문자열 우편번호·기본주소·상세주소 흐름을 구현했다.
- 프로필 수정을 전체 화면으로 통합하고 저장 성공 후 재조회, 실패 시 입력값 보존을 적용했다.

### 검증 결과

- 관련 컴포넌트 23건, 전체 컴포넌트 141건, ESLint·build를 통과하고 주소 검색·저장·복귀를 브라우저에서 확인했다.

### 관련 커밋

- `3d3eb35 feat(frontend): 프로필 수정과 우편번호 검색 동선 정리`

## 프로필 기본정보 저장 피드백

### 완료 범위

- 저장 성공 안내, 조회 실패·재시도, 저장 작업 간 상호 차단과 이름 저장 계약을 보완했다.

### 검증 결과

- 프론트 전체 테스트·lint·build와 백엔드 `UserServiceTest`·전체 테스트 및 브라우저 QA를 통과했다.

### 관련 커밋

- `33f597f feat: 프로필 기본정보 저장 피드백 및 계약 보완`

## 작가 검색 및 상세 페이지

### 완료 범위

- 작가명 부분 일치·대소문자 무시 검색과 안정적인 정렬, 공개 작가 목록·상세·진행 작품 페이지를 구현했다.
- 검색어·페이지 URL 보존과 헤더 작품·작가 검색 유형을 연결했다.

### 검증 결과

- 프론트 유틸리티 12건·컴포넌트 32건과 전체 백엔드 테스트, ESLint·build·diff check를 통과하고 검색·정렬·URL·반응형 화면을 브라우저에서 확인했다.

### 관련 커밋

- `720006a feat(backend): 작가 검색 조회 기반 추가`
- `cb97a85 feat(backend): 공개 작가 조회 API 추가`
- `b2e92ff feat(frontend): 작가 목록과 상세 페이지 추가`
- `196ed91 feat(frontend): 헤더 작품·작가 검색 연결`

## 리뷰 기능 실제 연동

### 완료 범위

- Flyway V6으로 리뷰를 주문 기반 계약으로 변경하고 사용자·작품·주문 외래키와 주문당 리뷰 1개 unique 제약을 적용했다.
- 구매자의 `CONFIRMED` 주문 리뷰 작성·수정, 내 리뷰, 작가가 받은 리뷰와 판매 작품별 작성 현황을 API·화면에 연결했다.

### 검증 결과

- 백엔드 전체 테스트와 프론트 컴포넌트 129건·유틸리티 19건, ESLint·build를 통과했다.
- 빈 스키마 V1~V6 migration과 동시 작성 unique, 소유 범위, 리뷰 작성·수정·조회 브라우저 흐름을 확인했다.

## 1:1 문의 실제 연동

### 완료 범위

- 문의 도메인·API·Cloudinary 첨부·권한과 사용자 작성·목록, 관리자 목록·필터·답변 화면을 구현했다.
- 사용자 상태 필터, 실제 미답변 총개수 배지와 관리자 바로가기를 후속 보완했다.

### 검증 결과

- 문의 등록·권한·관리자 답변 백엔드 테스트와 프론트 컴포넌트·ESLint·build를 통과했다.

### 관련 커밋

- `f6f5a83 feat(backend): 1:1 문의 API와 관리자 답변 구현`
- `0e13772 feat(frontend): 1:1 문의 실제 연동`
- `d8d9b2a fix(frontend): 문의 작성 폼 너비와 여백 수정`
- `ea18bcf feat(backend): 사용자 문의 상태 필터 추가`
- `41dd38a feat(frontend): 미답변 문의 배지 실제 연동`
- `f2881ff feat(frontend): 관리자 문의 관리 바로가기 추가`

### 현재도 의미 있는 검증 한계

- 첨부 문의 작성, 관리자 답변, 중복 답변·타인 접근과 일반 사용자의 관리자 화면 차단은 문서상 수동 검증 예정으로 남아 있었다.

## 헤더·인증 폼·포커스 접근성

### 완료 범위

- 헤더 메뉴의 펼침 상태·제어 대상·Escape·포커스 복귀와 배경 차단을 적용했다.
- 인증 폼 label·오류 연결·알림·오류 포커스와 전역 `focus-visible`, 필요한 범위의 reduced-motion을 구현했다.
- 모바일 홈 히어로의 이미지 비율과 위치를 조정해 핵심 문구와 작품 정보가 잘리지 않도록 했다.

### 검증 결과

- 단위 15건·컴포넌트 61건, ESLint·build와 데스크톱·390px 키보드·스크린리더 동작을 검증했다.

### 관련 커밋

- `503d78a fix(frontend): 헤더 메뉴 키보드 접근성 개선`
- `db095f4 fix(frontend): 인증 폼 오류 접근성 보완`
- `6b4fb26 fix(frontend): 포커스 표시와 동작 감소 설정 보완`
- `0c301cd chore: 프론트 접근성 백로그 완료 처리`

## 공통 dialog 접근성

### 완료 범위

- 작품 원본과 리뷰 dialog를 Portal 기반 공통 컴포넌트로 전환하고 최초 포커스, Tab 순환, Escape, opener 복귀와 배경 inert를 적용했다.

### 검증 결과

- Flyway 변경 없이 기존 schema version 4를 유지했고 자동 테스트·ESLint·build 및 데스크톱·390px dialog 동작을 확인했다.

### 관련 커밋

- `48c9f32 feat(frontend): 공통 접근성 dialog 기반 추가`
- `f42854a fix(frontend): 기존 dialog 접근성 계약 적용`
- `e35560d chore: 공통 dialog 접근성 검증 결과 정리`

## 모바일 메뉴 viewport

### 완료 범위

- 900px 이하에서 메뉴 높이를 `calc(100dvh - var(--header-height))`로 설정하고 내부 스크롤을 유지했다.

### 검증 결과

- `Header.test.jsx` 6건과 production build·diff check를 통과하고 모바일 viewport 하단까지 배경이 이어지는지 확인했다.

## 작가 마이페이지 메뉴 구조

### 완료 범위

- 네 작가 전용 라우트를 `ArtistRoute`로 묶고 일반 회원의 자식 화면·API 마운트를 차단했다.
- 일반·작가 메뉴를 별도 navigation으로 분리하고 `aria-current`와 작은 화면 줄바꿈을 적용했다.

### 검증 결과

- 단위 15건·컴포넌트 83건, ESLint·build와 역할별 접근, 1440px·768px·390px·200% 확대를 확인했다.

### 관련 커밋

- `9e85ccc fix(auth): 작가 전용 라우트 접근 계약 추가`
- `763ff69 fix(frontend): 작가 마이페이지 메뉴 구조 개선`

## UI token 정리

### 완료 범위

- 정의되지 않은 `--space-7` 4곳을 기존 spacing token으로 교체하고 전역 token·컴포넌트 구조는 변경하지 않았다.

### 검증 결과

- 관련 Vitest 5개 파일·25건, ESLint·diff check와 네 화면의 데스크톱·모바일 QA를 통과했다.

### 관련 커밋

- `d4a968b chore: spacing 토큰 정리 계획 추가`
- `a693493 fix(frontend): 미정의 spacing 토큰 교체`

## 안내 콘텐츠와 미완성 route 정리

### 완료 범위

- `/qna`를 1:1 문의 안내, `/info`를 이용 안내로 정리하고 미완성 route를 제거했다.
- 푸터의 고객센터·경매 안내와 로그인 후 문의 작성 복귀를 연결했다.

### 검증 결과

- 화면·링크·fallback과 로그인 상태 문의 동선을 브라우저에서 확인하고 ESLint·컴포넌트 테스트·build를 통과했다.

### 관련 커밋

- `9693368 feat(frontend): 안내 콘텐츠와 미완성 route 정리`

### 현재도 의미 있는 검증 한계

- 비로그인 `/qna → 로그인 → 문의 작성` 복귀는 코드로 구현했으나 브라우저에서 확인하지 못했다.

## 실행·테스트·데모 문서와 배포 설정

### 완료 범위

- 비밀값 없는 공통 백엔드 설정, 백엔드·프론트 예제 env, 실행·테스트·배포 체크리스트와 역할별 데모 흐름을 문서화했다.
- `VITE_API_BASE_URL`과 `CORS_ALLOWED_ORIGINS`을 환경변수화했다.

### 주요 설계 결정

- Hibernate `ddl-auto`를 `update`에서 `validate`로 변경하고 Flyway를 스키마 변경 기준으로 확정했다.
- 기존 `daliyatelier.env` 철자는 호환성을 위해 유지했다.
- CORS 빈 목록·wildcard를 거부하고 기본 origin은 `http://localhost:5173`, 프론트 API 기본값은 `http://localhost:8080`으로 유지했다.
- 포인트 결제는 INTERNAL 데모이며 실제 PG가 아니고 배송 추적 API는 없으며 Cloudinary 업로드는 실제 외부 연동임을 명시했다.

### 검증 결과

- 빈 MySQL에서 Flyway V1~V6와 Hibernate validate 기동, API 200, CORS 허용·거부를 확인했다.
- 백엔드 319건 통과·조건부 7건 제외, 프론트 유틸리티 19건·컴포넌트 129건과 lint·build를 통과했다.
- 별도 checkout에서 예제 파일 재현과 비밀값·로컬 계정 비포함을 확인했다.

### 관련 커밋

- `45c7c22 chore: 실행 환경변수와 배포 주소 설정 정리`
- `d65e98b chore: 프로젝트 실행과 역할별 데모 문서 작성`

### 현재도 의미 있는 검증 한계

- 기존 로컬 계정이 유효하지 않아 회원가입·로그인 smoke test와 전체 거래 회귀는 실행하지 않았다.

## 핵심 도메인 품질 감사

### 완료 범위

- 사용자 흐름, 권한, 트랜잭션·동시성, 결제·원장, Flyway, 프론트 상태와 오류 계약을 감사해 `QA-001`~`QA-014`를 확정 결함·잠재 위험, P0~P2로 분류했다.
- 상세 근거는 `QUALITY_AUDIT.md`에 보존했다.

### 발견사항 처리 결과

| ID | 심각도·판정 | 핵심 내용 | 결과 |
| --- | --- | --- | --- |
| QA-001 | 높음·확정 결함 | `SHIPPED → DELIVERED` 호출 경로 부재 | 해결 |
| QA-002 | 중간·확정 결함 | 구매자 요청·판매자 승인 환불 정책 미연결 | 해결 |
| QA-003 | 높음·확정 결함 | 운영 데모 충전 표시·발행 제한 부재 | 해결 |
| QA-004 | 높음·잠재 위험 | 콜백 예외의 부분 커밋·rollback-only 충돌 위험 | 해결 |
| QA-005 | 중간·확정 결함 | 계정·원장 합계 외 의미적 불일치 검사 부족 | 해결 |
| QA-006 | 낮음·잠재 위험 | 동시 최초 콜백 unique 충돌 복구 부재 | 실제 PG 도입 전 검증 필요 |
| QA-007 | 높음·확정 결함 | 기존 Flyway 이력으로 빈 MySQL 생성 불가 | 해결 |
| QA-008 | 높음·잠재 위험 | 기존 활성 거래의 예치·원장 이관 부재 | 운영·공유 DB가 없어 적용 대상 없음 |
| QA-009 | 중간·잠재 위험 | 기존 null `reserve`가 migration을 중단할 위험 | 신규 baseline 직접 생성으로 적용 대상 없음 |
| QA-010 | 중간·확정 결함 | 충전 조회 전·실패 후 0원과 충전 가능 상태 표시 | 해결 |
| QA-011 | 낮음·확정 결함 | 변경 전 회원가입 중복확인 응답 경합 | 해결 |
| QA-012 | 낮음·잠재 위험 | 충전 금액 변경 후 이전 멱등성 키 재사용 | 해결 |
| QA-013 | 중간·확정 결함 | 도메인별 오류 응답 계약 불일치 | 해결 |
| QA-014 | 낮음·잠재 위험 | 음수 페이지가 구조화된 400으로 변환되지 않음 | 해결 |

## 감사 발견사항 후속 수정

### 완료 범위

- QA-004는 업무 변경과 실패 inbox 기록의 트랜잭션을 분리해 실패 롤백과 재처리를 보장했다.
- QA-013은 오류 응답을 `timestamp`, `status`, `code`, `message`, `path`로 통일했다.
- QA-011은 최신 입력과 일치하는 중복확인 응답만 반영하고 미확인 상태 제출을 차단했다.
- QA-014는 잘못된 page·size를 `INVALID_POINT_REQUEST` 400으로 변환했다.

### 검증 결과

- QA-013 시점 백엔드 231건, QA-011·014 시점 235건, 최종 236건에서 실패·오류 0건과 환경 의존 7건 제외를 확인했다.
- 프론트 최종 유틸리티 12건·컴포넌트 19건, ESLint·build와 구매 확정·환불 승인·거절·주문 링크 브라우저 흐름을 확인했다.
- 회원가입 늦은 성공 응답은 브라우저에서 재현하지 못해 응답 순서 역전 컴포넌트 테스트로 검증했다.

### 관련 커밋

- `b6a5399 fix(payment): 콜백 실패 트랜잭션 경계 명확화`
- `33430ea refactor(backend): API 예외 응답 계약 통합`
- `b42ecd7 fix(frontend): 회원가입 중복확인 응답 경합 방지`
- `c6c94e8 fix(backend): 포인트 페이지 요청 검증 추가`

### 현재도 의미 있는 검증 한계

- 실제 PG 처리기와 MySQL의 동시 최초 콜백 수신은 검증하지 않았다.
- 브라우저 장애 주입과 운영 MySQL·부하 환경 검증은 수행하지 않았다.

## Flyway baseline 재구성

### 주요 설계 결정

- 운영·공유 DB가 없는 개발 단계라는 전제에서 기존 개발 DB는 승인 후 재생성 대상으로 확정했다.
- 기존 V0~V6 대신 `V1__create_baseline_schema.sql`을 최종 기준으로 삼고 레거시 이관 DML·조건부 ALTER는 포함하지 않았다.
- 적용된 migration은 수정하지 않고 이후 변경은 V2 이상으로 누적한다.
- QA-008·009는 운영·공유 DB와 활성 거래 이관 대상이 없어 적용 대상 없음으로 판정했다.

### 검증 결과

- 빈 MySQL에서 신규 V1 단독 적용과 14개 테이블, 일반 인덱스 9개, unique 9개, 외래키 26개, CHECK 9개를 확인했다.
- Hibernate `ddl-auto=validate`, 재실행 0건과 업무 데이터 0건을 확인했다.
### 관련 커밋

- `6166197 fix(backend): Flyway 최종 기준 스키마 재구성`

## 현재도 의미 있는 검증 한계

- 실제 PG 승인·취소·서명 검증과 운영 정산은 구현·검증하지 않았다.
- 실제 PG의 동시 최초 콜백 unique 충돌은 검증하지 않았다.
- 운영 환경과 같은 MySQL 설정의 락 타임아웃, 대량 입찰 부하와 실행 계획은 검증하지 않았다.
- 21억 원 경계의 마지막 유효 입찰 이후 UI 상태는 충전 상한 때문에 실행하지 않았다.
- 일부 브라우저 장애 주입 시나리오는 자동 테스트로 대체했다.

## 부록: 포인트 운영 조회 SQL

### 실패 또는 미처리 콜백

```sql
SELECT callback_event_id, provider, provider_event_id, pg_order_id,
       status, attempt_count, last_error, received_at, processed_at
FROM payment_callback_event
WHERE status IN ('RECEIVED', 'FAILED')
ORDER BY received_at;
```

### 최대 재시도 도달 콜백

```sql
SELECT callback_event_id, provider, provider_event_id, attempt_count,
       last_error, received_at
FROM payment_callback_event
WHERE status = 'FAILED' AND attempt_count >= 5
ORDER BY received_at;
```

### 계정과 원장 잔액 불일치

```sql
SELECT account.user_id,
       account.available_balance,
       account.held_balance,
       COALESCE(SUM(tx.available_delta), 0) AS ledger_available_balance,
       COALESCE(SUM(tx.held_delta), 0) AS ledger_held_balance
FROM point_account account
LEFT JOIN point_transaction tx ON tx.user_id = account.user_id
GROUP BY account.user_id, account.available_balance, account.held_balance
HAVING account.available_balance <> COALESCE(SUM(tx.available_delta), 0)
    OR account.held_balance <> COALESCE(SUM(tx.held_delta), 0)
ORDER BY account.user_id;
```

## 모바일 메뉴의 페이지 이동 후 닫힘 처리

### 원인과 구현 내용

- 일반 route 전환에서 `Header`가 재사용되어 로고 클릭이나 브라우저 history 이동 시 열린 모바일 메뉴 상태가 유지되고, body 스크롤 잠금과 배경 `inert`도 남았다.
- 모바일 메뉴가 열린 location key를 상태로 관리해 로고, 내부 링크, 뒤로가기·앞으로가기 등 route 변경 수단과 관계없이 메뉴가 닫히도록 했다.
- 닫힘 effect cleanup으로 body `overflow`와 `main`/`footer`의 `inert`를 복원하고, 기존 Escape의 햄버거 focus 복귀와 데스크톱 breakpoint 동작을 유지했다.

### 자동 테스트 결과

- Header 테스트 12건, 전체 프론트 component test 31개 파일·166건을 통과했다.
- 수정 파일 ESLint와 production build, `git diff --check`를 통과했다. build에서는 기존 500kB 초과 chunk 경고만 확인됐다.

### 브라우저 QA 결과

- 사용자 브라우저 QA에서 로고·공개/인증 메뉴 링크·뒤로가기·앞으로가기 후 메뉴 닫힘과 본문 스크롤 복원을 확인했다.
- 키보드 Escape의 햄버거 focus 복귀와 데스크톱 너비 전환 후 메뉴·검색 동작에 문제가 없음을 확인했다.

### 관련 커밋

- `edcaf06 chore: 모바일 메뉴 닫힘 처리 계획 추가`
- `a3b04de fix(frontend): 페이지 이동 시 모바일 메뉴 닫기`

## 낙찰 후 주문·배송지·결제 연결 개선

### 완료 범위

- 낙찰 작품, 종료된 낙찰 입찰과 작품 상세에 낙찰 구매자 본인의 nullable `orderId`만 제공하고 주문 상태·배송지 상태·가능 작업은 기존 주문 상세 계약을 재사용했다.
- 낙찰 작품 카드·입찰 현황·작품 상세에서 정확한 구매 주문으로 바로 이동하고, 주문 화면이 목록 페이지와 무관하게 `orderId`로 상세를 직접 조회하도록 했다.
- 주문 상세의 기존 상태와 배송지 정보를 기준으로 미확정 주문은 배송지 입력을 열고, 확정된 결제 대기 주문은 결제 흐름을 제공했다. `orderId`가 없는 비정상 낙찰 데이터에는 목록 탐색 대신 명확한 대체 안내를 표시했다.
- 결제 가능한 주문에서 기존 포인트 요약 API로 사용 가능 포인트·전체 예치 포인트·결제 금액을 표시하고, 조회 중 결제 차단과 실패 후 재조회를 구현했다.
- 정상 낙찰 예치가 결제를 담보한다는 점을 제목과 본문으로 명확히 안내하고, 사용 가능 포인트가 결제 금액보다 적다는 이유로 부족 상태나 충전 유도를 만들지 않았다.
- 결제 충돌·예치 무결성 오류는 주문 상세와 포인트를 다시 조회한 뒤 서버 오류를 안내하도록 했다.

### 자동 테스트 결과

- 백엔드 전체 테스트 336건 중 329건 통과, 실패·오류 0건, 환경 의존 7건 제외를 확인했다.
- 프론트 컴포넌트 테스트 32개 파일·173건과 유틸리티 테스트 20건을 모두 통과했다.
- ESLint, production build와 `git diff --check`를 통과했다. build에서는 기존 500kB 초과 chunk 경고만 확인됐다.

### 브라우저 QA 결과

- 낙찰 작품 카드, 입찰 현황과 작품 상세에서 동일한 주문으로 직접 진입하고 배송지 미확정·확정 후 다음 행동이 올바르게 연결되는 것을 확인했다.
- 결제 대기 주문에서 포인트 조회가 정상 동작하고, 포인트 결제 안내에 사용 가능 포인트 147,000P, 전체 예치 포인트 41,000P, 결제 금액 41,000원이 표시되는 것을 확인했다.
- 주문 화면 전체 디자인은 이번 범위를 확대하지 않고 후속 UI 재설계 대상으로 남겼다.

### 관련 커밋

- `0c81c5b chore: 낙찰 후 주문 동선 개선 계획 추가`
- `dea2383 feat(backend): 낙찰 후 구매 주문 연결 계약 보완`
- `5c378ea feat(frontend): 낙찰 후 주문·배송지·결제 동선 개선`
- `4bffcff feat(frontend): 포인트 결제 안내 가시성 개선`
