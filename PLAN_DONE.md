# DailyAtelier 완료된 구현 계획

> [!IMPORTANT]
> 완료 기록은 핵심 구현, 주요 설계 결정, 검증 결과와 후속 범위만 요약한다.
> 과거 계획의 상세 체크리스트, 구현 전 조사, 변경 예정 파일 목록과 단계별 작업 절차 전문은 옮기지 않는다.

## 1:1 문의 실제 연동

### 완료 범위

- 백엔드: `Inquiry` 도메인 확장, 스키마 변경, API 컨트롤러와 서비스 계층 구현, Cloudinary 첨부 파일 연동 및 권한 설정 완료.
- 프론트엔드: `InquiryWrite`, `InquiryList` 화면을 실제 API에 연결하고 가짜 성공 화면과 mock 데이터를 제거. `AdminRoute` 라우트 가드와 관리자 문의 관리 페이지(`/admin/inquiries`) 추가.
- 권한 제어: 일반/작가 사용자는 본인 문의만 조회 가능. 관리자는 전체 문의 조회, 필터링, 답변 등록 가능.

### 구현 커밋

- `f6f5a83 feat(backend): 1:1 문의 API와 관리자 답변 구현`
- `0e13772 feat(frontend): 1:1 문의 실제 연동`

### 자동 검증 결과

- 백엔드: 문의 등록, 권한 접근 거부, 관리자 답변 처리 등 단위/통합 테스트 모두 통과 확인.
- 프론트엔드: 컴포넌트 단위 테스트(`Inquiry.test.jsx`, `AdminRoute.test.jsx`), 전체 ESLint, 프로덕션 빌드 모두 통과 확인.

### 브라우저 QA 시나리오 (수동 검증 예정)

- 일반 사용자(일반/작가)의 첨부파일 포함 문의 작성 및 본인 문의/상세 조회
- 관리자 계정의 전체 목록 조회, 상태 필터 확인, 미답변 문의에 대한 답변 등록 완료
- 중복 답변, 타인 문의 접근 등 비정상 상황에서의 에러 처리 및 UI 안내
- 일반 사용자의 관리자 페이지 접근 시도 차단 및 403 처리 UI 동작 확인

## 주문 상태 변경 후 최신 목록 유지

### 완료 범위

- 공통 `createLatestRequest` 계약이 새 목록 조회 전에 이전 요청을 abort하고 세대 값으로
  늦은 성공·실패·finally를 폐기하며, dispose 이후 실행과 상태 변경을 차단하게 했다.
- 구매자 주문 조회와 작가 판매 주문 관리의 effect, 오류 재시도, mutation 성공과 409
  충돌 후 목록 재조회를 같은 최신 요청 함수로 통합했다.
- 후속 목록 조회는 호출 시점의 최신 필터·페이지를 사용하고, 최신 요청만 목록·오류·
  loading을 변경한다. 최신 요청 실패 시 두 화면 모두 기존 목록을 null로 초기화한다.
- 상세 캐시 즉시 갱신, 상세 강제 재조회, 필터·페이지 초기화와 주문별 mutation 중복 방지
  계약은 유지했다. 주문 API, 백엔드 상태 전이와 전역 interceptor는 변경하지 않았다.

### 구현 커밋

- `69fe2d6 fix(frontend): 주문 목록 최신 응답 정책 적용`

### 자동 검증 결과

- 유틸리티 테스트 19개, 컴포넌트 테스트 101개, 전체 ESLint와 production build가
  통과했고 `git diff --check` 오류가 없었다.
- 이전 필터 요청의 늦은 성공·실패, 최신 요청 실패, abort 오류 미노출, unmount 이후
  상태 차단과 판매자 페이지 요청·mutation 후 재조회 경합을 자동 테스트로 검증했다.

### 브라우저 QA 결과

- 구매자 계정에서 낙찰 주문의 배송지를 확정하고 목록과 상세가 오류 없이 갱신되는 것을
  확인했다. 로컬 QA 접속 주소는 백엔드 CORS 계약과 같은 `http://localhost:5173`을 사용했다.
- 작가 계정에서 판매 주문 필터를 빠르게 전환한 뒤 결제 완료 주문을 배송 준비로 변경했다.
  기존 필터에서는 주문이 제거되고 배송 준비 필터에서 최신 상태로 다시 표시됐으며 브라우저
  콘솔 오류가 없었다.
- `주문·낙찰 목록 URL 상태 보존`과 새로운 주문 진입 동선은 후속 범위로 유지한다.

## 주문 UX 통합 개선

### 완료 범위

- 구매자·판매자 주문 카드를 모바일 우선 정보 순서와 분리된 조작 영역으로 통합하고,
  주문별 상세 토글의 접근 가능한 이름·상태·제어 대상을 연결했다.
- 판매 주문 상세를 현재 상태, 주문·결제, 구매자 연락처, 배송지, 발송 정보와 환불 정보
  section으로 재구성하고 다음 작업·보조 이동·환불 작업의 위계를 구분했다.
- 결제 대기 중 이미 확정된 배송지의 실제 주소 값이 바뀔 때 기존 `AccessibleDialog`로
  기존·변경 값을 비교하고 명시적으로 확인한 경우에만 기존 PUT 흐름을 실행하게 했다.
- 주문 API, 상태 전이, 환불 정책, 배송지 저장 계약과 백엔드는 변경하지 않았다.

### 구현 커밋

- `781f48a fix(frontend): 주문 목록 모바일 조작성 개선`
- `28aefa8 fix(frontend): 판매 주문 상세 정보 구조 개선`
- `fc81779 feat(frontend): 배송지 재확정 dialog 추가`

### 자동 검증 결과

- ESLint, 유틸리티 테스트 15개, 컴포넌트 테스트 97개와 production build가 통과했고
  `git diff --check` 오류가 없었다.
- 목록·상세 접근성, 판매 상세의 정보 section과 긴 데이터, 배송지 최초 확정·변경 없음·
  재확정 취소·확인·오류·중복 제출 시나리오를 컴포넌트 테스트로 검증했다.

### 브라우저 QA 결과

- 구매자 주문에서 기존 화면 흐름으로 배송지를 최초 확정한 뒤 실제 재확정 dialog의
  기존·변경 값, 변경 강조, 최초 취소 포커스, Escape 종료·호출 버튼 포커스 복귀와 변경
  저장을 확인했다. 저장 중 관련 버튼의 disabled 상태도 확인했다.
- 같은 로컬 주문을 포인트 결제하고 작가 계정에서 배송 준비 상태로 전이해 판매 상세의
  주문·결제, 구매자, 긴 배송지, 발송 정보와 390px 한 열 발송 입력 폼을 확인했다.
- 구매자 화면과 판매자 상세는 1440px, 768px, 390px에서 페이지 가로 overflow가 없었고,
  구매자 목록·필터·배송지 폼 버튼은 44px 이상이었다. 브라우저 콘솔 오류는 없었다.
- 자동화 브라우저에서 확대 단축키가 실제 zoom 값을 변경하지 않아 200% 확대는 직접
  재현하지 못했다. 대신 768px·390px 축소 폭과 컴포넌트 테스트의 긴 데이터 회귀로
  레이아웃을 확인했다.

### 후속 범위

- 당시 `주문 상태 변경 후 최신 목록 유지`와 관련 주문 바로가기는 완료 처리하지 않고
  후속 작업으로 유지했다. 최신 목록 작업은 이후 구현해 이 문서 상단에 완료 기록했으며,
  관련 주문 바로가기는 `BACKLOG.md`에 미완료로 유지한다.

## 작품 탐색 기능

### 완료 범위

- 1단계에서 작품 형태·카테고리·등록 시각, 검색 상태·정렬 모델과 동적 조회 기반을 추가했다.
- 2단계에서 비로그인 접근이 가능한 `GET /api/arts/search`와 구조화된 오류 응답 계약을 구현했다.
- 3단계에서 `/search`의 검색·필터·정렬·페이지네이션·URL 동기화와 작품 등록 분류 UX를 구현했다.
- 4단계에서 검색과 세 경매 목록의 조회·카드·로딩·오류·빈 상태·페이지네이션을 통합하고 상세 왕복 URL을 보존했다.

### 주요 구현 계약

- 형태는 `DIGITAL`, `PHYSICAL`, 카테고리는 확정된 `ArtCategory` enum을 사용하고 `material`은 재료·매체·기법 설명으로 유지한다.
- `UPCOMING`은 `now < bidStartTime`, `ONGOING`은 `bidStartTime <= now < closingTime`, `ENDED`는 판매·유찰 처리 작품과 `closingTime <= now`인 활성 작품이며, 취소 작품은 공개 탐색에서 제외한다.
- `ENDING_SOON`, `NEWEST`, `PRICE_ASC`, `PRICE_DESC`는 각 기준 동률 시 작품 ID 내림차순으로 정렬한다. API 페이지는 0부터, 화면 URL 페이지는 1부터 시작한다.
- `/search`는 예정·진행·종료 전체 공개 작품을 탐색한다. `/auction/total`은 `ONGOING`, `/auction/digital`은 `ONGOING + DIGITAL`, `/auction/analog`는 `ONGOING + PHYSICAL`을 preset으로 강제하고 URL 쿼리로 덮어쓰지 못하게 한다.
- 경매 URL은 형태별 허용 카테고리·정렬·페이지만 보존하고, 작품 상세에서 목록으로 돌아갈 때 원래 검색·경매 URL을 복원한다.

### 자동 검증

- 백엔드 전체 271건 중 264건 통과, 실패·오류 0건, 환경 의존 7건 제외를 확인했다. 정렬 동률, 예정·진행·종료 시간 경계, 취소 제외가 포함됐다.
- 프론트 컴포넌트 42건과 유틸리티 15건이 모두 통과했다.
- ESLint, production build, `git diff --check`가 모두 통과했다.
- 실제 MySQL 빈 DB에 Flyway V1→V2→V3를 적용하고 Hibernate schema validation을 통과했다. V1·V2·V3의 `success=1`, V3 필수 컬럼·인덱스, 재실행 시 추가 migration 0건을 확인했다.

### 브라우저 대표 QA

- `/search`에서 상태 필터 변경과 URL 동기화, `/auction/total`의 진행 작품 제한, `/auction/digital`과 `/auction/analog`의 진행 상태·형태 preset을 확인했다.
- 고정 preset과 충돌하는 URL 쿼리가 제거되고 고정 조건이 유지되는 것을 확인했다.
- 경매 목록 → 작품 상세 → 목록 복귀 시 원래 카테고리·정렬 URL이 복원되는 것을 확인했다.
- 모바일 390×844에서 검색·경매 화면의 가로 넘침, 필터 겹침, 카드 깨짐이 없었고 브라우저 콘솔 경고·오류는 0건이었다.
- API로 QA 데이터를 준비하는 과정에서 PowerShell 요청 인코딩으로 한글 작품명이 `?`로 저장됐지만, 제품의 검색·상태·형태·URL 기능 문제가 아니며 QA 데이터 생성 클라이언트의 문자 인코딩 문제였다.

## 작품 조회 흐름

## 1. 백엔드 API 계약

- [x] 백엔드 작품 조회 API 3개를 구현한다.
  - [x] `GET /api/arts`에 진행 중 작품 페이지 조회와 최신 등록순 기본 정렬을 구현한다.
  - [x] `GET /api/arts/{artId}`에 상태와 무관한 작품 상세 조회, 404, `isOwner` 응답을 구현한다.
  - [x] `GET /api/users/me/arts`에 로그인한 작가의 전체 등록 작품 페이지 조회를 구현한다.
  - [x] 페이지 크기 상한과 범위 초과 페이지의 빈 응답을 적용한다.

## 2. 토큰 및 공개 조회 정책

- [x] 작품 조회와 인증 API의 보안 정책을 분리한다.
  - [x] 작품 목록과 상세 GET 요청을 비로그인 사용자에게 공개한다.
  - [x] 작품 등록, 내 작품, 찜 상태·등록·해제는 인증을 유지한다.
  - [x] 만료 토큰의 401 응답에서 저장된 로그인 정보를 알림 없이 제거한다.
  - [x] 공개 작품 요청이 401이면 토큰 없이 한 번 재요청한다.
  - [x] 찜 상태 요청이 401이면 상세 화면을 게스트 상태로 전환한다.
  - [x] 403은 토큰 만료가 아닌 권한 오류로 처리한다.

## 3. 전체 작품 목록 연동

- [x] `/auction/total`을 실제 진행 중 작품 목록으로 교체한다.
  - [x] URL의 `?page=`를 기준으로 1부터 시작하는 페이지 상태를 관리한다.
  - [x] 잘못된 페이지 값은 1페이지로 보정하고 범위 초과 페이지는 빈 목록으로 표시한다.
  - [x] 로딩, 오류, 재시도, 빈 목록 상태를 구현한다.
  - [x] 실제 DB `artId`로 상세 페이지 링크를 연결한다.
  - [x] 작가명, 현재가, 마감 일시와 마감 임박 상태를 표시한다.
  - [x] 이미지 로딩 실패 시 공용 placeholder를 표시한다.

## 4. 작품 상세 연동

- [x] `ArtDetail.jsx`를 실제 상세 API로 교체한다.
  - [x] `mockArts` 의존성을 제거하고 모든 작품 상태의 상세 정보를 표시한다.
  - [x] 로딩, 일반 오류, 재시도 상태를 구현한다.
  - [x] 404 전용 화면과 이전 목록 페이지로 돌아가기 동작을 구현한다.
  - [x] 작품 상태, 현재가, 마감 일시와 마감 임박 상태를 표시한다.
  - [x] 상세 이미지와 원본 이미지 모달에 fallback을 적용한다.
  - [x] 등록자 본인이면 찜 버튼을 숨긴다.
  - [x] 로그인한 타인의 찜 조회·등록·해제와 게스트 동작을 구현한다.

## 4-1. 작품 조회 UI 개선

- [x] 데스크톱 작품 목록과 상세 상태 UI의 가독성을 개선한다.
  - [x] 데스크톱 작품 목록을 4열에서 3열로 변경해 카드 크기를 확대한다.
  - [x] 마감 임박 배지의 글자와 크기, 색상 대비를 강화한다.
  - [x] 상세 로딩·404·일반 오류의 임시 `□` 문자를 의미 있는 상태별 아이콘으로 교체한다.
  - [x] 데스크톱 화면에서 카드와 상태 아이콘을 시각 검증한다.

## 5. 내 작품 관리 연동

- [x] `ManageArts.jsx`를 실제 내 작품 API로 교체한다.
  - [x] `MOCK_MY_ARTS`와 목업 안내를 제거한다.
  - [x] 전체 목록과 동일하게 URL `?page=` 기반 페이지네이션을 구현한다.
  - [x] 범위 초과 페이지를 빈 목록 화면으로 표시한다.
  - [x] 로딩, 오류, 재시도, 등록 작품 없음 상태를 구현한다.
  - [x] 작품 상태, 가격, 마감 일시를 표시하고 실제 상세 페이지로 연결한다.
  - [x] 이미지 로딩 실패 시 공용 placeholder를 표시한다.

## 6. 홈 신규 작품 연동

- [x] 홈 화면의 신규 작품 영역을 실제 API 데이터로 교체한다.
  - [x] 최신 진행 중 작품 6개를 조회한다.
  - [x] 로딩, 오류, 빈 상태를 구현한다.
  - [x] 실제 `artId`로 상세 페이지를 연결한다.
  - [x] 현재가, 마감 일시와 마감 임박 상태를 표시한다.
  - [x] 이미지 로딩 실패 시 공용 placeholder를 표시한다.
  - [x] 종료 작품 탭과 베스트 작품 영역은 기존 상태를 보존한다.

## 7. 공용 이미지 fallback

- [x] 작품 이미지 공용 fallback을 구현한다.
  - [x] `frontend/public/img/art-placeholder.svg`를 추가한다.
  - [x] 전체 목록, 상세, 내 작품, 홈 신규 작품에서 공용으로 사용한다.
  - [x] 이미지 오류가 반복되지 않도록 한 번만 placeholder로 교체한다.
  - [x] placeholder에서도 작품명 대체 텍스트를 유지한다.

## 8. 검증 및 변경 보존

- [x] 전체 구현을 검증하고 기존 사용자 변경을 보존한다.
  - [x] 목록 상태 필터, 정렬, 페이지 크기와 범위 초과 응답을 테스트한다.
  - [x] 모든 작품 상태의 상세 조회와 존재하지 않는 작품 404를 테스트한다.
  - [x] 본인·타인·비로그인 사용자의 `isOwner` 응답을 테스트한다.
  - [x] 내 작품 소유자 필터와 페이지네이션을 테스트한다.
  - [x] 공개 API와 인증 API 보안 정책 및 만료 토큰 처리를 테스트한다.
  - [x] 백엔드 전체 테스트를 실행한다.
  - [x] 변경된 프론트 파일 ESLint와 프론트 빌드를 실행한다.
  - [x] Git diff에서 기존 `AGENTS.md` 변경이 보존되었는지 확인한다.
  - [x] 사용자 확인 전에는 커밋하거나 push하지 않는다.

---

## 입찰 MVP

### 정책 및 API 계약

- [x] 인증 사용자만 진행 중이며 시작 시각 이상, 마감 시각 미만인 경매에 입찰하도록 확정했다.
- [x] 판매자 본인의 작품 입찰을 금지했다.
- [x] 입찰가는 현재가보다 높은 1원 이상 21억 원 이하의 정수로 제한했다.
- [x] `POST /api/arts/{artId}/bids`와 공통 오류 응답 계약을 확정했다.
- [x] `GET /api/users/me/bids`에서 사용자별 입찰을 작품 단위로 요약하도록 확정했다.
- [x] 조회용 상태를 `ONGOING`, `IMMINENT`, `ENDED`로 정의했다.

### 입찰 등록과 동시성 제어

- [x] 입찰 이력 저장과 `Art.currentPrice` 변경을 하나의 트랜잭션으로 구현했다.
- [x] 작품 행을 `PESSIMISTIC_WRITE`로 잠근 뒤 경매 상태와 최신 현재가를 다시 검증하도록 구현했다.
- [x] 락 대기 힌트를 3초로 설정하고 인식 가능한 락 실패를 `409 BID_CONFLICT`로 변환했다.
- [x] 같은 작품의 동시 입찰에서 낮은 금액이 높은 금액을 덮어쓰지 못하도록 검증했다.
- [x] 서로 다른 작품의 입찰은 서로 차단하지 않으며 트랜잭션 종료 후 락이 해제되는지 검증했다.

### 내 입찰 조회와 프론트 연동

- [x] 로그인 사용자의 입찰을 작품별 최고 입찰가와 최근 입찰 시각으로 집계했다.
- [x] 작품 현재가와 비교한 최고가 여부, 상태, 정렬, 페이지네이션을 구현했다.
- [x] 작품 상세 페이지에 실제 입찰 폼과 성공·오류·재조회 흐름을 연결했다.
- [x] 마이페이지 입찰 현황의 목업을 제거하고 실제 API, 필터, 요약, 빈 목록과 오류 상태를 연결했다.
- [x] 백엔드 전체 테스트와 프론트 빌드·관련 ESLint 및 주요 화면을 검증했다.

### 완료 시 남긴 후속 범위

- [x] 마감 후 낙찰자 확정, `artStatus` 자동 변경, 낙찰 작품 조회와 결제 연계는 후속 작업으로 분리했다.

## 경매 마감·낙찰 MVP

### 마감 결과와 자동 처리

- [x] `Art`에 낙찰 입찰과 실제 종료 시각을 저장하고 `0=진행`, `1=유찰`, `2=낙찰` 상태를 확정했다.
- [x] 입찰 0건은 유찰, 입찰이 있으면 금액·시각·ID 우선순위의 최고 입찰을 낙찰로 저장한다.
- [x] 현재가와 최고 입찰가가 다르면 확정하지 않고 작품 ID와 가격을 오류 로그에 남긴다.
- [x] 10초 주기 스케줄러와 서버 시작 catch-up으로 누락 마감을 자동 재시도한다.
- [x] 작품별 트랜잭션과 비관적 락으로 중복 마감 및 입찰·마감 경합을 제어한다.

### 결과 조회와 마이페이지

- [x] 입찰자의 낙찰 작품 목록과 입찰별 `PENDING`, `WON`, `LOST` 결과 API를 구현했다.
- [x] 작가 작품을 전체·진행·종료로 조회하고 `PENDING`, `SOLD`, `UNSOLD` 결과와 낙찰가·입찰 수를 제공한다.
- [x] 작가 응답에는 낙찰자 ID와 닉네임을 노출하지 않는다.
- [x] 낙찰 목록, 입찰 현황, 작가 작품, 관심 작품과 마이페이지 요약을 실제 API로 연결했다.
- [x] 결제와 리뷰는 MVP 화면에서 제외하고 로딩·오류·재시도·빈 목록·인증 만료 상태를 구현했다.

### 통합 및 운영 검증

- [x] `artId=1`의 레거시 상태 오류가 과거 작품 등록 기본값에서 비롯된 것을 확인하고 입찰 0건 유찰로 보정했다.
- [x] 실제 MySQL에서 작품 등록, 두 사용자 입찰, 자동 마감, 작가 `SOLD`, 낙찰자 목록, 패찰자 `LOST` 흐름을 검증했다.
- [x] 마감 경계 시각, 스케줄러 지연 중 입찰 차단, 실패 작품의 다음 주기 자동 재시도를 검증했다.
- [x] MySQL 세션 락 대기를 3초로 설정하고 1초 락은 대기 후 성공, 5초 락은 약 3.1초 후 `409 BID_CONFLICT`가 반환됨을 실측했다.
- [x] `winning_bid_id`, `closed_at`과 마감 조회 복합 인덱스 `art_status, closing_time, art_id`를 실제 DB에서 확인했다.
- [x] 백엔드 전체 68개 테스트, 프론트 전체 ESLint와 프로덕션 빌드를 통과했다.
- [x] 데스크톱·모바일 화면과 실제 작가·입찰자 계정의 상태 표시를 검증했다.

### MVP 이후 범위

- 결제·배송·환불, 낙찰 포기와 차순위자 승계
- 리뷰 작성 API와 구매 확정
- 마감 임박 자동 연장, 사용자 알림과 실시간 갱신
- 관리자 재처리 화면, 분산 스케줄러 락, 대량 부하 및 운영 모니터링

---

## 작품 수정·삭제

### 정책과 상태 확장

- [x] 인증된 소유 작가만 진행 중이고 마감 전인 작품을 수정·삭제할 수 있도록 정책을 확정했다.
- [x] 입찰 전에는 가격·기간·설명·이미지를 수정하고, 입찰 후에는 비가격 정보만 수정하도록 제한했다.
- [x] 작품명은 수정 대상에서 제외하고 부분 수정에서 미전달 값과 명시적 초기화를 구분했다.
- [x] 입찰 없는 작품은 물리 삭제하고 입찰 있는 작품은 이력을 보존한 채 `CANCELED`로 전환했다.
- [x] 취소 작품을 공개 목록·입찰·마감 대상에서는 제외하고 작가·입찰자 이력에는 취소 결과로 보존했다.
- [x] 삭제 작품의 찜 행은 작품 참조만 분리하고, 리뷰가 있는 작품의 물리 삭제는 데이터 무결성을 위해 거절했다.

### API와 동시성 처리

- [x] `PATCH /api/arts/{artId}`와 `DELETE /api/arts/{artId}` 계약 및 오류 상태를 구현했다.
- [x] 작품 행 비관적 쓰기 잠금 뒤 소유권, 상태, 실제 마감 시각과 입찰 이력을 다시 검사했다.
- [x] 시작가 변경 시 현재가를 동기화하고, 삭제 결과를 `DELETED` 또는 `CANCELED`로 구분해 반환했다.
- [x] 서버 시각 의존성을 `Clock`으로 주입해 마감 경계 조건을 재현 가능하게 했다.

### 검증과 후속 범위

- [x] 권한, 입력값, 수정 허용 범위, 물리 삭제·취소 전환과 관련 조회 회귀 테스트를 완료했다.
- [x] 수정·삭제와 입찰·마감이 경합할 때 잠금 획득 순서에 따라 최신 상태로 판단하는지 검증했다.
- [x] 서로 다른 작품 작업은 불필요하게 차단하지 않고 기존 입찰·마감·조회·등록 테스트가 유지되는지 확인했다.
- [x] 프론트 삭제 확인창과 취소 결과 표시, Cloudinary 원본 이미지 정리는 후속 연동 범위로 남겼다.

---

## 주문 기능

### 주문 도메인과 멱등 생성

- [x] 낙찰 작품마다 주문을 하나만 생성하도록 `orders.art_id` 유니크 제약을 적용했다.
- [x] 구매자·판매자·작품·낙찰가·배송 정보를 주문 시점 스냅샷으로 보존했다.
- [x] 낙찰 확정과 `PAYMENT_PENDING` 주문 생성을 같은 작품 단위 트랜잭션에서 처리했다.
- [x] 스케줄러 재시작·재실행에도 기존 주문을 반환하도록 주문 생성을 멱등 처리했다.
- [x] 주문 생성 실패 시 낙찰 확정도 함께 롤백하고 다음 마감 주기에서 재처리하도록 검증했다.

### 배송지와 주문 상태

- [x] 완전한 기본 배송지는 주문 생성 시 복사하고, 주소가 없거나 불완전해도 배송지 미확정 주문을 생성했다.
- [x] 구매자가 `PAYMENT_PENDING` 동안 주문별 배송지를 입력·재확정하도록 구현했다.
- [x] 배송지 확정 전 결제와 `PAID` 이후 배송지 변경을 차단했다.
- [x] 우편번호를 문자열로 보존하고 수령인·연락처·주소 조합과 길이를 검증했다.
- [x] `PAYMENT_PENDING → PAID → PREPARING → SHIPPED → DELIVERED → CONFIRMED` 정상 전이와 취소·환불 전이를 구현했다.
- [x] 결제 전 포기와 기한 만료는 `CANCELED`, 결제 후 취소는 `REFUNDED`와 별도 사유로 기록했다.
- [x] 주문 생성 후 24시간 결제 기한과 서버 시작 catch-up·주기 만료 처리를 구현했다.
- [x] 결제·만료·낙찰 포기 경합은 주문 행 비관적 잠금 후 최신 상태를 재검사하도록 구현했다.

### 구매자·판매 작가 API와 프론트 연동

- [x] 구매자 주문 목록·상세·배송지 확정·낙찰 포기·구매 확정 API를 구현했다.
- [x] 판매 작가 주문 목록·상세·준비·발송 API를 구현하고 토큰 기준 소유권과 역할을 검증했다.
- [x] 주문 상태 필터·페이지네이션·상태별 집계와 공통 오류 응답을 구현했다.
- [x] 낙찰 작품에서 주문으로 진입하고 구매자 주문 화면의 목업 데이터와 하드코딩 배송지를 제거했다.
- [x] 작가 판매 주문 관리 화면과 준비·발송 처리를 실제 API에 연결했다.
- [x] 구매자·작가 화면의 상태·오류 변환, 로딩·빈 목록·재시도·인증 만료와 중복 요청 방지를 공통 처리로 정리했다.

### 완료 검증과 후속 범위

- [x] 주문 엔티티·상태·주소·저장소·서비스·API 단위 및 통합 테스트를 완료했다.
- [x] 낙찰 마감과 주문 생성, 결제·만료·포기 경합 및 주문 저장 실패 롤백 테스트를 완료했다.
- [x] 프론트 관련 테스트·ESLint·프로덕션 빌드와 데스크톱·모바일 화면을 검증했다.
- [x] 실제 MySQL에서 낙찰 마감부터 주문 생성, 주소 확정, 상태 전이와 기한 만료까지 검증했다.
- 실제 포인트 차감·예치·충전 원장은 다음 `PLAN.md`의 포인트 원장 작업으로 분리했다.
- 네이버페이·토스페이 실제 연동, 차순위 낙찰 승계와 운영 고도화는 `BACKLOG.md`에서 관리한다.

---

# DailyAtelier 6단계 포인트 원장 기능 완료 기록

## 완료 결과와 설계 원칙

- 낙찰 주문 포인트 결제, 입찰 예치, 충전·취소·환불 반대 거래, 동시성·멱등성·실패 재처리를 구현하고 API와 화면까지 연결했다.
- `PointAccount`, 불변 `PointTransaction`, `PointHold`, `PointCharge`와 콜백 inbox를 도입했다.
- 신규 포인트 조회·변경 기준은 `PointAccount`다. `User.reserve`는 기존 데이터 이관과 API 응답 호환을 위한 읽기 전용 필드로 유지했다.
- 잔액 변경은 원장과 같은 트랜잭션에서 처리하며, 취소·환불은 원거래를 보존한 반대 거래로 기록한다.
- 사용 가능 잔액과 예치 잔액을 분리하고 입찰 전액 또는 증액 차액을 예치하며 패찰·취소·기한 만료 시 정확히 한 번 해제한다.
- 포인트 계정 행을 먼저 잠근 뒤 hold·주문·충전을 처리하며, `COMMIT`, hold 확정과 주문 `PAID`는 하나의 트랜잭션으로 처리한다.
- 멱등성 키, PG 주문번호와 공급자 이벤트 ID의 유니크 제약으로 중복 반영을 방지한다.
- 외부 PG는 인터페이스와 메타데이터 경계만 마련하고 권한이 제한된 `INTERNAL` 데모 충전만 연결했다.
- Flyway V1·V2로 포인트 원장 스키마와 외래키 정규화를 적용했다.

## 6단계별 결과

### 1단계 — 스키마와 불변 원장 기반

계정·원장·예치·충전 모델과 DB 제약을 구현하고 기존 `User.reserve`를 이관했다. 사용자별 `OPENING_BALANCE` 1회 생성을 보장했으며, H2 테스트와 MySQL 8.0 Flyway V1·V2 적용·재실행 0건, 인덱스·유니크·CHECK·외래키 및 계정·원장 합계 일치를 확인했다.

### 2단계 — 충전 상태와 내부 결제 어댑터

충전 생성·승인·실패·취소·환불과 `CHARGE`·`REFUND` 원장을 구현했다. 멱등 재호출, 다른 요청 충돌, 금액·권한 검증, 동시 승인 1회 반영과 원장 실패 전체 롤백을 확인했다. 콜백 중복 처리 방지와 최대 5회 재처리를 검증했다.

### 3단계 — 입찰 예치와 차액 재입찰

신규 최고 입찰 전액 예치, 동일 사용자 차액 증액, 패찰 해제와 재입찰 새 hold를 구현했다. 잔액 전액 입찰, 1포인트 부족 거절, 저장 실패 롤백과 작품 취소·입찰 경합의 정확히 한 번 해제를 확인했다. 동일 계정의 동시 입찰에서도 초과 예치가 발생하지 않았다.

### 4단계 — 자동 마감과 낙찰 주문 결제

최고 입찰·현재가·활성 예치 정합성을 검증하고 주문 결제수단 `INTERNAL_POINT`를 저장했다. 주문 `PAID`, `COMMIT`, hold 확정과 포기·만료 예치 해제를 원자적으로 처리했다. 결제·만료·포기·환불 경합과 실패 롤백을 검증했으며 백엔드 전체 196건을 통과했다.

### 5단계 — API와 프론트 연동

사용 가능·예치 포인트, 거래·충전 내역 API와 내부 충전·주문 결제 화면을 연결했다. 주문 결제에 `Idempotency-Key`를 적용하고 중복 클릭 및 409 후 재조회를 구현했다. 백엔드 전체 201건 중 198건 통과, 3건 제외와 프론트 단위 테스트 9건, ESLint·Vite 빌드 통과를 확인했다.

### 6단계 — 전체 동시성·MySQL 검증

- H2 백엔드 전체 206건 중 201건을 통과했고 MySQL 전용 5건은 환경 조건으로 제외했다.
- 다수 작품·사용자 동시 입찰에서 활성 예치와 모든 참여 계정의 원장 합계 일치를 확인했다.
- MySQL 8.0 마이그레이션 복구·재실행과 인덱스·유니크·CHECK·외래키를 확인했다.
- MySQL `REPEATABLE-READ`, 동일 계정 행 잠금 직렬화, 교착 감지와 부분 변경 롤백을 확인했다.
- 프론트 단위 테스트 9건, ESLint와 Vite 프로덕션 빌드를 통과했다.

## 최종 검증

- 잔액 초과 사용과 동일 금액의 중복 예치가 발생하지 않는다.
- 패찰·취소 시 예치가 정확히 한 번 해제되고 낙찰 결제가 정확히 한 번 확정된다.
- 주문 `PAID`와 `COMMIT`이 함께 존재하며 실패 시 주문·hold·잔액·원장이 모두 롤백된다.
- 환불은 원거래를 보존한 하나의 반대 거래이며 멱등 재호출·중복 콜백은 잔액을 추가 변경하지 않는다.
- 계정 잔액과 원장 합계의 일치를 H2·MySQL 및 동시성 시나리오에서 확인했다.

## 운영 조회 SQL

실패 또는 미처리 콜백:

```sql
SELECT callback_event_id, provider, provider_event_id, pg_order_id,
       status, attempt_count, last_error, received_at, processed_at
FROM payment_callback_event
WHERE status IN ('RECEIVED', 'FAILED')
ORDER BY received_at;
```

최대 재시도 도달 콜백:

```sql
SELECT callback_event_id, provider, provider_event_id, attempt_count,
       last_error, received_at
FROM payment_callback_event
WHERE status = 'FAILED' AND attempt_count >= 5
ORDER BY received_at;
```

계정과 원장 잔액 불일치:

```sql
SELECT account.user_id,
       account.available_balance,
       account.held_balance,
       COALESCE(SUM(tx.available_delta), 0) AS ledger_available_balance,
       COALESCE(SUM(tx.held_delta), 0) AS ledger_held_balance
FROM point_account account
LEFT JOIN point_transaction tx ON tx.user_id = account.user_id
GROUP BY account.user_id, account.available_balance, account.held_balance
HAVING account.available_balance <> COALESCE(SUM(tx.available_delta), 0)
    OR account.held_balance <> COALESCE(SUM(tx.held_delta), 0)
ORDER BY account.user_id;
```

## 후속 BACKLOG

- 네이버페이 실제 결제 준비·승인·취소와 웹훅 서명 검증
- 토스페이 실제 결제 요청·승인·취소와 웹훅 서명 검증
- 외부 PG 실패 inbox 운영 재처리와 PG 정산·포인트 원장 대사
- 내부 포인트와 외부 결제를 조합하는 복합 결제
- 카드·계좌이체와 결제수단별 부분 취소·부분 환불













# 핵심 도메인 품질 감사 완료 기록

## 완료 결과

- `dev`를 기준으로 사용자 흐름, 소유권·권한, 트랜잭션·동시성, 결제 콜백·포인트 원장, Flyway, 프론트 상태, 구조·응답 일관성의 일곱 영역 감사를 완료했다.
- 상세 결과와 파일·테스트 근거, 재현·검증 방법, 권장 수정안은 `QUALITY_AUDIT.md`에 보존했다.
- 발견사항은 `QA-001`~`QA-014`이며, 확정 결함과 잠재 위험 및 P0~P2 우선순위를 구분했다.
- 운영 DB·실제 PG·부하·실행 계획·락 타임아웃 등 실행하지 않은 검증은 `BACKLOG.md` 대상으로 유지했다.
- 감사 단계에서는 코드, 테스트, 설정 파일을 수정하지 않았고, 수정 작업은 `PLAN.md`의 별도 실행 계획으로 분리했다.

### 발견사항 결과

| ID | 심각도 | 판정 | 요약 | 우선순위 | 처리 상태 |
| --- | --- | --- | --- | --- | --- |
| QA-001 | 높음 | 확정 결함 | `SHIPPED → DELIVERED` 실제 호출 경로가 없어 구매 확정 흐름이 중단됨 | P0 | 이번 수정 대상 |
| QA-002 | 중간 | 확정 결함 | 구매자 요청·판매자 승인 환불 정책이 API·화면에 연결되지 않음 | P1 | 이번 수정 대상 |
| QA-003 | 높음 | 확정 결함 | 운영 데모 충전임을 명시하거나 발행량을 제한하는 장치가 없음 | P0 | 이번 수정 대상 |
| QA-004 | 높음 | 잠재 위험 | 콜백 처리기 예외 흡수로 부분 커밋 또는 rollback-only 충돌 가능성이 있음 | P0 | 이번 수정 대상 — 트랜잭션 재현 테스트 선행 |
| QA-005 | 중간 | 확정 결함 | 정합성 검사가 계정 잔액과 원장 합계 외의 의미적 불일치를 놓침 | P1 | 이번 수정 대상 |
| QA-006 | 낮음 | 잠재 위험 | 동시 최초 콜백의 유니크 충돌을 멱등 성공으로 복구하지 않음 | P2 | BACKLOG — 실제 PG 콜백 진입점 도입 시 함께 검증 |
| QA-007 | 높음 | 확정 결함 | 현재 Flyway 이력만으로 빈 MySQL DB를 생성할 수 없음 | P0 | 이번 수정 대상 |
| QA-008 | 높음 | 잠재 위험 | 기존 활성 경매·주문의 포인트 예치와 원장 관계를 이관하지 않음 | P0 | 이번 수정 대상 — 기존 데이터 조사·이관 정책 선행 |
| QA-009 | 중간 | 잠재 위험 | 기존 null `reserve`가 V1의 `NOT NULL` 변경을 중단시킬 수 있음 | P1 | 이번 수정 대상 |
| QA-010 | 중간 | 확정 결함 | 충전 초기 조회 전·실패 후에도 0원 잔액과 충전 가능 상태를 표시함 | P1 | 해결 — 구현·자동 검증 및 정상 충전 브라우저 검증 완료 |
| QA-011 | 낮음 | 확정 결함 | 이전 중복확인 응답이 변경된 회원가입 입력을 검증 완료로 표시함 | P2 | 해결 — 구현 및 자동 회귀 검증 완료 |
| QA-012 | 낮음 | 잠재 위험 | 실패한 충전 후 금액을 바꿔도 이전 멱등성 키를 재사용함 | P2 | 해결 — 요청 키 수명 자동 검증 완료 |
| QA-013 | 중간 | 확정 결함 | 도메인별 예외 처리 범위가 달라 오류 응답 계약이 일관되지 않음 | P1 | 이번 수정 대상 |
| QA-014 | 낮음 | 잠재 위험 | 포인트 목록의 음수 페이지가 명시적인 400 API 오류로 변환되지 않음 | P2 | 해결 — 자동 및 로컬 API 검증 완료 |

# Flyway 최종 기준 스키마 재구성 완료 기록

## 변경 결정

- 실제 운영 DB와 보존해야 할 공유 개발 DB가 없는 개발 단계라는 전제에서 기존 개발 DB는 업그레이드하지 않고 승인 후 수동 재생성하는 대상으로 확정했다.
- 기존 V0~V6의 구현 이력은 이 문서와 Git 이력에 보존하되, 현재 최종 스키마는 신규 `V1__create_baseline_schema.sql`이 대체한다.
- 신규 V1에는 기존 포인트 잔액·원장 생성 DML과 조건부 `ALTER` 등 레거시 DB 호환성 SQL을 포함하지 않았다.
- 신규 V1 적용 이후의 변경은 적용된 migration을 수정하지 않고 V2 이상으로 누적한다.

## QA 처리 결과

- `QA-007`: 해결. 신규 V1이 빈 MySQL에서 전체 스키마를 생성하고 Hibernate `ddl-auto=validate`를 통과했다.
- `QA-008`: 현재 프로젝트 범위에서 적용 대상 없음. 운영·공유 DB와 활성 거래 이관 대상이 없으며 기존 개발 DB는 재생성하므로 활성 경매·주문 이관을 구현하지 않았다.
- `QA-009`: 현재 프로젝트 범위에서 적용 대상 없음. 신규 V1이 `users.reserve`를 `INT NOT NULL DEFAULT 0`으로 직접 생성하므로 레거시 null 정규화가 필요하지 않다.

## 구현과 검증

- 구현 커밋: `6166197 fix(backend): Flyway 최종 기준 스키마 재구성`
- 별도 빈 MySQL 스키마에서 신규 V1 단독 적용과 14개 최종 테이블 생성을 확인했다.
- 일반 인덱스 9개, 유니크 제약 9개, 외래키 26개, CHECK 제약 9개와 조건식을 확인했다.
- Hibernate `ddl-auto=validate` 컨텍스트 기동, 두 번째 migrate 실행 0건과 14개 업무 테이블 데이터 0건을 확인했다.
- 기존 개발 DB에는 접속하거나 변경하지 않았고 테스트 스키마도 삭제하지 않았다.

# 품질 감사 QA-004·QA-011·QA-013·QA-014 완료 기록

## QA-004 결제 콜백 트랜잭션 경계

- 구현 커밋: `b6a5399 fix(payment): 콜백 실패 트랜잭션 경계 명확화`
- 처리기 업무 변경과 실패 inbox 기록의 트랜잭션을 분리했다.
- H2 기반 테스트에서 처리기 DB 변경 후 예외 시 업무 변경 롤백, `FAILED` 상태·시도 횟수 기록과 재처리 성공을 확인했다.
- 실제 PG 처리기와 MySQL 동시 최초 수신은 검증하지 않았으며 `QA-006`과 함께 BACKLOG로 유지한다.

## QA-013 공통 오류 응답 계약

- 구현 커밋: `33430ea refactor(backend): API 예외 응답 계약 통합`
- 회원·작품 도메인 오류를 공통 예외 계약으로 전환하고 전역 처리기에서 `timestamp`, `status`, `code`, `message`, `path` 응답을 생성하도록 통일했다.
- 주문·입찰·포인트·보안의 기존 상태·코드·메시지를 유지하면서 대표 400·401·403·404·409 응답 계약을 검증했다.
- 백엔드 테스트 231건은 실패·오류 0건, 제외 7건이었고 프론트 유틸리티 11건과 컴포넌트 6건, ESLint와 production build가 통과했다.
- 이 구현 시점에는 `QA-014` 포인트 페이지 검증을 포함하지 않았으며 후속 커밋으로 분리했다.

## QA-011 회원가입 비동기 중복확인 경합

- 구현 커밋: `b42ecd7 fix(frontend): 회원가입 중복확인 응답 경합 방지`
- 일반·작가 회원가입에서 요청 식별자와 입력값 스냅샷을 비교해 현재 입력과 일치하는 최신 중복확인 응답만 반영하도록 공통 훅으로 통합했다.
- 입력 변경 시 이전 요청과 검증 상태를 무효화하고, 확인 중이거나 미확인 상태에서는 제출을 차단한다.
- 두 화면에 지연 응답, 응답 순서 역전, 실패 후 재확인, 서버 중복 거부와 정상 제출을 포함한 컴포넌트 테스트 8건을 추가했다.
- 전체 프론트 유틸리티 테스트 11건과 컴포넌트 테스트 14건, ESLint와 production build가 통과했다. 전체 백엔드 테스트는 235건 중 실패·오류 0건, 제외 7건이었다.
- 브라우저에서 일반·작가 계정을 실제 생성하고 입력 변경 무효화, 미확인 상태 제출 차단과 늦은 실패 응답 경합을 확인했다.
- 늦은 성공 응답 경합은 로컬 응답이 빨라 브라우저에서 재현하지 못했으며, 응답 순서 역전 컴포넌트 테스트로 검증했다.

## QA-014 포인트 페이지 요청 검증

- 구현 커밋: `c6c94e8 fix(backend): 포인트 페이지 요청 검증 추가`
- 거래·충전 내역 조회에서 `page < 0`, `size < 1`, `size > 50`을 거부하고 `INVALID_POINT_REQUEST` 구조화된 400 응답으로 변환했다.
- 음수 page, size 0, size 51과 정상 기본 요청 및 size 50을 API·서비스 테스트로 검증했다.
- 백엔드 테스트 235건은 실패·오류 0건, 제외 7건이었고 프론트 유틸리티 11건과 컴포넌트 6건, ESLint와 production build가 통과했다.
- `QA-011`은 이 구현 시점에는 포함하지 않고 후속 커밋으로 분리했으며, `QA-006`은 실제 PG 도입 전까지 BACKLOG로 유지한다.

## 후속 브라우저 회귀 결과

### 포인트 충전·내역

- 초기 조회 중 충전 버튼 비활성화와 잔액 자리표시자, 조회 완료 후 잔액과 데모 충전 안내를 확인했다.
- 50,000P 충전 후 잔액과 충전·거래 내역이 갱신됐으며 화면 재조회와 새로고침 후에도 충전 1건·원장 1건으로 유지돼 중복 적립이 없었다.
- 거래·충전 내역의 정상 `page=0`, `size=20` 요청은 200을 반환했다. `page=-1`, `size=0`, `size=51`은 두 목록 모두 공통 오류 필드와 `INVALID_POINT_REQUEST`를 가진 400을 반환했다.
- 조회 실패 후 재조회와 응답 유실 뒤 동일 요청 재시도는 이번 브라우저 검증에서 장애를 주입하지 않아 자동 테스트 범위로 유지한다.

### 주문 목록

- 일반 회원 구매 주문 목록과 작가 회원 판매 주문 관리 화면에서 주문 0건 통계와 빈 상태를 확인했다.
- 이후 생성한 환불 주문 1건으로 구매자 목록의 `환불` 상태와 상세의 환불 완료 안내, 판매자 목록·상세의 환불 상태와 승인·거절 버튼 미노출을 확인했다. 새로고침 후에도 같은 상태가 유지됐다.
- 구매자·판매자 목록·상세 API는 모두 주문 상태 `REFUNDED`, 환불 요청 상태 `APPROVED`, 가능한 작업 빈 목록을 반환했다.
- 배송 완료, 구매 확정, 환불 요청·승인·거절 실행과 중복 재요청의 전체 브라우저 흐름은 미실행 상태로 유지한다.

## 환불 요청 완료 상태 전이 후속 수정

- 구현 커밋: `76e7db9 fix(backend): 환불 요청 완료 상태 전이 추가`
- 환불 승인 시 주문 상태 `REFUNDED`와 함께 환불 요청 상태를 `REQUESTED → APPROVED`로 전이한다.
- 승인 완료 후 동일 승인은 멱등 성공하며 동일 거절도 기존 멱등 성공을 유지한다. 완료 상태에서 반대 작업은 충돌로 차단한다.
- `V2__normalize_approved_refund_requests.sql`로 기존 `REFUNDED + REQUESTED` 데이터만 `APPROVED`로 보정했고, 로컬 백엔드 기동 시 Flyway 적용을 확인했다.
- 전체 백엔드 테스트 236건은 실패·오류 0건, 제외 7건이었다. 환불 승인 시 상태 전이, 동일 작업 멱등성, 반대 작업 충돌, 포인트 환불 원장 1회 처리와 구매자·판매자 조회 일관성을 포함한다.
- 구매자·판매자 브라우저 최종 회귀와 목록·상세 API 대조가 통과했다. 구매자 목록의 `환불` 문구를 `환불 완료`로 명확히 하는 개선은 BACKLOG로 유지한다.

## 주문 화면 환불 상태 표시와 낙찰 요약 동선 개선

- `2829c0d feat(frontend): 환불 상태 표시 체계 정리`와 `7a1a17a feat(frontend): 주문 상세 환불 처리 결과 표시`로 구매자·판매자 주문 목록과 상세에 환불 요청 사유·요청 시각·처리 결과·처리 시각을 구분해 표시했다.
- 환불 완료 문구와 포인트 반환 안내를 명확히 하고, 처리 완료 주문에서는 승인·거절 작업을 노출하지 않도록 정리했다.
- 마이페이지 홈의 낙찰 작품 요약마다 `/mypage/order-status?artId={artId}` 형식의 `주문 확인` 링크를 추가하고 진행 중 입찰과 로딩·오류·빈 상태에는 노출하지 않았다.
- 프론트 유틸리티 테스트 12건과 컴포넌트 테스트 19건, ESLint와 production build를 통과했다.
- 작품 상세에서 관련 주문을 탐색하는 동선과 권한·접근 불가 상태별 안내는 `BACKLOG.md`에 유지한다.

# 품질 감사 수정 계획 최종 종료 기록

## 최종 사용자 흐름 검증

- 배송 완료 후 구매 확정 성공을 확인했다.
- 환불 요청 후 작가 승인과 거절 흐름을 각각 확인했다.
- 마이페이지 낙찰 작품의 `주문 확인` 링크가 해당 주문 상세를 여는 것을 확인했다.
- 구매자 주문 화면과 작가 판매 주문 화면의 상태 문구가 정상임을 확인했다.
- 환불 중복 요청 차단은 주문 작업 중복 방지 유틸리티와 백엔드 상태 충돌·멱등성 자동 테스트로 대체 검증했다.
- 포인트 초기 조회 실패 상태와 재조회 복구는 `Charge` 컴포넌트 테스트로 대체 검증했다.
- 검증 중 브라우저 콘솔에서 새 애플리케이션 오류가 확인되지 않았다.

## 최종 자동 회귀 검증

- 백엔드 전체 테스트 236건 중 실패·오류 0건, 환경 의존 테스트 7건 제외를 확인했다.
- 프론트 유틸리티 테스트 12건과 컴포넌트 테스트 19건이 모두 통과했다.
- ESLint와 production build가 통과했다.
- 브라우저 장애 주입, 실제 PG·운영 MySQL·부하·락 검증 등 운영 환경 의존 범위는 기존 `BACKLOG.md`에서 계속 관리한다.

위 검증을 기준으로 품질 감사 발견사항 수정 실행 계획을 종료하고 `PLAN.md`를 신규 계획 대기 상태로 정리했다.

## 작가 검색 및 최소 작가 상세 페이지

- 구현 커밋: `720006a feat(backend): 작가 검색 조회 기반 추가`, `cb97a85 feat(backend): 공개 작가 조회 API 추가`, `b2e92ff feat(frontend): 작가 목록과 상세 페이지 추가`, `196ed91 feat(frontend): 헤더 작품·작가 검색 연결`
- 작가 권한 사용자만 대상으로 작가명 부분 일치·대소문자 무시 검색과 전체 목록을 제공하고, 작가명과 공개 식별자 기준의 안정적인 정렬을 적용했다.
- 작가 목록과 최소 상세 화면에 기본 프로필 이미지, 전체 소개, 실제 입찰 가능한 진행 작품 수와 공개 작품 페이지를 제공했다. 공개 작품에서는 취소 상태를 제외하고 마감 임박순과 작품 ID 역순을 적용했다.
- `/artists`의 검색어·페이지와 상세 작품 페이지를 URL에 유지하고 로딩, 빈 결과, 오류·재시도, 없는 작가 상태를 처리했다.
- 헤더에 작품·작가 검색 유형을 추가했다. 작품 검색은 기존 `/search?q=...` 계약을 유지하고 작가 검색은 `/artists?keyword=...`로 연결했다.
- 백엔드 전체 테스트, 프론트 유틸리티 테스트 12건, 컴포넌트 테스트 32건, ESLint, production build와 `git diff --check`가 통과했다.
- 브라우저에서 URL 직접 진입, 뒤로 가기, 페이지 이동, 재시도, 빈·공백·대소문자 검색, 검색 결과 없음, 없는 작가, 데스크톱·모바일 헤더 동작을 확인했다.
- 로컬 테스트 작가로 `김하` 부분 검색, 영문 대소문자 무시 검색, 전체 목록, 결과 없음과 동명이인·유사 이름 정렬을 확인했다.

# 판매자 최소 입찰 증분 설정 완료 기록

## 완료 내용

- 구현 커밋: `620e0f4 feat(backend): 최소 입찰 증분 정책 추가`,
  `5194f5e feat(frontend): 입찰 증분 설정과 다음 입찰가 안내`
- 작품에 최소 입찰 증분을 저장하고 생성·수정·상세 응답에서 일관된 계약을 제공했다.
- 기본값 1,000원, 100원~10,000,000원 범위와 100원 배수 제한을 적용했다.
- 첫 입찰·후속 입찰·동일 사용자 재입찰 모두 최신 현재가와 증분으로 검증하고,
  작품 잠금 뒤 후행 요청을 다시 검증하도록 했다.
- 경매 시작 전에는 증분 수정이 가능하고 시작 시각부터는 409
  `MINIMUM_BID_INCREMENT_LOCKED`로 차단했다.
- 판매 등록 화면에 도움말·실시간 첫 입찰가 예시·validation을, 작품 상세에 현재가·
  다음 입찰 가능 금액·최소 증분을 추가했다.

## DB 복구와 마이그레이션 검증

- 로컬 `dailyatelier` DB의 V4 부분 적용 상태를 기존 데이터 삭제나 초기화 없이
  복구했다.
- 기존 작품 4건의 ID와 행 수를 보존하고 `minimum_bid_increment`를 1,000으로
  백필했으며 DEFAULT 1,000과 `chk_art_minimum_bid_increment` CHECK를 확인했다.
- CHECK 표현은 MySQL이 정규화한 `% 100`과 원본의 `MOD(..., 100)`을 동등하게
  검증했다.
- 스키마·데이터가 V4 최종 상태와 일치한 뒤 Flyway 실패 이력을 정상화했고,
  V1~V4 성공, Hibernate `ddl-auto=validate`, Flyway 재실행 migration 0건을 확인했다.
- 격리된 빈 MySQL과 V1→V3 기존 데이터 fixture에서도 V4 적용, 행·식별자 보존,
  1,000원 백필과 재실행 0건을 확인했다.

## 자동 검증 결과

- 백엔드 관련 DTO·서비스·API·트랜잭션·동시성 테스트와 전체 회귀 테스트가 통과했다.
- 프런트 증분 등록·상세 전용 테스트와 전체 테스트, ESLint와 production build가
  통과했다.
- 시작가·증분 경계, 첫·후속·재입찰, 잠금 후 최신가 검증, 포인트 차액 예치,
  마감·낙찰·주문 회귀를 확인했다.

## 브라우저 QA 결과

- 로컬 전용 작가 1명과 일반 회원 2명을 정상 회원가입 경로로 준비하고 실제 로그인과
  역할을 검증했다. 계정 현황은 Git 제외 문서 `_local/test-accounts.md`에 정리했다.
- QA 작품 ID 5~9를 보존했으며 기존 INTERNAL 데모 충전 API로 두 구매자에게 각각
  1,000,000P를 준비했다.
- 판매 등록 validation과 반응형 화면, 경매 전 PATCH 성공·시작 후 409, 상세 금액,
  정확한 최소가 첫 입찰, 최소가 미달·비배수 실제 입찰가, 재입찰과 예치 차액,
  두 구매자 stale-response, 마감·낙찰·주문 생성을 확인했다.
- 다음 최소 입찰가 2,100,000,000원 표시는 확인했다. 다만 정상 데모 충전 경로의
  계정당 1,000,000P 상한 때문에 마지막 유효 입찰 후 nullable 다음 최소가와 disabled
  상태는 실행하지 않았고, 직접 DB 조작도 하지 않았다. 이 한 건은 `BACKLOG.md`의
  로컬 전용 고액 QA fixture 후속 항목으로 유지한다.

# 프런트 접근성 P1 개선 완료 기록

## 완료 내용

- 구현 커밋: `503d78a fix(frontend): 헤더 메뉴 키보드 접근성 개선`,
  `db095f4 fix(frontend): 인증 폼 오류 접근성 보완`,
  `6b4fb26 fix(frontend): 포커스 표시와 동작 감소 설정 보완`
- 문서 커밋: `0c301cd chore: 프론트 접근성 백로그 완료 처리`
- 헤더 모바일 메뉴와 데스크톱 드롭다운에 펼침 상태, 제어 대상, Escape 종료와
  호출 요소 포커스 복귀를 적용하고 모바일 메뉴가 열린 동안 배경을 차단했다.
- 로그인과 일반·작가 회원가입 입력에 지속적으로 보이는 label과 오류 연결을 추가하고,
  제출 성공·실패 안내와 오류 포커스 정책을 보완했다.
- 전역 `focus-visible` 표시를 제공하고 reduced-motion 처리는 전역 motion 제거 없이
  홈 자동 슬라이드, smooth scroll과 확인된 비필수 transition으로 제한했다.

## 자동 검증 결과

- ESLint, 단위 테스트 15개, 컴포넌트 테스트 61개와 production build가 통과했고
  `git diff --check` 오류가 없었다.
- Header와 인증 폼 테스트는 변경 원인에 맞춰 각각 첫 번째와 두 번째 기능 커밋에
  포함했다.

## 브라우저 QA 결과

- 390px 모바일 메뉴의 최초 포커스, 배경 스크롤 차단, Escape 종료와 포커스 복귀,
  가로 overflow 없음 및 데스크톱 trigger의 포커스 복귀를 확인했다.
- 작가 회원가입 입력 10개의 label 연결과 `aria-describedby` 대상 존재를 확인했다.
- 브라우저 환경에서 reduced-motion 강제 에뮬레이션과 200% 확대 전체 회귀는 수행하지
  못했다. 구현은 smooth scroll, 홈 자동 재생과 확인된 비필수 transition으로 제한했다.

# 공통 dialog 키보드·스크린리더 접근성 개선 완료 기록

## 완료 내용

- 구현 커밋: `48c9f32 feat(frontend): 공통 접근성 dialog 기반 추가`,
  `f42854a fix(frontend): 기존 dialog 접근성 계약 적용`
- 문서 커밋: `e35560d chore: 공통 dialog 접근성 검증 결과 정리`
- 작품 원본 이미지와 구매자·작가 리뷰 dialog 3곳을 공통 Portal 기반으로 전환하고
  최초 포커스, Tab·Shift+Tab 순환, Escape 종료와 opener 포커스 복귀를 적용했다.
- `dialog`, `aria-modal`, 제목과 선택적 설명 연결 계약을 제공하고 배경 root의 inert,
  기존 body overflow 보존·복원과 unmount·라우트 이동 cleanup을 공통 처리했다.
- 제거된 opener는 오류 없이 복귀를 생략하고 overlay 클릭 닫기와 내부 클릭 유지 정책을
  공통 처리했다.
- Portal 전환으로 작품 이미지 크기가 0이 되던 기존 CSS 회귀를 보완했으며 배송지
  재확정 modal 등 새로운 기능은 구현하지 않았다.

## 자동 검증 결과

- ESLint, 단위 테스트 15개, 컴포넌트 테스트 68개와 production build가 통과했고
  `git diff --check` 오류가 없었다.
- 기존 overflow 값 복원, 제거된 opener, focus trap, overlay·내부 클릭과 cleanup을
  컴포넌트 테스트로 검증했다.

## 브라우저 QA 결과

- 데스크톱의 세 dialog와 390px 작품·작가 리뷰 dialog에서 최초 포커스, Tab·Shift+Tab
  순환, Escape·버튼 종료, opener 복귀와 모바일 경계 내 레이아웃을 확인했다.
- 배경 inert, body scroll 잠금·복원, 열린 상태의 라우트 이동 cleanup, Portal 렌더링과
  콘솔 오류 없음도 확인했다. 구매자 리뷰 동작은 컴포넌트 회귀 테스트로 보완했다.
- Flyway는 기존 4개 migration과 schema version 4를 검증했으며 추가 migration이나
  QA 목적의 DB 데이터 변경 없이 백엔드를 기동했다.

# 로그인 후 원래 작업 위치 복귀 완료 기록

## 완료 내용

- 구현 커밋: `30c21d6 fix(auth): 로그인 후 안전한 원래 위치 복귀`
- 문서 커밋: `df0314e chore: 로그인 복귀 검증 결과 정리`
- React Router location state의 pathname, search와 hash를 안전한 내부 복귀 경로로
  변환하고 로그인 성공 후 한 번만 소비하도록 했다.
- 보호 라우트, 작품 상세의 입찰·찜 로그인 유도와 구매자·판매자 주문 화면의 API 401
  이동이 같은 복귀 계약을 사용하도록 연결했다.
- 절대 URL, protocol-relative URL, 백슬래시 URL, 인증 화면 순환과 손상된 state는
  홈으로 대체하고 직접 로그인은 기존 홈 이동을 유지했다.
- 로그인 전 실패한 변경 요청의 자동 재실행과 작가 역할 guard는 범위에서 제외했다.

## 자동 검증 결과

- ESLint, 단위 테스트 15개, 컴포넌트 테스트 78개와 production build가 통과했고
  `git diff --check` 오류가 없었다.
- 보호 라우트, 작품 상세 로그인 유도, 구매자·판매자 주문 401, 기본 홈 이동과 안전하지
  않은 복귀 값 차단을 컴포넌트 테스트로 검증했다.

## 브라우저 QA 결과

- 보호된 주문 URL의 query·hash 복귀, 작품 상세 로그인 복귀, 직접 로그인 시 홈 이동과
  다른 탭에서 인증이 해제된 뒤 현재 주문 화면 복귀를 확인했다.
- 브라우저 콘솔 오류가 없었고 실패한 변경 요청을 자동 재실행하는 동작은 추가하지 않았다.

# 작가 마이페이지 메뉴 구조 분리와 전용 route guard 완료 기록

## 완료 내용

- 구현 커밋: `9e85ccc fix(auth): 작가 전용 라우트 접근 계약 추가`,
  `763ff69 fix(frontend): 작가 마이페이지 메뉴 구조 개선`
- 네 작가 전용 라우트를 로그인 보호 안의 공통 `ArtistRoute`로 묶고 일반 회원은 자식
  화면과 API가 마운트되기 전에 접근 가능한 권한 안내를 보도록 했다.
- 비로그인은 기존 로그인 복귀 계약으로 원래 URL을 보존하고 작가는 기존 화면에 정상
  진입하도록 했으며, 페이지 내부의 중복 alert·redirect만 제거했다.
- 마이페이지의 일반 회원 메뉴와 작가 관리 메뉴를 별도 navigation으로 분리하고 실제
  링크, `aria-current`, 그룹별 시각 표현과 작은 화면 줄바꿈을 적용했다.
- 백엔드 역할 권한과 주문 UI는 변경하지 않았다.

## 자동 검증 결과

- ESLint, 단위 테스트 15개, 컴포넌트 테스트 83개와 production build가 통과했고
  `git diff --check` 오류가 없었다.
- 비로그인·일반·작가 route 계약, 자식 렌더링 차단, 메뉴 역할별 노출과 링크 목적지를
  컴포넌트 테스트로 검증했다.

## 브라우저 QA 결과

- 비로그인의 작가 URL 로그인 이동과 복귀, 일반 회원의 네 전용 URL 접근 차단, 작가의
  작품 등록·작품 관리·작품 리뷰·판매 주문 화면 정상 진입을 확인했다.
- 1440px, 768px, 390px과 200% 확대에서 두 메뉴 그룹이 페이지 가로 overflow를 만들지
  않고 현재 위치와 키보드 focus-visible이 표시되는 것을 확인했다.
- 일반 회원 토큰으로 내 작품과 판매 주문 API가 403을 반환해 기존 서버 권한 보호가
  유지되는 것을 확인했으며 브라우저 콘솔 오류는 없었다.

## 홈 종료 작품 mock 제거·실제 데이터 연결

### 구현 결과

- 공개 작품 검색 응답에 `SOLD`·`UNSOLD` 종료 결과를 추가하고, 마감 시각 내림차순과 작품 ID 내림차순을 사용하는 `RECENTLY_ENDED` 정렬을 제공했다.
- 기존 `ENDED` 조회가 판매·유찰·시간상 종료 작품을 포함하고 취소 작품을 제외하는 계약을 유지했다.
- 홈 종료 작품 mock과 문자열 작품 ID를 제거하고 실제 검색 API에서 최근 종료 작품 6개를 조회하도록 연결했다.
- 전체·디지털·실물 탭, 이전 요청 취소, 판매·유찰 배지와 낙찰가·최종가, 이미지 fallback, 로딩·오류·빈 상태와 재시도를 적용했다.
- 종료 작품 전체 보기에서 `status=ENDED`, `sort=RECENTLY_ENDED`와 선택한 작품 형식을 검색 화면에 보존한다.

### 자동 검증 결과

- 백엔드 검색 서비스·저장소·컨트롤러 테스트 12개가 통과했다.
- 프론트 단위 테스트 19개와 전체 컴포넌트 테스트 104개가 통과했다.
- 홈 종료 작품 테스트에서 최초 조회 파라미터, 실제 숫자 ID, 판매·유찰 표시, 탭별 최신 요청, 로딩·오류·빈 상태와 재시도를 검증했다.
- ESLint, production build와 `git diff --check`가 통과했고 한글 테스트 문자열을 UTF-8로 확인했다.

### 브라우저 QA 결과

- 사용자가 준비된 로컬 판매·유찰·디지털·실물 데이터를 사용해 최소 브라우저 체크리스트 전체 통과를 확인했다.
- 홈 실제 종료 작품 표시, 판매·유찰 가격 문구, 전체·디지털·실물 탭, 숫자 작품 ID 상세 이동과 종료 작품 전체 보기 URL을 확인했다.
- 이전 컴파일 결과의 8080 서버가 `RECENTLY_ENDED`를 400으로 거절한 문제는 변경된 백엔드를 다시 빌드·실행해 해소했고, 같은 요청의 200 응답과 실제 `SOLD`·`UNSOLD` 결과를 확인했다.
- 이미지 fallback, 빈 상태, 오류와 재시도는 자동화 테스트로 보완했으며 추가 필수 수동 검증 항목은 남지 않았다.
